{"config":{"lang":["en"],"separator":"[\\s\\-]+","pipeline":["stopWordFilter"],"fields":{"title":{"boost":1000.0},"text":{"boost":1.0},"tags":{"boost":1000000.0}}},"docs":[{"location":"","title":"Agent Skills SDK","text":"<p>A Python SDK for discovering, retrieving, and serving Agent Skills to LLM agents.</p>"},{"location":"#what-this-site-covers","title":"What this site covers","text":"<ul> <li>How to integrate skills into agent frameworks</li> <li>Provider and registry concepts behind progressive disclosure</li> <li>Per-package API reference generated from docstrings</li> <li>Project roadmap and architecture decisions (ADRs)</li> </ul>"},{"location":"#install","title":"Install","text":"<p>Pick one provider plus one integration:</p> <pre><code>pip install agentskills-fs agentskills-langchain\n</code></pre> <p>Or install the authoring CLI:</p> <pre><code>pip install agentskills-tools\n</code></pre>"},{"location":"#quick-start","title":"Quick start","text":"<pre><code>import asyncio\nfrom pathlib import Path\n\nfrom agentskills_core import SkillRegistry\nfrom agentskills_fs import LocalFileSystemSkillProvider\n\n\nasync def main() -&gt; None:\n    registry = SkillRegistry()\n    await registry.register_all(LocalFileSystemSkillProvider(Path(\"my-skills\")))\n\n    catalog = await registry.get_skills_catalog(format=\"xml\")\n    print(catalog)\n\n    skill = registry.get_skill(\"incident-response\")\n    print(await skill.get_body())\n\n\nasyncio.run(main())\n</code></pre> <p>Continue with Getting Started and Concepts.</p>"},{"location":"DEVELOPMENT/","title":"Development","text":"<p>Part of the Agent Skills SDK.</p>"},{"location":"DEVELOPMENT/#prerequisites","title":"Prerequisites","text":"<ul> <li>Python 3.12 or newer</li> <li>Poetry 2.0+</li> </ul>"},{"location":"DEVELOPMENT/#setup","title":"Setup","text":"<pre><code>poetry install\n</code></pre> <p>This creates a <code>.venv</code> in the project root and installs all packages in editable mode along with dev dependencies (pytest, ruff).</p>"},{"location":"DEVELOPMENT/#testing","title":"Testing","text":"<p>Run the full test suite from the repository root:</p> <pre><code>poetry run pytest packages/ -v\n</code></pre> <p>Run tests for a single package:</p> <pre><code>poetry run pytest packages/core/agentskills-core -v\npoetry run pytest packages/adapters/agentskills-adapters -v\npoetry run pytest packages/providers/agentskills-fs -v\npoetry run pytest packages/providers/agentskills-http -v\npoetry run pytest packages/integrations/agentskills-langchain -v\npoetry run pytest packages/integrations/agentskills-agentframework -v\npoetry run pytest packages/integrations/agentskills-mcp-server -v\npoetry run pytest packages/tools/agentskills-tools -v\npoetry run pytest packages/testing/agentskills-testing -v\n</code></pre>"},{"location":"DEVELOPMENT/#coverage","title":"Coverage","text":"<pre><code>poetry run python scripts/dev.py test:cov\n</code></pre> <p>This runs the suite with branch coverage, writes <code>coverage.xml</code> and <code>htmlcov/</code>, and fails if any package or the aggregate is below its floor. CI runs the same command and publishes the report to the job summary of every run.</p> <p>Coverage is measured by import name, not by path \u2014 <code>[tool.coverage.run] source_pkgs</code> in the root <code>pyproject.toml</code>. Measuring <code>packages/</code> instead would count the test files, and a test file is trivially covered by the act of running it.</p> <p>Floors are enforced at two levels:</p> Scope Where Why Aggregate <code>[tool.coverage.report] fail_under</code> Enforced by <code>coverage report</code> itself, so a bare run is gated too Per package <code>COVERAGE_FLOORS</code> in <code>scripts/dev.py</code> An aggregate floor alone lets one package rot behind the others <p>Both were set to the value measured when the gate was introduced, and are meant to ratchet upward. Raise a floor when its package moves up. If you have to lower one, say why in the PR \u2014 the point of the gate is that lowering it is a visible decision rather than a silent drift.</p>"},{"location":"DEVELOPMENT/#linting-formatting","title":"Linting &amp; Formatting","text":"<p>This project uses Ruff for both linting and formatting. Configuration lives in the root <code>pyproject.toml</code>.</p> <pre><code># Check for lint issues\npoetry run ruff check packages/ examples/\n\n# Auto-fix safe issues\npoetry run ruff check packages/ examples/ --fix\n\n# Format code\npoetry run ruff format packages/ examples/\n</code></pre>"},{"location":"DEVELOPMENT/#cleaning-caches","title":"Cleaning Caches","text":"<p>Remove all generated caches and build artifacts:</p> <pre><code># PowerShell\nGet-ChildItem -Recurse -Directory -Include __pycache__,.pytest_cache,.ruff_cache,*.egg-info | Remove-Item -Recurse -Force\n\n# Bash / macOS / Linux\nfind . -type d \\( -name __pycache__ -o -name .pytest_cache -o -name .ruff_cache -o -name '*.egg-info' \\) -exec rm -rf {} +\n</code></pre>"},{"location":"DEVELOPMENT/#dev-task-runner","title":"Dev Task Runner","text":"<p>A task runner script is available at <code>scripts/dev.py</code> for common development tasks:</p> <pre><code>python scripts/dev.py lint          # Check linting\npython scripts/dev.py lint:fix      # Auto-fix lint issues\npython scripts/dev.py format        # Auto-format code\npython scripts/dev.py format:check  # Check formatting without changes\npython scripts/dev.py typecheck     # Run mypy type checking\npython scripts/dev.py deps          # Check every package declares what it imports\npython scripts/dev.py check         # Lint + format check + type check + deps\npython scripts/dev.py test          # Run all tests\npython scripts/dev.py test:cov      # Run tests with coverage\npython scripts/dev.py clean         # Remove cache files\npython scripts/dev.py all           # Format + lint + test\n</code></pre>"},{"location":"DEVELOPMENT/#ci","title":"CI","text":"<p>GitHub Actions runs automatically on every push and pull request to <code>main</code>. The pipeline is defined in <code>.github/workflows/ci.yml</code> and includes these jobs:</p> <ul> <li>Lint: checks formatting (<code>ruff format --check</code>), linting (<code>ruff check</code>), and that every package declares the third-party modules it imports</li> <li>Test: runs <code>pytest</code> across Python 3.12, 3.13, and 3.14</li> <li>Coverage: runs the coverage gate and publishes the report to the job summary</li> <li>Test (latest permitted dependencies): resolves without <code>poetry.lock</code> to surface upstream breakage; advisory only</li> </ul> <p>All checks must pass before a PR can be merged. The CI status badge is shown on the root README.</p>"},{"location":"DEVELOPMENT/#security-ci","title":"Security CI","text":"<ul> <li>pip-audit: scans installed dependencies for known vulnerabilities (runs in the lint job)</li> <li>CodeQL: static application security testing for Python (<code>.github/workflows/codeql.yml</code>)</li> <li>Dependabot: automated dependency updates for <code>pip</code> and <code>github-actions</code> (<code>.github/dependabot.yml</code>)</li> </ul> <p>For the full security policy and threat model, see SECURITY.md.</p>"},{"location":"DEVELOPMENT/#type-checking","title":"Type Checking","text":"<p>All packages ship <code>py.typed</code> markers for PEP 561 compatibility. Run mypy via the dev task runner:</p> <pre><code>python scripts/dev.py typecheck\n</code></pre> <p>Type annotations are expected on all public functions and methods.</p>"},{"location":"DEVELOPMENT/#dependency-declarations","title":"Dependency Declarations","text":"<p>Every package here is installed into one shared virtualenv alongside its siblings, so a package can import a module it never declared and nothing will notice. The failure only appears for somebody who installed that one package from PyPI, and it appears as an <code>ImportError</code> on their first command. <code>agentskills-tools</code> shipped that way for a whole milestone, riding on the <code>pyyaml</code> that <code>agentskills-core</code> pulled in.</p> <pre><code>python scripts/check_declared_dependencies.py\n</code></pre> <p>It compares the module-level imports in each package's source against its declared dependencies. Imports nested in a function or guarded by <code>except ImportError</code> are ignored, because that is the pattern for an optional capability \u2014 <code>tiktoken</code> in the CLI, <code>agent_framework</code> in the MCP server \u2014 and declaring those would defeat the point of making them optional.</p>"},{"location":"DEVELOPMENT/#logging-conventions","title":"Logging Conventions","text":"<p>Every package logs into one <code>agentskills.*</code> namespace. Get a logger with the shared helper and pass <code>__name__</code> \u2014 the distribution prefix is rewritten, so <code>agentskills_http.static</code> logs as <code>agentskills.http.static</code>:</p> <pre><code>from agentskills_core import get_logger\n\n_logger = get_logger(__name__)\n</code></pre> <p>Never call <code>logging.getLogger(__name__)</code> directly. It produces <code>agentskills_http.static</code>, which does not descend from <code>agentskills</code>, so a host that raises the level on the namespace root would silently miss that module.</p> <p>A host application controls output with a single call:</p> <pre><code>logging.getLogger(\"agentskills\").setLevel(logging.DEBUG)\n</code></pre>"},{"location":"DEVELOPMENT/#levels","title":"Levels","text":"Level Use for Volume <code>DEBUG</code> Fetch, parse and cache events Per request <code>INFO</code> Registration outcomes Once per skill <code>WARNING</code> Degraded but recovered behaviour \u2014 a retried request, an unrecognised metadata key Rare <p>There is no <code>ERROR</code> level in the SDK. Anything that fails raises, and the caller decides whether it was an error. Logging and raising the same failure reports it twice and takes that decision away.</p>"},{"location":"DEVELOPMENT/#never-log-secrets","title":"Never log secrets","text":"<p>The library attaches only a <code>NullHandler</code>, so it has no idea where records end up \u2014 assume a shared log aggregator.</p> <ul> <li>Never log a raw URL. Pass it through <code>redact_url()</code> first. Credentials live in query   strings (SAS tokens, signed-URL signatures) and in userinfo. Providers with a configured base   URL should use <code>redact_url(url, relative_to=base_url)</code>, which drops the host as well.</li> <li>Never log request headers, redacted or otherwise. There is no legitimate operational   question that a header value answers, and a redactor you have to remember to call is a trap.</li> <li>Never pass <code>exc_info</code> for a transport exception. <code>httpx</code> exception reprs embed the full   request URL, query string included \u2014 the same leak fixed in the error paths.</li> </ul> <p>New log statements touching a URL or a request must be covered by a test asserting the secret does not appear in <code>caplog.text</code>. See <code>TestLogRecordsCarryNoSecrets</code> in the HTTP provider tests.</p>"},{"location":"DEVELOPMENT/#releasing","title":"Releasing","text":""},{"location":"DEVELOPMENT/#1-bump-version","title":"1. Bump version","text":"<p>All packages share the same version. Use the bump script to update all <code>pyproject.toml</code> files at once:</p> <pre><code># Patch: 0.2.0 -&gt; 0.2.1\n.\\scripts\\bump-version.ps1\n\n# Minor: 0.2.0 -&gt; 0.3.0\n.\\scripts\\bump-version.ps1 -Bump minor\n\n# Major: 0.2.0 -&gt; 1.0.0\n.\\scripts\\bump-version.ps1 -Bump major\n\n# Explicit version\n.\\scripts\\bump-version.ps1 -Version 1.0.0\n\n# Preview without changing files\n.\\scripts\\bump-version.ps1 -Bump minor -DryRun\n</code></pre> <p>The script also rewrites each dependent's <code>agentskills-core</code> constraint to <code>&gt;=&lt;new&gt;,&lt;1.0</code>. The packages ship in lockstep, so a dependent must require the core it was released with \u2014 otherwise pip can resolve an older core against a newer dependent and fail at import.</p>"},{"location":"DEVELOPMENT/#2-commit-and-merge","title":"2. Commit and merge","text":"<p>Create a branch, commit the version bump, open a PR, and merge to <code>main</code>.</p>"},{"location":"DEVELOPMENT/#3-tag","title":"3. Tag","text":"<p>Publishing is triggered by the tag and nothing else. Verify the bump landed, then tag <code>main</code>:</p> <pre><code>python scripts/check_release_version.py --tag v&lt;version&gt;\ngit tag v&lt;version&gt;\ngit push origin v&lt;version&gt;\n</code></pre> <p>The tag must be <code>v</code> followed by the exact version string \u2014 <code>0.3.0rc1</code> is tagged <code>v0.3.0rc1</code>. A tag that is neither <code>vX.Y.Z</code> nor <code>vX.Y.Z</code> plus <code>a1</code>, <code>b1</code> or <code>rc1</code> fails the run before anything is built, naming the shape it expected. Pre-release tags publish to PyPI like any other: <code>pip</code> will not install one unless <code>--pre</code> is passed, so they cannot reach anybody who did not ask for them.</p>"},{"location":"DEVELOPMENT/#4-approve","title":"4. Approve","text":"<p><code>.github/workflows/publish.yml</code> builds every distribution, then waits on the <code>pypi</code> environment for a reviewer. Approving releases all of them; there is one approval per release, not one per package.</p> <p>Publishing uses PyPI Trusted Publishing \u2014 the runner mints a short-lived OIDC token, and no PyPI API token exists anywhere in the repository, in Actions secrets, or on a workstation. Each distribution is uploaded with a PEP 740 provenance attestation.</p> <p>Packages go up in dependency order: core, then providers, then integrations. PyPI does not enforce this; it exists so that nobody installing mid-release resolves a package whose dependency is not there yet. If a run fails part-way, the failing step names the package and everything above it is already live \u2014 re-running the job is safe, because published versions are skipped rather than re-uploaded.</p> <p>The GitHub Release, with auto-generated notes, is created only after every package is live, so the notes never advertise a version that failed to publish. A pre-release tag produces a release marked as a pre-release.</p>"},{"location":"DEVELOPMENT/#dry-runs","title":"Dry runs","text":"<p>Run the Publish workflow manually (<code>workflow_dispatch</code>). It runs the version guard, builds every distribution and uploads the artifact, then stops \u2014 publishing takes a tag. What it does not exercise is the upload itself, and there is no longer a scratch index that would.</p>"},{"location":"DEVELOPMENT/#publishing-by-hand","title":"Publishing by hand","text":"<p><code>scripts/publish.ps1</code> still works and needs a PyPI token. It is for emergencies \u2014 recovering a release when GitHub Actions is down. Prefer the workflow: it is the only path that produces attestations and an audit trail.</p>"},{"location":"DEVELOPMENT/#one-time-setup","title":"One-time setup","text":"<p>Trusted Publishing is configured once per project, per index. Under Manage \u2192 Publishing, add a GitHub publisher:</p> Field Value Owner <code>pratikxpanda</code> Repository <code>agentskills-sdk</code> Workflow <code>publish.yml</code> Environment <code>pypi</code> <p>Then create the environment under Settings \u2192 Environments and add required reviewers to it. The trusted publisher is bound to the workflow filename; renaming <code>publish.yml</code> breaks publishing with an opaque OIDC error.</p>"},{"location":"DEVELOPMENT/#bootstrapping-a-project-that-does-not-exist-yet","title":"Bootstrapping a project that does not exist yet","text":"<p>A project with no releases has nothing to attach a publisher to, so you register a pending publisher instead, from the account-level publishing page rather than the project's settings.</p> <p>Pending publishers must be uniquely identifiable by their claims, and every package here shares the same owner, repository, workflow and environment. The index cannot tell which pending project an incoming token belongs to, so only one pending publisher can exist at a time:</p> <p>A pending trusted publisher matching this configuration has already been registered for a different project name.</p> <p>Bootstrapping N projects on a fresh index therefore takes N sequential rounds \u2014 register one, publish it, and the pending publisher converts to a project-scoped one, freeing the slot for the next. <code>skip-existing</code> makes each round cheap, since packages already published are skipped.</p> <p>This is worth knowing before adding a package, or publishing to any new index.</p>"},{"location":"DEVELOPMENT/#project-structure","title":"Project Structure","text":"Package Description <code>packages/core/agentskills-core</code> Storage-agnostic abstractions (<code>SkillProvider</code>, <code>Skill</code>, <code>SkillRegistry</code>, <code>validate_skill</code>) <code>packages/adapters/agentskills-adapters</code> Import AGENTS.md, Copilot instructions, Cursor rules, and Claude skills as native <code>Skill</code> objects <code>packages/providers/agentskills-fs</code> Load skills from the local filesystem <code>packages/providers/agentskills-http</code> Load skills from a static HTTP server <code>packages/integrations/agentskills-langchain</code> Integrate skills with LangChain agents <code>packages/integrations/agentskills-agentframework</code> Integrate skills with Microsoft Agent Framework agents <code>packages/integrations/agentskills-mcp-server</code> MCP server for exposing skills as MCP tools and resources (<code>agentskills-mcp-server</code> on PyPI) <code>packages/retrieval/agentskills-retrieval</code> Query-time skill selection: BM25 and embedding rankers (<code>agentskills-retrieval</code> on PyPI) <code>packages/tools/agentskills-tools</code> The <code>agentskills</code> command: <code>init</code>, <code>validate</code>, <code>lint</code>, <code>inspect</code>, <code>serve</code> <code>packages/testing/agentskills-testing</code> Provider conformance suite, <code>InMemorySkillProvider</code>, and pytest fixtures <p>Each package has its own <code>pyproject.toml</code> under <code>packages/</code> and can be published independently. Every package except <code>agentskills-core</code> depends on it. The root <code>pyproject.toml</code> uses Poetry to manage workspace-level dependencies and installs all packages in editable mode.</p>"},{"location":"ROADMAP/","title":"Agent Skills SDK \u2014 Roadmap","text":"<p>Public roadmap for the Agent Skills SDK. Themes and ordering, not dates.</p> <p>This document describes what we intend to build and why. It is intentionally coarse-grained: detailed scoping, discussion, and progress tracking live in GitHub Issues, grouped by milestone.</p> <p>Written-up specifications for the items below \u2014 problem, approach, open questions, acceptance criteria \u2014 live in docs/issues/, one file per milestone.</p>"},{"location":"ROADMAP/#product-principles","title":"Product Principles","text":"<p>These constrain every item below. If a proposal conflicts with one of these, it needs an explicit design doc arguing the trade-off.</p> <ol> <li>Spec-first. The SDK implements the Agent Skills open format.    We do not invent proprietary extensions to the skill format; where we need more (e.g. <code>version</code>),    we use optional, backward-compatible frontmatter fields and push for upstream adoption.</li> <li>Progressive disclosure is the contract. Every abstraction must let an agent pay only for    the tokens it actually needs. Anything that forces eager loading of full skill bodies is a bug.</li> <li>Skills are untrusted code. Skill content lands verbatim in an agent's context. Trust,    provenance, and integrity are first-class product features, not documentation footnotes.</li> <li>Small, composable packages. <code>agentskills-core</code> stays dependency-light. Providers and    framework integrations are optional installs and never leak into core.</li> <li>Framework-agnostic core, thin adapters. Framework-specific behaviour belongs in the    integration package. If two integrations need the same logic, it moves to core.</li> </ol>"},{"location":"ROADMAP/#themes","title":"Themes","text":"Theme Why it matters Correctness &amp; spec coverage Close the gaps between the SDK and the full skill format so real-world skills work unmodified. Performance &amp; resilience Skills are fetched on the hot path of an agent turn. Redundant I/O is latency and cost. Agent effectiveness Retrieval is table stakes. The product question is whether an agent holding a skill actually performs better \u2014 and whether anyone can prove it. Interoperability Teams already have instructions written in other formats. Meeting them where they are beats asking them to start over. Trust &amp; supply chain The differentiator for enterprise adoption. Skills are code; treat them like it. Operability Platform teams cannot run what they cannot see. Logs, traces, metrics, usage signals. Developer experience Adoption is gated on how fast someone can author, validate, and ship a skill. Ecosystem breadth More providers and framework integrations widen the addressable surface. Project health An open-source project is a product; release engineering and governance are features."},{"location":"ROADMAP/#shipped-v03-foundations","title":"Shipped \u2014 v0.3 \"Foundations\"","text":"<p>Released 2026-07-31. Closed the highest-severity correctness and performance gaps before the API surface widened. Specifications and implementation notes: docs/issues/v0.3.md.</p> <p>Two changes are breaking for existing users, which a minor version is entitled to pre-1.0 but which the release notes should state plainly: <code>SkillProvider</code> gained resource discovery, and a skill carrying an invalid <code>version</code> in its frontmatter now fails registration instead of being registered with a warning.</p> Item Theme Package(s) Notes Provider content caching Performance <code>agentskills-fs</code>, <code>agentskills-http</code> A single skill's <code>SKILL.md</code> was fetched up to 5x per session \u2014 twice during registration (<code>validate_skill()</code> calls <code>get_body()</code> and <code>get_metadata()</code> independently), once per catalog build, and again on each tool call. Now cached per provider instance with an explicit <code>invalidate()</code>. HTTP revalidation via <code>ETag</code> / <code>Last-Modified</code> is opt-in (<code>revalidate=True</code>) rather than default, since a conditional request per access defeats the point for the common static-host case. Concurrent catalog build Performance <code>agentskills-core</code> <code>get_skills_catalog()</code> fetched metadata serially. Now fans out with <code>asyncio.gather</code> under a bounded semaphore (<code>SkillRegistry(catalog_concurrency=8)</code>); output ordering is unchanged. Non-blocking filesystem I/O Performance <code>agentskills-fs</code> The provider was <code>async</code> but read synchronously, blocking the event loop. Path resolution, stat and read now run in a worker thread via <code>asyncio.to_thread</code>. Resource discovery API Correctness <code>agentskills-core</code> + providers Agents had no way to learn which references/scripts/assets exist; discovery depended entirely on SKILL.md prose. <code>list_resources(skill_id)</code> is now an optional provider capability paired with a <code>supports_resource_listing</code> flag (ADR 0002). The default implementation raises <code>ResourceListingNotSupportedError</code> rather than returning <code>{}</code>, so \"cannot enumerate\" is never mistaken for \"has no resources\". Filesystem enumerates directly; HTTP requires an opt-in per-skill <code>index.json</code>, which also gives the supply-chain work a natural home for integrity hashes. Binary-safe resources Correctness <code>agentskills-core</code> + all integrations All three integrations decoded provider bytes with <code>.decode(\"utf-8\", errors=\"replace\")</code>, silently destroying images, PDFs, and archives. A shared <code>encode_resource_content()</code> now returns valid UTF-8 verbatim and wraps everything else in a base64 JSON envelope. Using MCP's native binary content blocks instead of a JSON string remains a follow-up. Optional <code>version</code> frontmatter Correctness <code>agentskills-core</code> Skills had no version, so nothing could be pinned, compared, or checked for drift. <code>version</code> is now optional, validated as semver when present, returned by <code>get_metadata()</code>, and rendered in both catalog formats only when set. Two things surfaced during the work. First, the acceptance criteria were self-contradictory: \"fully backward-compatible\" and \"invalid semver fails registration\" cannot both hold, so this is recorded as a deliberate breaking change for skills carrying a non-semver <code>version</code> today. Second, YAML types the field before validation ever sees it \u2014 <code>1.0</code> is a float, <code>2024-01-15</code> a date \u2014 so non-string values get an error naming the coercion instead of a bare type mismatch. No new dependency: the semver.org regex is inlined rather than pulling a package in for one match. HTTP error classification Resilience <code>agentskills-http</code> Every non-2xx mapped to <code>SkillNotFoundError</code>, so a <code>503</code> and a genuine <code>404</code> were indistinguishable. Now <code>404</code>/<code>410</code> \u2192 <code>SkillNotFoundError</code>, and <code>5xx</code>/<code>408</code>/<code>425</code>/<code>429</code>/timeouts/connection errors \u2192 a new <code>SkillUnavailableError</code> carrying <code>retry_after</code>, with bounded jittered-backoff retry that honours <code>Retry-After</code>. Also fixed a credential leak found during the work: chaining <code>httpx.HTTPStatusError</code> put the full request URL \u2014 query string included \u2014 into every traceback, exposing SAS tokens and signed-URL signatures. Structured logging Operability all Outside one warning the SDK was silent, so retries, cache hits and registration outcomes were invisible in production. Everything now logs under one <code>agentskills.*</code> namespace via <code>get_logger(__name__)</code>, which rewrites the distribution prefix so <code>agentskills_http.static</code> becomes <code>agentskills.http.static</code> \u2014 plain <code>logging.getLogger(__name__)</code> produced names that did not descend from a common root, so a host could not raise the level on the library with one call. Only a <code>NullHandler</code> is attached. There is deliberately no <code>ERROR</code> level: failures raise, and logging them too would report the same event twice. <code>redact_url()</code> is the shared sanitiser for anything URL-shaped, lifted out of the HTTP provider's private <code>_describe()</code> so errors and logs cannot drift apart; headers are never logged at all, on the grounds that a redactor you must remember to call is a trap. Coverage gate in CI Project health repo <code>pytest-cov</code> was not even a dev dependency. Coverage is now measured by import name rather than by path \u2014 measuring <code>packages/</code> counted the test files, which are trivially covered by being run, and inflated the figure from a real 96% to a meaningless 99%. Floors are enforced twice: an aggregate <code>fail_under</code> that <code>coverage report</code> applies on its own, and a per-package floor in <code>scripts/dev.py</code>, because an aggregate alone lets one package rot behind the others. Demonstrated: five uncovered statements in core drop that package from 99% to 97% while the aggregate never moves. Both sets start at the measured value and ratchet. The badge states the enforced floor (<code>coverage \u226596%</code>) rather than a per-commit number, since a live figure needs a third-party account the project does not have. Automated PyPI publish Project health repo Releasing was six <code>poetry publish</code> runs from a workstation holding a long-lived PyPI token. Now tag-triggered, with Trusted Publishing (OIDC) so no token exists in the repo, in Actions secrets, or on a laptop, and PEP 740 provenance attestations on every artifact. Only a bare <code>vX.Y.Z</code> tag reaches PyPI; every other tag shape falls through to TestPyPI, so a malformed tag cannot burn a real version. A guard refuses to build unless all six packages agree on the version and match the tag. One environment approval gates the whole release rather than one per package, and re-running a half-finished release is safe. The GitHub Release moved to the end of the same workflow, because it was previously created on tag push regardless of whether publishing succeeded. Agent Framework 1.x API rename Correctness <code>agentskills-agentframework</code>, <code>agentskills-mcp-server</code> <code>agent-framework-core</code> 1.12.1 renamed <code>BaseContextProvider</code> to <code>ContextProvider</code>; our constraint <code>&gt;=1.0.0rc3,&lt;2.0</code> admitted it, so a fresh install raised <code>ImportError</code> on import. Masked locally because <code>poetry.lock</code> pinned 1.0.0rc3. Floor raised to <code>&gt;=1.0</code>. Python 3.14 support Project health all Every package capped <code>python</code> at <code>&lt;3.14</code>, so installs failed on current stable Python. Ceiling raised to <code>&lt;4.0</code>; no dependency justified the old cap."},{"location":"ROADMAP/#shipped-v04-developer-experience","title":"Shipped \u2014 v0.4 \"Developer Experience\"","text":"<p>Released 2026-08-17. Made authoring and validating skills a first-class workflow, and third-party providers provably correct. Specifications and implementation notes: docs/issues/v0.4.md.</p> <p>Three new distributions ship with this milestone \u2014 <code>agentskills-tools</code>, <code>agentskills-testing</code> and <code>agentskills-adapters</code> \u2014 bringing the lockstep-versioned set to nine. Nothing is breaking for existing users.</p> Item Theme Package(s) Notes <code>agentskills</code> CLI DX new <code>agentskills-tools</code> <code>init</code> (scaffold a skill), <code>validate &lt;path&gt;</code> (spec check, exit non-zero on failure), <code>lint</code> (style/token-budget warnings), <code>inspect</code> (render catalog/metadata), <code>serve</code> (run the MCP server without writing config by hand). Separate package and stdlib <code>argparse</code> so the validation Action inherits no dependencies; <code>serve</code> is an optional extra. Skill validation GitHub Action DX repo Composite action at <code>actions/validate</code> wrapping <code>agentskills validate</code> and <code>lint</code>. Findings land on the pull request diff via the CLI's own <code>--format github</code>, so the annotation logic is unit-tested in the package rather than in a script beside the workflow. Highest-leverage adoption lever \u2014 it puts the SDK in other people's CI. Provider conformance test kit Correctness new <code>agentskills-testing</code> <code>ProviderConformanceSuite</code> \u2014 subclass it, supply a <code>provider</code> fixture, and pytest runs the whole contract against your implementation: error types, traversal rejection, <code>bytes</code> not <code>str</code>, metadata that is neither shared nor carrying the body, and a <code>list_resources()</code> that agrees with the flag callers branch on. Size limits sit in an opt-in <code>ContentLimitConformanceSuite</code>, since an in-memory provider has no external source to bound. Its first run found that <code>agentskills-http</code> raised a plain <code>ValueError</code> for traversal where <code>agentskills-fs</code> raised <code>SkillNotFoundError</code> \u2014 an ABC cannot catch that, which is the argument for the kit. Test doubles DX <code>agentskills-testing</code> <code>InMemorySkillProvider</code> \u2014 a real provider backed by a dict that passes the suite above \u2014 plus <code>build_skill()</code> and pytest fixtures registered via an entry point. Replaced the four duplicated <code>_mock_provider</code> helpers in this repo, which had been raising <code>KeyError</code> where a real provider raises <code>ResourceNotFoundError</code>. Registry-level discovery DX <code>agentskills-core</code> + providers Optional <code>discover()</code> on providers, following ADR 0002, plus <code>registry.register_all(provider)</code> \u2014 atomic, and reporting every validation failure at once rather than the first. The filesystem provider enumerates subdirectories holding a <code>SKILL.md</code>; the HTTP provider reads a root <code>index.json</code>, the same filename and shape as the per-skill resource manifest one level up. Registering N skills no longer requires knowing all N IDs up front. Catalog filtering &amp; budget DX / Perf <code>agentskills-core</code> <code>get_skills_catalog(tags=\u2026, include=\u2026, exclude=\u2026, max_chars=\u2026)</code>. Tags come from the spec's free-form <code>metadata</code> mapping rather than a new top-level field, so nothing has to be defended upstream. The ID filters run before any metadata is fetched, so narrowing a large registry costs proportionally fewer provider round-trips. <code>max_chars</code> drops whole entries from the end and says so \u2014 <code>truncated</code>/<code>shown</code>/<code>total</code> on the XML root, a closing note in Markdown \u2014 because a catalog that shrinks silently makes agent behaviour non-reproducible. Skill evaluation harness Agent effectiveness <code>agentskills-tools</code> <code>agentskills eval</code> runs each case in a skill's <code>evals/</code> folder twice \u2014 once with the skill's body in the system prompt, once without \u2014 and reports the delta, because absolute pass rates mostly measure the model while the difference isolates the skill. Assertions are deterministic (<code>contains</code>, <code>not_contains</code>, <code>regex</code>) or judged by a declared model; <code>repeat</code>/<code>threshold</code> make sampling noise visible instead of letting one lucky sample pass as a measurement. Model access is a one-method protocol resolved from a dotted path, so no provider SDK is a dependency anywhere. Completions cache by model, prompts, and repeat index, so tightening an expectation re-grades answers already bought. Landed in the CLI rather than <code>agentskills-testing</code>: the requirement is that it never runs in the default <code>pytest</code> run, which makes it a command, not a test kit. Foreign format adapters Interoperability new <code>agentskills-adapters</code> <code>agentskills-adapters</code> imports <code>AGENTS.md</code>, <code>.github/copilot-instructions.md</code>, Cursor <code>.mdc</code> rules, and Claude skill folders as ordinary <code>Skill</code> objects. Missing descriptions become explicit synthesized catalog text, Cursor <code>globs</code> stay in native metadata, and <code>agentskills init --from</code> writes a validated editable <code>SKILL.md</code>. It is an import layer, not a spec fork: a migration path for existing instructions rather than a second runtime model. Token cost reporting DX <code>agentskills-tools</code> <code>agentskills inspect --cost</code> splits a skill into what is charged on every turn (the catalog entry), what is charged per load (the body, broken down by section), and what is charged only if the agent reads it (each resource). Authors reliably budget the body while ignoring a description that costs a hundred tokens a turn forever, so <code>--budget</code> and <code>--turn-budget</code> gate the two halves separately \u2014 one threshold would be dominated by the body and hide exactly the number the feature exists to surface. Sections do not nest, so the parts sum to the whole, which is the only property that makes a breakdown checkable. <code>tiktoken</code> is used when importable and a character heuristic otherwise, but the counter is named in every report and <code>--tokenizer tiktoken</code> refuses to fall back: a budget gate whose arithmetic depends on what happens to be installed is worse than no gate. It stays out of the dependency list \u2014 a compiled wheel that downloads its vocabulary on first use is a poor trade for a tool whose main job is reading YAML in CI. Documentation site Project health repo Added a MkDocs Material site with a docs-first navigation (getting started, concepts, one page per package, roadmap and ADRs) and API pages generated via <code>mkdocstrings</code> from existing docstrings. New docs workflow builds the site in CI (<code>mkdocs build --strict</code>) and deploys versioned docs on published releases via <code>mike</code>, with <code>latest</code> as the default alias. Root README was reduced to overview + quick start + links. Architecture decision records Project health <code>docs/adr/</code> Added an ADR index and template plus backfilled records for six cross-package decisions: fully async provider interface, multi-package lockstep versioning, provider caching/invalidation, exception taxonomy (<code>not found</code> vs <code>unavailable</code>), binary resource envelope, and logging namespace/severity conventions. Each ADR links back to the issue section where the decision was made so future reversals are explicit rather than accidental. Simplify the publish workflow Project health repo Dropped the TestPyPI dry-run path. Pending trusted publishers must be uniquely identifiable by their claims, and all seven distributions share owner, repo, workflow and environment \u2014 so only one pending publisher can exist at a time, and bootstrapping them on a fresh index takes one sequential publish round each. The result was a dry run configured for one package out of seven, which failed in a way that looked like a real problem. An unrecognised tag now fails the run instead of falling through to another index, <code>workflow_dispatch</code> builds without uploading, and pre-release tags go to PyPI, which handles them natively."},{"location":"ROADMAP/#shipped-v05-agent-effectiveness","title":"Shipped \u2014 v0.5 \"Agent Effectiveness\"","text":"<p>Released 2026-08-19. Everything before this makes skills safe and cheap to ship. This milestone is about making them worth shipping \u2014 the point where the SDK stops being a loader and starts changing how well the agent performs. Specifications and implementation notes: docs/issues/v0.5.md.</p> <p>One new distribution ships with this milestone \u2014 <code>agentskills-retrieval</code> \u2014 bringing the lockstep-versioned set to ten. Nothing is breaking for existing users.</p> <p>The first two items changed contracts the rest build on \u2014 the frontmatter schema and the shape of a body fetch \u2014 so the table is ordered by dependency rather than by value.</p> Item Theme Package(s) Notes Selection metadata Correctness <code>agentskills-core</code> Optional <code>when_to_use</code> / <code>when_not_to_use</code> frontmatter, each a list of at most five 200-character entries. False activation is as damaging as non-activation, and a description alone carries no negative signal. Rendered in both catalog formats and omitted when absent; <code>get_skills_catalog(selection_hints=False)</code> trades the accuracy back for tokens. Optional and backward-compatible per principle 1; still to be pushed upstream. Section-level disclosure Agent effectiveness <code>agentskills-core</code> + integrations <code>get_skill_body()</code> is all-or-nothing, so a thorough 4k-token skill is charged in full to use one section. Split the body by heading, return an outline plus <code>get_skill_section(skill_id, heading)</code>. Extends progressive disclosure one level inward rather than adding a new idea, and stops penalising well-written skills. Semantic skill selection Agent effectiveness new <code>agentskills-retrieval</code> The catalog is injected on every turn, so prompt cost is linear in registered skills. Embed descriptions once, select top-k against the current turn, inject a handful. Descriptions are already written to be discriminative, so the corpus exists for free. Ships with a zero-dependency lexical default; embeddings are pluggable. Opt-in, and it must log its selection \u2014 this trades a deterministic prompt for a better one. Stateful / session-aware disclosure Agent effectiveness <code>agentskills-agentframework</code> The provider re-injected the whole catalog on every turn regardless of what the agent had already read. <code>after_run</code> now records full-body loads in session <code>state</code>, and later turns prune those entries down to a one-line reminder \u2014 caching saves provider I/O, but pruning is what makes turn N+1 cheaper. Declines to prune when the reminder would cost more than the entries, and never prunes to an empty catalog. Topic-based pruning was left out: that is semantic selection again, and it belongs in <code>agentskills-retrieval</code>. Single-skill fast path Performance <code>agentskills-core</code> + integrations A catalog exists to let a model choose; with one skill there is nothing to choose, so <code>resolve_fast_path()</code> inlines the body and drops the catalog, the usage instructions and the four body-access tools, removing a whole model round trip. Triggers on the effective set, so a registry narrowed to one by retrieval qualifies too. The token ceiling defaults to the size below which the fast path wins at any conversation length rather than to a guess; measured saving is 38-49% for a small skill. Resource tools stay \u2014 a skill carrying a 2 MB dataset must not have it inlined because the skill count happened to be one. Vision-native assets Agent effectiveness <code>agentskills-core</code> + integrations A base64 envelope is the right answer for an opaque binary and the wrong one for a diagram \u2014 the model got a wall of characters where a picture was. One classifier in core decides which is which; the three integrations differ only in how they wrap the result. Detection reads magic bytes before the filename, because a name is a claim and bytes are evidence. Native delivery is opt-in via <code>vision=True</code>, since handing an image to a text-only model is an API error rather than a worse answer, and no integration can ask a model whether it can see. Images get their own 5 MiB ceiling: the 64 KiB cap tracks tokens, and a native image is billed by tile count instead. PDF and SVG are excluded, and everything non-renderable keeps the v0.3 envelope unchanged."},{"location":"ROADMAP/#now-v06-trust-operability","title":"Now \u2014 v0.6 \"Trust &amp; Operability\"","text":"<p>The enterprise story. This is the work that makes the SDK viable as the substrate for Agent Skills Hub.</p> Item Theme Package(s) Notes Skill integrity &amp; provenance Trust <code>agentskills-core</code> Optional manifest with per-file SHA-256, verified on load. Then detached signature verification (Sigstore) and an \"unverified skill\" policy switch. Skills are code \u2014 this is the missing supply-chain control. Content policy pipeline Trust <code>agentskills-core</code> Pluggable <code>SkillPolicy</code> hooks that run before content enters agent context: reject, redact, or annotate. Ships with a heuristic prompt-injection scanner and a max-token guard; enterprises plug in their own. <code>allowed-tools</code> enforcement Trust integrations The field is validated but never enforced. Integrations should be able to constrain the agent's tool surface while a skill is active. SSRF hardening Trust <code>agentskills-http</code> Host allow/deny lists, private/link-local IP range blocking by default, DNS-rebinding-aware connection checks. <code>require_tls</code> alone is not an SSRF control. Secret redaction Trust <code>agentskills-core</code>, <code>agentskills-http</code> Central redaction helper applied to exception messages and log records so <code>Authorization</code> headers and SAS tokens never surface in tracebacks. OpenTelemetry instrumentation Operability <code>agentskills-core</code> + providers Spans for fetch/parse/validate, metrics for fetch latency, cache hit ratio, and payload size. Optional dependency, no-op when OTel is absent. Skill usage telemetry hooks Operability <code>agentskills-core</code> Callback protocol emitting \"skill X disclosed at level Y\". Answers the question every platform team asks: which skills are actually being used? Feeds Hub analytics directly. Startup health check Resilience <code>agentskills-core</code> <code>registry.health()</code> verifying every provider is reachable and every skill parses, at boot. A misconfigured provider should fail deployment, not fail silently mid-conversation. Serve-stale on provider failure Resilience providers If a refresh fails but cached content exists, serve the stale copy and flag it rather than failing the agent turn. A registry outage should degrade the agent, not break it. Builds on v0.3 caching and error classification."},{"location":"ROADMAP/#next-v07-ecosystem","title":"Next \u2014 v0.7 \"Ecosystem\"","text":"<p>Breadth, once the core contracts are stable enough that each new package is cheap to add.</p> Item Theme Notes Skills as MCP prompts Interoperability The MCP server exposes skills as tools only. Many clients surface prompts as slash commands, so the same registry becomes user-invocable in Claude Desktop, VS Code, and others for very little work. Git provider Ecosystem Most skills live in Git repos. Clone/fetch with ref or commit pinning, sparse checkout, local cache. Likely the single most requested provider. Object storage provider Ecosystem S3 / Azure Blob / GCS via a common abstraction, with native credential chains instead of hand-rolled headers. OCI artifact provider Ecosystem Skills as OCI artifacts in any container registry \u2014 inherits existing signing, replication, and RBAC infrastructure. Pairs naturally with the integrity work in v0.6. Database provider Ecosystem Reference implementation over SQL for teams storing skills in an existing system of record. OpenAI Agents SDK integration Ecosystem Notable gap in the current integration matrix. Additional framework adapters Ecosystem Pydantic AI, Semantic Kernel, LlamaIndex, CrewAI. Prioritize by inbound demand rather than building all of them speculatively. Node/TypeScript port Ecosystem Large commitment. Only if there is clear pull \u2014 the MCP server already serves TS agents today, which may be sufficient."},{"location":"ROADMAP/#v10-stability","title":"v1.0 \u2014 Stability","text":"Item Notes API freeze Public surface documented and frozen; anything not documented is explicitly private. Compatibility policy SemVer commitments, a written deprecation policy with a minimum support window, and coordinated cross-package version guarantees. Release automation end-to-end Changelog generation, signed artifacts with build provenance/attestations, automated publish on tag. Hub interoperability The SDK contracts the Hub depends on (versioning, integrity, telemetry, MCP gateway composition) are stable and documented."},{"location":"ROADMAP/#explicit-non-goals","title":"Explicit Non-Goals","text":"<p>Stating these prevents recurring proposals and scope creep.</p> <ul> <li>Executing skill scripts. The SDK retrieves scripts; it does not run them. Sandboxed   execution is the host application's responsibility. We will document the hazard, not own it.</li> <li>Authoring or hosting UI. That is Agent Skills Hub's job.</li> <li>Authentication and authorization. Providers accept caller-supplied credentials. The SDK   is not an identity or policy system.</li> <li>Being an agent framework. We integrate with frameworks; we do not compete with them.</li> <li>Forking the skill format. Divergence from the open spec is a last resort.</li> </ul>"},{"location":"ROADMAP/#how-we-plan-work","title":"How We Plan Work","text":"Artifact Purpose This roadmap Direction and sequencing. Reviewed at the start of each minor version. No dates. GitHub Milestones One per minor version (<code>v0.3</code>, <code>v0.4</code>, \u2026). An item is committed when it has an issue in a milestone. GitHub Issues The single unit of work. Status, assignment, discussion, and linked PRs. Labelled by <code>theme:*</code>, <code>package:*</code>, <code>type:*</code>, and <code>good-first-issue</code>. docs/issues/ The durable specification behind each roadmap item, one file per milestone. Filed issues link back here instead of duplicating the text; shipped items stay for the record. GitHub Project board Now / Next / Later / Done view across issues, linked from the README. docs/adr/ Short, immutable records of decisions already made and their trade-offs. Written when a decision is hard to reverse or likely to be questioned later. <p>Contributing to the roadmap: open a GitHub Discussion for an idea, or an issue for something concrete. Changes to a public contract should be agreed on the issue before a PR is opened. Items marked <code>good-first-issue</code> are the recommended entry point.</p>"},{"location":"SKILLS-DIRECTORY/","title":"Agent Skills Directory","text":"<p>A curated directory of Agent Skills repositories, awesome lists, and community resources. Use these as inspiration, reference implementations, or load them into your own agents using the HTTP provider.</p>"},{"location":"SKILLS-DIRECTORY/#official-skills-repositories","title":"Official Skills Repositories","text":"Repository Description anthropics/skills Anthropic's official skill examples: documents, creative, development tools, and more microsoft/skills Skills, MCP servers, and custom agents for Azure SDK and Microsoft AI Foundry development vercel-labs/agent-skills Vercel's official collection of agent skills for React, web design, and deployment trailofbits/skills Trail of Bits skills for security research, vulnerability detection, and audit workflows agentskills/agentskills Agent Skills specification and documentation"},{"location":"SKILLS-DIRECTORY/#community-skills","title":"Community Skills","text":"<p>Top community repositories sorted by GitHub stars.</p> Repository Description ComposioHQ/awesome-claude-skills 874+ SaaS app automation skills via Composio toolkits, plus curated community skills across document processing, development, data analysis, business, creative, and productivity categories hesreallyhim/awesome-claude-code Skills, hooks, slash-commands, agent orchestrators, and plugins for Claude Code sickn33/antigravity-awesome-skills 800+ agentic skills for Claude Code, Antigravity, and Cursor VoltAgent/awesome-agent-skills 300+ agent skills from official dev teams and the community, compatible with Codex, Antigravity, Gemini CLI, Cursor, and more heilcheng/awesome-agent-skills Skills, tools, tutorials, and capabilities for AI coding agents (Claude, Codex, Copilot, VS Code) libukai/awesome-agent-skills Agent Skills guide with quick start, recommended skills, latest news, and practical examples Prat011/awesome-llm-skills LLM and AI Agent Skills, resources, and tools for Claude Code, Codex, Gemini CLI, and more SamurAIGPT/awesome-openclaw Resources, tools, skills, tutorials, and articles for the OpenClaw AI agent rohitg00/awesome-claude-code-toolkit Agents, curated skills, commands, plugins, and hooks for Claude Code Code-and-Sorts/awesome-copilot-agents Curated list of instructions, prompts, skills, and agent markdown files for GitHub Copilot"},{"location":"SKILLS-DIRECTORY/#using-skills-with-this-sdk","title":"Using Skills with This SDK","text":"<p>Any skill hosted on a web server can be loaded using the HTTP provider:</p> <pre><code>from agentskills_core import SkillRegistry\nfrom agentskills_http import HTTPStaticFileSkillProvider\n\nprovider = HTTPStaticFileSkillProvider(\"https://example.com/skills\")\nregistry = SkillRegistry()\nawait registry.register(\"my-skill\", provider)\n</code></pre> <p>For skills stored locally, use the filesystem provider:</p> <pre><code>from pathlib import Path\nfrom agentskills_fs import LocalFileSystemSkillProvider\n\nprovider = LocalFileSystemSkillProvider(Path(\"path/to/skills\"))\n</code></pre>"},{"location":"SKILLS-DIRECTORY/#contributing","title":"Contributing","text":"<p>Know of a skill repository or resource that should be listed here? Open an issue or submit a PR to add it.</p>"},{"location":"concepts/","title":"Concepts","text":""},{"location":"concepts/#progressive-disclosure","title":"Progressive disclosure","text":"<p>Agent Skills are delivered in layers:</p> <ol> <li>Catalog entry (name/description) in the system prompt</li> <li>Body outline \u2014 section keys and their token cost \u2014 when a skill is large</li> <li>One section, or the full skill body, only when selected</li> <li>Individual references, scripts, and assets only when needed</li> </ol> <p>This keeps the always-on prompt surface small while preserving depth.</p>"},{"location":"concepts/#section-level-disclosure","title":"Section-level disclosure","text":"<p>Layer 2 exists because a long skill body is all-or-nothing: an agent that needs the rollback procedure pays for the onboarding notes too. <code>get_skill_outline(skill_id)</code> returns the body's headings as addressable keys with a token estimate for each, and <code>get_skill_section(skill_id, key)</code> fetches one.</p> <pre><code>'incident-response': ~439 tokens in 8 sections.\n\n- incident-response (~14) \u2014 Incident Response\n  - when-to-declare-an-incident (~50) \u2014 When to Declare an Incident\n  - roles (~70) \u2014 Roles\n  ...\n</code></pre> <p>Three properties of that design are load-bearing:</p> <ul> <li>Keys are flat slugs, with an ordinal on collision. Two <code>## Setup</code> headings become   <code>setup</code> and <code>setup-2</code>. A hierarchical path would imply a tree the splitter does not build.</li> <li>Sections do not nest. A section covers its own text up to the next heading of any   level, so fetching a parent does not include what is indented under it. The indentation in   the outline shows depth; it does not show containment. This is what makes the parts sum to   the whole, which <code>agentskills inspect --cost</code> depends on.</li> <li>The outline says when not to use it. A section fetch costs a tool call, a model turn,   and the outline that preceded it. Below <code>WHOLE_BODY_CHEAPER_TOKENS</code> (1000) the rendered   outline tells the agent to call <code>get_skill_body</code> instead, and past about three sections it   says the same. Shipping the split without that guidance would make the common case worse in   order to improve the rare one.</li> </ul> <p>The rendering lives in <code>SkillOutline.render()</code> in core rather than in each integration, so the three integrations cannot drift into quoting different costs for the same skill.</p>"},{"location":"concepts/#selection-metadata","title":"Selection metadata","text":"<p><code>description</code> carries positive evidence only, so a skill has no way to say where it stops applying. Two optional frontmatter fields close that gap:</p> <pre><code>---\nname: incident-response\ndescription: Triage and mitigate production incidents.\nwhen_to_use:\n  - A production service is degraded or down\nwhen_not_to_use:\n  - Debugging a failing test locally\n---\n</code></pre> <p>False activation is not a lesser failure than non-activation. It is worse: it costs a full body load and puts instructions written for a different situation in front of the model.</p> <p>Both fields are optional lists of at most five non-empty strings of at most 200 characters each. The bounds are not arbitrary \u2014 these fields ride in the catalog, so they are charged on every turn for every registered skill, and a skill needing a sixth condition is usually two skills. They render next to the description in both catalog formats and are omitted entirely when absent, so a skill written before they existed renders byte-for-byte as it did.</p> <p>Callers who want the cheaper catalog back can pass <code>get_skills_catalog(selection_hints=False)</code>.</p>"},{"location":"concepts/#skill-selection","title":"Skill selection","text":"<p>Progressive disclosure makes each skill cheap. It does nothing about the number of skills. The catalog is injected on every turn, so its cost is linear in how many are registered, and two things get worse as a registry grows, not one: the token bill, and the model's ability to pick correctly from a long list.</p> <p><code>include</code>, <code>exclude</code>, <code>tags</code> and <code>max_chars</code> all narrow the catalog already, but each requires the caller to know the answer in advance \u2014 and <code>max_chars</code> drops entries from the end, which is arbitrary with respect to relevance. <code>agentskills-retrieval</code> narrows it by what was asked instead:</p> <pre><code>from agentskills_retrieval import LexicalSelector, build_selected_catalog\n\ncatalog = await build_selected_catalog(\n    registry, LexicalSelector(registry), \"the checkout API is returning 503s\"\n)\n</code></pre> <p>The integration point is the filter that already exists: a selector returns skill IDs, and those IDs feed <code>include=</code>, which is applied before any metadata is fetched. Narrowing fifty skills to five therefore also avoids forty-five provider round trips. Core stays ignorant of ranking; it accepts one optional <code>total=</code> so the rendered catalog can report the narrowing honestly rather than claiming to be complete.</p> <p>Selection is opt-in and always visible. From inside an agent, a skill that was ranked out is indistinguishable from one that was never registered \u2014 so every selection is logged with its scores, the near-misses are kept on <code>Selection.rejected</code>, and a selection that matches nothing falls back to the full catalog rather than leaving the agent with no skills at all.</p>"},{"location":"concepts/#providers","title":"Providers","text":"<p>A provider answers five content calls (<code>get_metadata</code>, <code>get_body</code>, <code>get_reference</code>, <code>get_script</code>, <code>get_asset</code>) and can optionally support resource listing and skill discovery.</p>"},{"location":"concepts/#registry","title":"Registry","text":"<p><code>SkillRegistry</code> coordinates providers and exposes the catalog. <code>register_all</code> lets a provider enumerate and register its own skills atomically.</p>"},{"location":"concepts/#error-model","title":"Error model","text":"<p>Errors are typed so callers can branch correctly:</p> <ul> <li><code>SkillNotFoundError</code> for stable absence</li> <li><code>SkillUnavailableError</code> for transient/backend failures</li> <li>Optional-capability errors for unsupported listing/discovery</li> </ul>"},{"location":"concepts/#cost-model","title":"Cost model","text":"<p>The catalog is charged every turn, while skill bodies/resources are charged only when loaded. The CLI <code>inspect --cost</code> reports this split explicitly.</p>"},{"location":"getting-started/","title":"Getting Started","text":""},{"location":"getting-started/#install-what-you-need","title":"Install what you need","text":"<p>The SDK is split into focused packages.</p> <ul> <li><code>agentskills-core</code> for registry and validation</li> <li>One provider (<code>agentskills-fs</code> or <code>agentskills-http</code>)</li> <li>One integration (<code>agentskills-langchain</code>, <code>agentskills-agentframework</code>, or <code>agentskills-mcp-server</code>)</li> </ul> <p>Example:</p> <pre><code>pip install agentskills-fs agentskills-langchain\n</code></pre>"},{"location":"getting-started/#register-skills","title":"Register skills","text":"<pre><code>from pathlib import Path\n\nfrom agentskills_core import SkillRegistry\nfrom agentskills_fs import LocalFileSystemSkillProvider\n\nregistry = SkillRegistry()\nawait registry.register_all(LocalFileSystemSkillProvider(Path(\"./skills\")))\n</code></pre>"},{"location":"getting-started/#inject-the-catalog","title":"Inject the catalog","text":"<pre><code>catalog = await registry.get_skills_catalog(format=\"xml\")\n</code></pre> <p>The catalog is what the agent sees on every turn. Full skill bodies and resources are fetched only on demand through tools.</p>"},{"location":"getting-started/#author-and-validate-skills","title":"Author and validate skills","text":"<pre><code>pip install agentskills-tools\nagentskills init incident-response --path ./skills\nagentskills validate ./skills\nagentskills lint ./skills\n</code></pre> <p>For integration-specific examples, see each package page in the site navigation.</p>"},{"location":"adr/","title":"Architecture decision records","text":"<p>ADRs capture cross-package decisions that are expensive to re-litigate. Use them for architecture choices that constrain providers, integrations, release process, or public behavior.</p>"},{"location":"adr/#numbering-and-file-names","title":"Numbering and file names","text":"<ul> <li>Number sequentially: <code>0001</code>, <code>0002</code>, <code>0003</code>, ...</li> <li>Use a short slug: <code>docs/adr/0009-meaningful-slug.md</code></li> <li>Never renumber existing ADRs</li> </ul>"},{"location":"adr/#status-values","title":"Status values","text":"<ul> <li><code>Accepted</code> for current decisions</li> <li><code>Superseded by ADR XXXX</code> when replaced</li> <li><code>Proposed</code> for an in-flight decision</li> </ul>"},{"location":"adr/#template","title":"Template","text":"<pre><code># ADR XXXX \u2014 Short title\n\n**Status:** Accepted\n**Date:** YYYY-MM\n**Packages:** `agentskills-core`, `agentskills-fs`\n\n## Context\n\nWhat changed, what constraints matter, and why this needed a decision.\n\n## Decision\n\nWhat we chose and the concrete rule.\n\n## Consequences\n\n**Good**\n\n- Benefits and why this is worth the trade.\n\n**Costs**\n\n- Ongoing costs, coupling, or limitations.\n\n## Alternatives considered\n\n- Rejected option and why.\n\n## Decision history\n\n- Link to the issue or PR where the decision was made.\n</code></pre>"},{"location":"adr/#index","title":"Index","text":"<ul> <li>ADR 0001 \u2014 MCP context provider packaging</li> <li>ADR 0002 \u2014 Optional provider capabilities</li> <li>ADR 0003 \u2014 Fully async provider interface</li> <li>ADR 0004 \u2014 Multi-package lockstep versioning</li> <li>ADR 0005 \u2014 Provider caching and invalidation</li> <li>ADR 0006 \u2014 Exception taxonomy</li> <li>ADR 0007 \u2014 Binary resource envelope</li> <li>ADR 0008 \u2014 Logging conventions</li> <li>ADR 0009 \u2014 Native image content, opt-in</li> </ul>"},{"location":"adr/0001-mcp-context-provider-packaging/","title":"ADR 0001 \u2014 MCP context provider lives in <code>agentskills-mcp-server</code> behind an extra","text":"<p>Status: Accepted Date: 2026-02 Packages: <code>agentskills-mcp-server</code>, <code>agentskills-agentframework</code></p>"},{"location":"adr/0001-mcp-context-provider-packaging/#context","title":"Context","text":"<p>There are two ways to connect a Microsoft Agent Framework agent to skills:</p> <ul> <li>In-process \u2014 <code>AgentSkillsContextProvider(registry)</code> in <code>agentskills-agentframework</code>, wrapping a <code>SkillRegistry</code> directly.</li> <li>Out-of-process \u2014 <code>agentskills-mcp-server</code> exposes skills as MCP tools and resources over stdio or HTTP.</li> </ul> <p>The MCP path required manual wiring: connect via one of Agent Framework's MCP tool classes, read the catalog resources, assemble the system prompt, pass the tools. Agent Framework's MCP tool classes register the tools automatically but do nothing about instructions, so the catalog had to be injected by hand on every agent.</p> <p>We needed a bridge, and had to decide where it lived. <code>agentskills-mcp-server</code> is deliberately framework-agnostic \u2014 it serves LangChain, custom agents, CLI clients, and anything else that speaks MCP. Adding a hard <code>agent-framework</code> dependency to it was not acceptable.</p>"},{"location":"adr/0001-mcp-context-provider-packaging/#decision","title":"Decision","text":"<p><code>AgentSkillsMcpContextProvider</code> ships inside <code>agentskills-mcp-server</code>, behind an optional <code>[agentframework]</code> extra, rather than in <code>agentskills-agentframework</code> or a separate package.</p> Layer Install Role MCP server (core) <code>agentskills-mcp-server</code> Universal protocol layer, framework-agnostic MCP + AF adapter <code>agentskills-mcp-server[agentframework]</code> Adds the context provider, pulls in <code>agent-framework</code> In-process provider <code>agentskills-agentframework</code> Wraps a <code>SkillRegistry</code> directly <p>The adapter injects instructions only. It reads <code>skills://catalog/{format}</code> and <code>skills://tools-usage-instructions</code> from the MCP session and calls <code>context.extend_instructions()</code>.</p> <p>It deliberately does not inject or filter tools. Agent Framework's MCP tool classes already register MCP tools natively, and a given MCP server may expose tools beyond skills \u2014 the adapter has no reliable way to tell which tools are skill tools, so attempting to filter them would be guesswork.</p> <p>It also does not start or manage server processes, and does not import from <code>agentskills_mcp_server</code>'s server module. It only needs a generic MCP session, which makes it transport-agnostic across <code>MCPStdioTool</code>, <code>MCPSseTool</code>, and <code>MCPStreamableHttpTool</code>.</p>"},{"location":"adr/0001-mcp-context-provider-packaging/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>The base MCP package stays framework-agnostic; <code>agent-framework</code> is only pulled in when someone opts into the extra.</li> <li>Discoverable \u2014 MCP users find the adapter in the package they already installed, not a separate one they have to know exists.</li> <li>No tool duplication or fragile tool filtering.</li> <li>Works with any MCP transport, since only the session is required.</li> </ul> <p>Costs</p> <ul> <li><code>agentskills-mcp-server</code> now has framework-specific code in its tree, even though the dependency is optional. If a second framework adapter is ever needed, this pattern does not scale and the adapters should move to their own packages.</li> <li>Prompt-template validation is duplicated between the two context providers. Acceptable for two call sites; extract to a shared helper if a third appears.</li> </ul>"},{"location":"adr/0001-mcp-context-provider-packaging/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Put it in <code>agentskills-agentframework</code>. Rejected: that package's contract is \"wrap a registry in-process\". Adding an MCP-session-based provider would give it two unrelated entry points and an MCP dependency it otherwise does not need.</li> <li>A separate <code>agentskills-mcp-agentframework</code> package. Rejected: a seventh package for one thin class, and users would have to discover it existed.</li> </ul>"},{"location":"adr/0002-optional-provider-capabilities/","title":"ADR 0002 \u2014 Optional provider capabilities are opt-in methods with a declared flag","text":"<p>Status: Accepted Date: 2026-07 Packages: <code>agentskills-core</code>, <code>agentskills-fs</code>, <code>agentskills-http</code>, all integrations</p>"},{"location":"adr/0002-optional-provider-capabilities/#context","title":"Context","text":"<p><code>SkillProvider</code> is an ABC whose five methods are all <code>@abstractmethod</code>. Every one is satisfiable by any backend: given a skill ID, return metadata, body, or a named resource.</p> <p>Two planned features do not fit that shape:</p> <ul> <li><code>list_resources()</code> (v0.3 issue 4) \u2014 enumerate the references, scripts and assets a skill   contains. A filesystem provider can do this trivially. A static HTTP host cannot: there is no   directory listing over plain HTTP, so enumeration requires an out-of-band manifest that may   or may not exist.</li> <li><code>discover()</code> (v0.4 issue 5) \u2014 enumerate the skills a backend holds, rather than requiring   explicit registration. Same asymmetry.</li> </ul> <p>Adding either as a required abstract method breaks every third-party <code>SkillProvider</code> at import time. We are pre-1.0 and can technically do that, but the SDK is published and the contract is the one thing external implementors depend on. Meanwhile a provider that genuinely cannot enumerate needs a way to say so that is distinguishable from \"enumerated successfully, found nothing\".</p> <p>That distinction is the crux. An agent told a skill has no references stops looking. An agent told the provider cannot enumerate goes and reads the skill body for names. Collapsing the two produces a confidently wrong answer \u2014 the same failure mode as the binary-resource corruption fixed in v0.3 issue 5, where a silent fallback destroyed data without raising.</p>"},{"location":"adr/0002-optional-provider-capabilities/#decision","title":"Decision","text":"<p>Optional capabilities are concrete methods on <code>SkillProvider</code> with a default implementation that refuses, paired with a declared capability flag.</p> <pre><code>class SkillProvider(ABC):\n    supports_resource_listing: bool = False\n\n    async def list_resources(self, skill_id: str) -&gt; dict[str, list[str]]:\n        raise ResourceListingNotSupportedError(...)\n</code></pre> <p>Three parts, each load-bearing:</p> <ol> <li>Not abstract. Existing third-party providers keep working untouched. Adding a capability    is never a breaking change.</li> <li>Default raises, never returns empty. <code>ResourceListingNotSupportedError</code> is a new    <code>AgentSkillsError</code> subclass. Returning <code>{}</code> would let \"cannot enumerate\" masquerade as \"no    resources exist\". A caller that ignores the flag gets an exception, not a wrong answer.</li> <li>Flag is a plain attribute, not a <code>ClassVar</code>. Capability can depend on instance    configuration. <code>LocalFileSystemSkillProvider</code> sets it <code>True</code> at class level;    <code>HTTPStaticFileSkillProvider</code> sets it per instance from its <code>resource_manifest</code> argument,    because whether a manifest exists is a deployment fact, not a property of the class.</li> </ol> <p>Integrations check the flag and surface the distinction to the agent rather than propagating an exception, since \"this backend cannot list resources\" is information the agent can act on, not an error it can retry.</p>"},{"location":"adr/0002-optional-provider-capabilities/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>Third-party providers are unaffected by this and every future capability addition.</li> <li>The unsupported case is impossible to confuse with the empty case.</li> <li>Providers that can only sometimes enumerate are expressible.</li> <li>Callers have a cheap pre-flight check (<code>if provider.supports_resource_listing:</code>) that avoids   exception-driven control flow.</li> </ul> <p>Costs</p> <ul> <li>The contract is weaker. Consumers must branch on capability rather than relying on the   interface, and static type checking cannot enforce that they do.</li> <li>Capability flags and methods can drift apart \u2014 a provider could implement <code>list_resources()</code>   and forget the flag, or set the flag and not implement the method. Nothing enforces the pair.   A conformance test helper in core would close this; deferred until a third capability exists.</li> <li>Every new capability adds a flag. Past three or four this wants restructuring into a   capability set or protocol classes. Revisit at the v1.0 API freeze, which is the natural point   to promote well-exercised capabilities to required methods in a single documented break.</li> </ul>"},{"location":"adr/0002-optional-provider-capabilities/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Required abstract method. Rejected: cleanest contract, but breaks every external   implementor for a method a third of providers cannot honestly implement. Reconsider at v1.0   when the shape has stopped moving.</li> <li>Default returning <code>{}</code>. Rejected on the grounds above \u2014 it is the option the original   issue recommended, and it is the one that produces silent wrong answers.</li> <li>Separate mixin ABCs (<code>SupportsResourceListing</code>). Rejected for now: <code>isinstance</code> checks are   cleaner than flags and give better typing, but the mixin cannot express per-instance   capability, which the HTTP provider needs. Worth revisiting if per-instance capability turns   out to be rare.</li> <li>Capability negotiation object (<code>provider.capabilities</code> returning a frozen set). Rejected as   premature for two capabilities; it is the natural refactor once there are several.</li> </ul>"},{"location":"adr/0003-fully-async-provider-interface/","title":"ADR 0003 \u2014 Provider interface stays fully async","text":"<p>Status: Accepted Date: 2026-08 Packages: <code>agentskills-core</code>, <code>agentskills-fs</code>, <code>agentskills-http</code>, integrations</p>"},{"location":"adr/0003-fully-async-provider-interface/#context","title":"Context","text":"<p><code>SkillProvider</code> is consumed from async call sites (MCP server, integrations, registry operations), but filesystem work originally used sync I/O under async methods in the local provider. That blocked the event loop and constrained concurrency in long-running processes.</p> <p>A forked model with both sync and async provider APIs was considered unnecessary complexity for a codebase already standardized on asyncio.</p>"},{"location":"adr/0003-fully-async-provider-interface/#decision","title":"Decision","text":"<p>The provider contract remains fully async.</p> <ul> <li>Public provider operations are async methods.</li> <li>Blocking I/O in providers is moved to worker threads.</li> <li>No parallel sync provider interface is introduced.</li> </ul>"},{"location":"adr/0003-fully-async-provider-interface/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>One concurrency model across core, providers, and integrations.</li> <li>Event-loop safety is enforced at provider boundaries.</li> <li>Call sites do not need sync/async adapter branches.</li> </ul> <p>Costs</p> <ul> <li>Provider implementors must reason about thread handoff for blocking I/O.</li> <li>Contributors can accidentally reintroduce blocking paths without tests.</li> </ul>"},{"location":"adr/0003-fully-async-provider-interface/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Add a sync provider interface and adapters: rejected as duplicate API   surface and behavioral drift risk.</li> <li>Keep sync-under-async reads: rejected for event-loop blocking and poor   server concurrency.</li> </ul>"},{"location":"adr/0003-fully-async-provider-interface/#decision-history","title":"Decision history","text":"<ul> <li>v0.3 issue 3: Stop blocking the event loop in the filesystem provider</li> </ul>"},{"location":"adr/0004-multi-package-lockstep-versioning/","title":"ADR 0004 \u2014 Multi-package architecture with lockstep versions","text":"<p>Status: Accepted Date: 2026-08 Packages: all</p>"},{"location":"adr/0004-multi-package-lockstep-versioning/#context","title":"Context","text":"<p>The SDK is split into focused distributions (core, providers, integrations, CLI, testing) rather than one package with optional extras. This shapes install UX and keeps framework/provider dependencies out of <code>agentskills-core</code>.</p> <p>The cost is release coordination. Inter-package dependency floors can drift and produce import-time failures if one distribution resolves an older sibling.</p>"},{"location":"adr/0004-multi-package-lockstep-versioning/#decision","title":"Decision","text":"<p>Keep the multi-package architecture and enforce lockstep versioning.</p> <ul> <li>Each package is independently published.</li> <li>All packages share one release version per milestone.</li> <li>Dependents require the matching <code>agentskills-core</code> floor for that release.</li> <li>Release checks fail if versions or required floors drift.</li> </ul>"},{"location":"adr/0004-multi-package-lockstep-versioning/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>Consumers install only what they need.</li> <li>Core stays dependency-light and reusable.</li> <li>Packaging boundaries match responsibility boundaries.</li> </ul> <p>Costs</p> <ul> <li>Release process is more complex than a monolith.</li> <li>New packages must be wired into multiple scripts/workflows.</li> <li>Version/floor checks are mandatory to avoid skew.</li> </ul>"},{"location":"adr/0004-multi-package-lockstep-versioning/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>One package with extras: rejected for dependency bloat and weaker isolation   between concerns.</li> <li>Independent package versioning: rejected for current cadence and dependency   coupling; lockstep keeps compatibility obvious before 1.0.</li> </ul>"},{"location":"adr/0004-multi-package-lockstep-versioning/#decision-history","title":"Decision history","text":"<ul> <li>v0.3 issue 10: Automate PyPI publishing with Trusted Publishing</li> <li>v0.3 issue 10 implementation notes: floor drift and lockstep enforcement</li> <li>v0.4 issue 12: Simplify publish workflow</li> </ul>"},{"location":"adr/0005-provider-caching-and-invalidation/","title":"ADR 0005 \u2014 Provider caching is per instance with explicit invalidation","text":"<p>Status: Accepted Date: 2026-08 Packages: <code>agentskills-fs</code>, <code>agentskills-http</code></p>"},{"location":"adr/0005-provider-caching-and-invalidation/#context","title":"Context","text":"<p>Repeated reads of <code>SKILL.md</code> were on the hot path for validation, catalog construction, and tool calls. Without caching, HTTP providers paid redundant network round-trips and local providers repeatedly parsed frontmatter.</p> <p>Revalidation and correctness matter for mutable hosts, but unconditional revalidation conflicts with the \"single request across common flows\" goal.</p>"},{"location":"adr/0005-provider-caching-and-invalidation/#decision","title":"Decision","text":"<p>Cache <code>SKILL.md</code> per provider instance and expose explicit invalidation.</p> <ul> <li>Caching key is <code>skill_id</code> on the provider instance.</li> <li><code>invalidate(skill_id=None)</code> clears one entry or all.</li> <li>HTTP revalidation (<code>ETag</code>/<code>Last-Modified</code>) is opt-in via provider config.</li> <li>Only <code>SKILL.md</code> is cached; resources are fetched on demand.</li> </ul>"},{"location":"adr/0005-provider-caching-and-invalidation/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>Fast default path for common static-host workflows.</li> <li>Correctness for mutable hosts remains available explicitly.</li> <li>Caller-visible cache control is simple and predictable.</li> </ul> <p>Costs</p> <ul> <li>Revalidation mode and default mode differ by round-trip behavior.</li> <li>Resource reads remain uncached by design.</li> <li>Cache lifetime is tied to provider lifetime.</li> </ul>"},{"location":"adr/0005-provider-caching-and-invalidation/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Always revalidate: rejected because it defeats the one-fetch fast path.</li> <li>TTL-based cache: rejected as guesswork for content freshness.</li> <li>Cache resources too: rejected for unbounded memory growth and lower payoff.</li> </ul>"},{"location":"adr/0005-provider-caching-and-invalidation/#decision-history","title":"Decision history","text":"<ul> <li>v0.3 issue 1: Cache SKILL.md content in providers</li> </ul>"},{"location":"adr/0006-exception-taxonomy-not-found-vs-unavailable/","title":"ADR 0006 \u2014 Distinguish not found from unavailable","text":"<p>Status: Accepted Date: 2026-08 Packages: <code>agentskills-core</code>, <code>agentskills-http</code>, integrations</p>"},{"location":"adr/0006-exception-taxonomy-not-found-vs-unavailable/#context","title":"Context","text":"<p>Callers need different behavior for \"resource does not exist\" versus \"backend is temporarily unavailable\". Treating both as the same error either causes pointless retries or suppresses fallback paths.</p> <p>HTTP semantics made this concrete: <code>404</code>/<code>410</code> and <code>429</code>/<code>5xx</code> are different operational states and should be surfaced differently in the SDK.</p>"},{"location":"adr/0006-exception-taxonomy-not-found-vs-unavailable/#decision","title":"Decision","text":"<p>Keep a split exception taxonomy.</p> <ul> <li><code>SkillNotFoundError</code> indicates stable absence.</li> <li><code>SkillUnavailableError</code> indicates transient/backend unavailability.</li> <li><code>SkillUnavailableError.retry_after</code> is preserved when available.</li> <li>Integrations use the distinction to choose retry/fallback behavior.</li> </ul>"},{"location":"adr/0006-exception-taxonomy-not-found-vs-unavailable/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>Retry behavior can be correct by construction.</li> <li>Agents can surface meaningful next steps to users.</li> <li>Operational incidents are distinguishable from content errors.</li> </ul> <p>Costs</p> <ul> <li>Provider implementations must map backend failures carefully.</li> <li>More exception types increase contract surface area.</li> </ul>"},{"location":"adr/0006-exception-taxonomy-not-found-vs-unavailable/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>One generic provider failure type: rejected because it hides retry intent.</li> <li>Retry all failures: rejected because <code>404</code>/<code>410</code> are not recoverable by retry.</li> </ul>"},{"location":"adr/0006-exception-taxonomy-not-found-vs-unavailable/#decision-history","title":"Decision history","text":"<ul> <li>v0.3 issue 7: Map HTTP errors to SDK exceptions with retry hints</li> </ul>"},{"location":"adr/0007-binary-resource-json-envelope/","title":"ADR 0007 \u2014 Binary resources use a JSON envelope","text":"<p>Status: Accepted Date: 2026-08 Packages: <code>agentskills-core</code>, all integrations</p>"},{"location":"adr/0007-binary-resource-json-envelope/#context","title":"Context","text":"<p>Resource APIs return text. Binary content needs transport across multiple integrations with consistent behavior and without silent corruption.</p> <p>MCP has native binary content blocks, but other integrations and existing consumers expected one uniform shape from <code>encode_resource_content()</code>.</p>"},{"location":"adr/0007-binary-resource-json-envelope/#decision","title":"Decision","text":"<p>Binary resources are encoded as a JSON envelope with media type and base64 payload.</p> <ul> <li>UTF-8 text remains plain text.</li> <li>Non-text bytes are encoded in a JSON object.</li> <li>The envelope is the cross-integration contract for now.</li> </ul>"},{"location":"adr/0007-binary-resource-json-envelope/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>One representation across integrations.</li> <li>No lossy decode attempts for binary content.</li> <li>Callers can branch on a stable envelope shape.</li> </ul> <p>Costs</p> <ul> <li>Consumers must unwrap one extra layer.</li> <li>MCP-native binary features are not used directly yet.</li> <li>A future migration will need compatibility handling.</li> </ul>"},{"location":"adr/0007-binary-resource-json-envelope/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Return opaque bytes everywhere: rejected due to integration mismatch.</li> <li>Use MCP-native binary blocks only: rejected for inconsistent behavior across   non-MCP integrations.</li> </ul>"},{"location":"adr/0007-binary-resource-json-envelope/#decision-history","title":"Decision history","text":"<ul> <li>v0.3 issue 5: Return binary resources without corrupting bytes</li> <li>v0.4 follow-up note requesting a future reversal toward MCP-native blocks</li> </ul>"},{"location":"adr/0008-logging-namespace-and-severity/","title":"ADR 0008 \u2014 One agentskills logging namespace with intentional severity","text":"<p>Status: Accepted Date: 2026-08 Packages: all</p>"},{"location":"adr/0008-logging-namespace-and-severity/#context","title":"Context","text":"<p>Logs were inconsistent across distributions because module names reflected package import paths (<code>agentskills_http.*</code>, <code>agentskills_core.*</code>), which broke a single logger hierarchy and made operator-level filtering unreliable.</p> <p>Severity conventions also needed explicit rules to avoid duplicate or noisy error reporting between raising code and catching boundaries.</p>"},{"location":"adr/0008-logging-namespace-and-severity/#decision","title":"Decision","text":"<p>Use one <code>agentskills.*</code> logger namespace and keep severity semantics narrow.</p> <ul> <li>Logger names are rewritten to one shared <code>agentskills.*</code> hierarchy.</li> <li><code>WARNING</code> signals dropped/degraded behavior that execution can continue from.</li> <li><code>ERROR</code> is not used in libraries that are about to raise.</li> <li>Exception boundaries decide final severity where needed.</li> </ul>"},{"location":"adr/0008-logging-namespace-and-severity/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>One logger root can control verbosity across all packages.</li> <li>Logs align better with operational decision points.</li> <li>Duplicate \"logged and raised\" error events are reduced.</li> </ul> <p>Costs</p> <ul> <li>Contributors must follow conventions rather than default habits.</li> <li>Misuse can creep back without review discipline.</li> </ul>"},{"location":"adr/0008-logging-namespace-and-severity/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Keep distribution-native logger names: rejected for fragmented control.</li> <li>Log errors at every raise site: rejected because it duplicates events and   preempts caller severity decisions.</li> </ul>"},{"location":"adr/0008-logging-namespace-and-severity/#decision-history","title":"Decision history","text":"<ul> <li>v0.3 issue 8: Structured logging across the SDK</li> </ul>"},{"location":"adr/0009-native-image-content/","title":"ADR 0009 \u2014 Images are returned natively, behind an opt-in flag","text":"<p>Status: Accepted Date: 2026-10 Packages: <code>agentskills-core</code>, <code>agentskills-langchain</code>, <code>agentskills-agentframework</code>, <code>agentskills-mcp-server</code></p>"},{"location":"adr/0009-native-image-content/#context","title":"Context","text":"<p>ADR 0007 made every binary resource a JSON envelope carrying base64. That gave one shape across three integrations, and it worked, but it hands a model a picture of a diagram as a wall of base64 that it cannot see. Every modern chat API accepts image content natively; sending one through a text field is strictly worse for both cost and accuracy.</p> <p>Fixing this changes what a tool returns, from <code>str</code> to a union, in all three integrations. That is the kind of change that breaks callers quietly, and the three integrations do not even agree on what \"native\" means \u2014 MCP has <code>ImageContent</code>, LangChain has standard content blocks, Agent Framework has <code>Content(type=\"data\")</code>.</p> <p>There is also no way to ask a model whether it can see. Handing an image block to a text-only deployment is an API error from the provider, not a worse answer, so guessing wrong fails the whole call.</p>"},{"location":"adr/0009-native-image-content/#decision","title":"Decision","text":"<p>Classification lives in core, once. <code>classify_resource()</code> returns a <code>ResourceMedia</code> saying what the bytes are and whether they are worth rendering. All three integrations call it and differ only in how they wrap the result.</p> <p>Detection is by magic bytes, then by name. A name is a claim; bytes are evidence. A <code>.png</code> holding a ZIP is not renderable, and a real PNG called <code>.dat</code> is.</p> <p>Only PNG, JPEG, GIF and WebP qualify. PDF is read by some models and rejected by others, and guessing wrong is an API error. SVG is text that already arrives readable \u2014 rasterising it would replace something the model can reason about with something it can only look at.</p> <p>Native delivery is opt-in, per integration, via <code>vision=False</code>. With the default, behaviour is byte-for-byte what it was, so this is not a breaking change and needs no version gate. The caller declares the capability because only the caller knows which model the tools are bound to.</p> <p>Images get their own, much larger ceiling. <code>max_inline_image_bytes</code> defaults to 5 MiB, the lowest per-image limit among the major vision APIs, against 64 KiB for opaque binaries. The old cap tracks tokens, and base64 in a text field is billed per byte; a native image is billed by tile count, so the same 64 KiB would have turned nearly every real screenshot into a stub saying it was too large.</p> <p>Everything else keeps the envelope from ADR 0007, unchanged. Text, unrecognised binaries, and images past the ceiling all fall through to <code>encode_resource_content()</code>. Falling back is always safe, which is why the classifier is allowed to be conservative.</p> <p>Scripts are not affected. <code>get_skill_script</code> still returns <code>str</code>. A script is code; there is nothing to render, and widening its return type would buy nothing.</p>"},{"location":"adr/0009-native-image-content/#consequences","title":"Consequences","text":"<p>Good</p> <ul> <li>A diagram bundled with a skill is something the model can actually see.</li> <li>One classifier, so the three integrations cannot drift on what an image is.</li> <li>Existing callers are untouched until they ask to be.</li> <li>ADR 0007's envelope stays the fallback rather than being replaced, so there   is no migration.</li> </ul> <p>Costs</p> <ul> <li>Two return types to reason about per resource tool, once <code>vision</code> is on.</li> <li>The caller has to know whether its model can see, and gets an API error if it   is wrong. This is a real cost, accepted because the alternative is the   library guessing.</li> <li><code>list_skill_resources</code> still advertises names only, so a caller cannot know   in advance which resources will come back as images.</li> </ul>"},{"location":"adr/0009-native-image-content/#alternatives-considered","title":"Alternatives considered","text":"<ul> <li>Sniff the model's capabilities. Rejected: the integrations are handed a   registry, not a model, and capability strings are not standardised.</li> <li>Always return native, gated on a major version. Rejected: it forces every   caller to change on a schedule set by us, to get a feature many do not need.</li> <li>Classify by file extension. Rejected: the failure mode is promising a   renderable image and delivering bytes the provider rejects.</li> <li>Advertise renderability from <code>list_skill_resources</code>. Rejected: it would   mean fetching and classifying every resource's bytes to answer a listing   call, and classifying by name alone would sometimes lie. It saves one tool   call and costs a read of everything.</li> <li>Include PDF. Rejected for now; revisit when support is uniform.</li> </ul>"},{"location":"adr/0009-native-image-content/#decision-history","title":"Decision history","text":"<ul> <li>v0.5 issue 6: Vision-native assets</li> <li>ADR 0007 \u2014 Binary resources use a JSON envelope</li> </ul>"},{"location":"issues/","title":"Issue Specifications","text":"<p>Detailed write-ups for roadmap items, kept in the repo whether or not they have been filed as GitHub issues yet.</p> <p>The roadmap says what and why in one line per item. These files carry the full specification: problem statement, proposed approach, open questions, and acceptance criteria.</p> <p>Each file covers one milestone, in the same order as the corresponding roadmap table.</p> Milestone Items State v0.3 \u2014 Foundations 12 Shipped v0.4 \u2014 Developer Experience 12 Shipped v0.5 \u2014 Agent Effectiveness 6 Shipped v0.6 \u2014 Trust &amp; Operability \u2014 Not specified; see the roadmap v0.7 \u2014 Ecosystem \u2014 Not specified; see the roadmap v1.0 \u2014 Stability \u2014 Not specified; see the roadmap <p>A milestone only gets a file once its items are concrete enough to have acceptance criteria. The later ones are deliberately still one-liners on the roadmap.</p>"},{"location":"issues/#relationship-to-github-issues","title":"Relationship to GitHub Issues","text":"Lives here Lives on the issue Problem statement, proposed design, acceptance criteria yes a link back to here Status, assignee, milestone, discussion, linked PRs yes <p>When an item is filed, add its number to the heading so the two stay connected:</p> <pre><code>## 3. Stop blocking the event loop in the filesystem provider ([#42](https://github.com/pratikxpanda/agentskills-sdk/issues/42))\n</code></pre> <p>The issue body should link back to its section here rather than duplicating it \u2014 duplicated text is what drifts. If the design changes during implementation, update the spec here: the issue thread records the discussion, this file records the conclusion.</p> <p>Shipped items stay, marked <code>**Status:** Shipped in vX.Y</code>, so the reasoning behind a change remains findable. An item whose design turned out to be wrong gets a note explaining why rather than being quietly deleted.</p>"},{"location":"issues/#labels","title":"Labels","text":"Prefix Values <code>theme:</code> <code>correctness</code>, <code>performance</code>, <code>resilience</code>, <code>agent-effectiveness</code>, <code>interoperability</code>, <code>trust</code>, <code>operability</code>, <code>dx</code>, <code>ecosystem</code>, <code>project-health</code> <code>package:</code> <code>core</code>, <code>fs</code>, <code>http</code>, <code>langchain</code>, <code>agentframework</code>, <code>mcp-server</code>, <code>tools</code>, <code>testing</code>, <code>retrieval</code>, <code>adapters</code> <code>type:</code> <code>bug</code>, <code>feature</code>, <code>docs</code>, <code>chore</code> flat <code>good-first-issue</code>, <code>help-wanted</code>, <code>breaking-change</code>"},{"location":"issues/v0.3/","title":"v0.3 \"Foundations\"","text":"<p>Specifications for the v0.3 roadmap items, in the same order as the table in the roadmap. Milestone: <code>v0.3</code>.</p> <p>Add the GitHub issue number to a heading once the item is filed.</p>"},{"location":"issues/v0.3/#1-cache-skillmd-content-in-providers","title":"1. Cache <code>SKILL.md</code> content in providers","text":"<p>Labels: <code>theme:performance</code>, <code>type:feature</code>, <code>package:fs</code>, <code>package:http</code></p>"},{"location":"issues/v0.3/#problem","title":"Problem","text":"<p><code>LocalFileSystemSkillProvider</code> and <code>HTTPStaticFileSkillProvider</code> re-fetch and re-parse <code>SKILL.md</code> on every call to <code>get_metadata()</code> or <code>get_body()</code>. A single skill is read up to five times in one agent session:</p> # Code path Trigger 1 <code>validate_skill()</code> \u2192 <code>skill.get_body()</code> Registration 2 <code>validate_skill()</code> \u2192 <code>skill.get_metadata()</code> Registration 3 <code>registry.get_skills_catalog()</code> \u2192 <code>skill.get_metadata()</code> System prompt / MCP resource read 4 tool call \u2192 <code>skill.get_metadata()</code> Agent turn 5 tool call \u2192 <code>skill.get_body()</code> Agent turn <p>For the HTTP provider that is five network round-trips for one file. For the filesystem provider the I/O is cheaper but the redundant YAML parsing remains.</p>"},{"location":"issues/v0.3/#proposal","title":"Proposal","text":"<p>Add a content cache keyed by <code>skill_id</code> to the internal <code>_get_skill_md()</code> / <code>_read_skill_md()</code> helper in both providers.</p> <pre><code>self._skill_md_cache: dict[str, str] = {}\n\nasync def _get_skill_md(self, skill_id: str) -&gt; str:\n    if skill_id not in self._skill_md_cache:\n        self._skill_md_cache[skill_id] = await self._fetch(...)\n    return self._skill_md_cache[skill_id]\n</code></pre> <p>Cache is per provider instance. No public API change.</p> <p>For HTTP, prefer conditional requests (<code>If-None-Match</code> / <code>If-Modified-Since</code> using the stored <code>ETag</code> / <code>Last-Modified</code>) over a blind TTL \u2014 a <code>304</code> costs one cheap round-trip and keeps long-lived servers correct when a skill is republished. Expose an explicit <code>invalidate(skill_id=None)</code> for callers that need it.</p>"},{"location":"issues/v0.3/#acceptance-criteria","title":"Acceptance criteria","text":"<ul> <li>[x] HTTP provider issues exactly one request per skill across a full register \u2192 catalog \u2192 tool-call flow (assert on mocked <code>httpx</code> call count)</li> <li>[x] Filesystem provider reads each <code>SKILL.md</code> once (spy on <code>Path.read_text</code>)</li> <li>[x] Cache is per-instance \u2014 two providers do not share state</li> <li>[x] <code>invalidate()</code> clears one skill or all</li> <li>[x] HTTP revalidates with <code>ETag</code>/<code>Last-Modified</code> and handles <code>304</code></li> <li>[x] Existing tests pass unchanged</li> </ul>"},{"location":"issues/v0.3/#implementation-notes","title":"Implementation notes","text":"<p>Status: implemented, awaiting release.</p> <p>Two acceptance criteria conflict: \"exactly one request per skill\" and \"revalidates with <code>ETag</code>/<code>Last-Modified</code>\" cannot both hold under one default, because revalidation costs a round-trip per access even when it returns <code>304</code>. Resolved by making revalidation opt-in: <code>HTTPStaticFileSkillProvider(..., revalidate=True)</code>. The default serves cached content until <code>invalidate()</code> is called, which is right for the common case (a static host, a process that outlives a few agent turns). Long-lived servers fronting a mutable host should opt in.</p> <p>Only <code>SKILL.md</code> is cached. Scripts, assets and references are left uncached: they are larger, typically read once, and caching them would make the provider's memory footprint depend on untrusted content size.</p>"},{"location":"issues/v0.3/#related-follow-ups-file-separately-if-wanted","title":"Related follow-ups (file separately if wanted)","text":"<ul> <li><code>validate_skill()</code> calls <code>get_body()</code> and <code>get_metadata()</code> independently, parsing twice. Fetch and split once.</li> <li><code>SkillRegistry.get_skills_catalog()</code> re-derives metadata for every skill on each call; the registry has no <code>unregister()</code>, so the catalog string can be memoised.</li> <li>Both providers duplicate <code>_get_skill_md()</code> \u2192 <code>split_frontmatter()</code>; a shared helper returning <code>tuple[dict, str]</code> avoids double parsing.</li> </ul>"},{"location":"issues/v0.3/#2-build-the-skills-catalog-concurrently","title":"2. Build the skills catalog concurrently","text":"<p>Labels: <code>theme:performance</code>, <code>type:feature</code>, <code>package:core</code></p>"},{"location":"issues/v0.3/#problem_1","title":"Problem","text":"<p><code>SkillRegistry.get_skills_catalog()</code> awaits <code>get_metadata()</code> for each skill in sequence. With an HTTP-backed registry of N skills that is N serial round-trips on the critical path of the first agent turn.</p>"},{"location":"issues/v0.3/#proposal_1","title":"Proposal","text":"<p>Fan out with <code>asyncio.gather</code> and a bounded semaphore (default concurrency ~8, configurable). Preserve deterministic ordering by sorting results after the gather, not relying on completion order.</p>"},{"location":"issues/v0.3/#acceptance-criteria_1","title":"Acceptance criteria","text":"<ul> <li>[x] Catalog output is byte-identical to the serial implementation for the same registry</li> <li>[x] Metadata fetches run concurrently (assert with an instrumented provider)</li> <li>[x] Concurrency is bounded and configurable</li> <li>[x] A failure in one skill's metadata surfaces with the offending <code>skill_id</code> in the message</li> </ul>"},{"location":"issues/v0.3/#implementation-notes_1","title":"Implementation notes","text":"<p>Status: implemented, awaiting release.</p> <p>Concurrency is bounded by an <code>asyncio.Semaphore</code> and configured per registry: <code>SkillRegistry(catalog_concurrency=8)</code>. Values below 1 raise <code>ValueError</code>.</p> <p>Ordering is preserved by zipping the <code>asyncio.gather</code> results back against <code>list_skills()</code> rather than sorting afterwards \u2014 <code>gather</code> already returns results in argument order, so no sort is needed and the catalog stays byte-identical to the serial version.</p> <p>Skill ID context is attached with <code>exc.add_note()</code> (PEP 678) rather than by wrapping in a new exception. Wrapping would have changed the exception type callers see, breaking anyone catching <code>SkillNotFoundError</code> from a catalog build.</p>"},{"location":"issues/v0.3/#3-stop-blocking-the-event-loop-in-the-filesystem-provider","title":"3. Stop blocking the event loop in the filesystem provider","text":"<p>Labels: <code>theme:performance</code>, <code>type:bug</code>, <code>package:fs</code></p>"},{"location":"issues/v0.3/#problem_2","title":"Problem","text":"<p><code>LocalFileSystemSkillProvider</code> exposes an <code>async</code> interface but reads synchronously via <code>Path.read_text()</code> / <code>read_bytes()</code>. Every skill read blocks the event loop, which matters for servers handling concurrent agent sessions (including the MCP server in streamable-HTTP mode).</p>"},{"location":"issues/v0.3/#proposal_2","title":"Proposal","text":"<p>Move blocking I/O to a worker thread (<code>asyncio.to_thread</code>, or <code>anyio.to_thread.run_sync</code> to stay loop-agnostic). Keep the size check before the read where possible so oversized files are rejected without loading them.</p>"},{"location":"issues/v0.3/#acceptance-criteria_2","title":"Acceptance criteria","text":"<ul> <li>[x] No blocking filesystem calls on the event loop</li> <li>[x] Size limit still enforced</li> <li>[x] Concurrent reads from one provider work correctly</li> </ul>"},{"location":"issues/v0.3/#implementation-notes_2","title":"Implementation notes","text":"<p>Status: implemented, awaiting release.</p> <p>Used <code>asyncio.to_thread</code> rather than <code>anyio.to_thread.run_sync</code>. Loop-agnosticism was not worth a new runtime dependency on a provider package whose only current dependency is <code>agentskills-core</code>; the rest of the SDK is asyncio-based already. Revisit if anyone asks for Trio support.</p> <p>The whole helper moved to the worker thread, not just the <code>read_text()</code> call. The path-traversal guard (<code>Path.resolve()</code>, <code>is_dir()</code>, <code>is_file()</code>, <code>stat()</code>) issues syscalls too, and on a cold cache or a network mount those dominate. Moving only the read would have left most of the blocking behind.</p>"},{"location":"issues/v0.3/#4-add-resource-discovery-to-the-provider-contract","title":"4. Add resource discovery to the provider contract","text":"<p>Labels: <code>theme:correctness</code>, <code>type:feature</code>, <code>package:core</code>, <code>package:fs</code>, <code>package:http</code></p> <p>Status: implemented. The <code>breaking-change</code> label was dropped \u2014 the chosen design adds no abstract method, so existing third-party providers keep working unchanged.</p>"},{"location":"issues/v0.3/#problem_3","title":"Problem","text":"<p>There is no way for an agent to learn which references, scripts, or assets a skill contains. <code>get_reference(skill_id, name)</code> requires a name the agent can only obtain if <code>SKILL.md</code> happens to mention it in prose. Progressive disclosure is effectively half-blind: the agent can fetch a resource but cannot enumerate one.</p>"},{"location":"issues/v0.3/#proposal_3","title":"Proposal","text":"<p>Add to <code>SkillProvider</code>:</p> <pre><code>async def list_resources(self, skill_id: str) -&gt; dict[str, list[str]]:\n    \"\"\"Return available resource names grouped by kind: references, scripts, assets.\"\"\"\n</code></pre> <ul> <li>Filesystem: list the <code>references/</code>, <code>scripts/</code>, <code>assets/</code> subdirectories, applying the same identifier-safety rules used on read.</li> <li>HTTP: static servers cannot be enumerated. Suggest an optional per-skill <code>index.json</code> manifest \u2014 which also gives the supply-chain work a natural home for integrity hashes.</li> <li>Surface as a <code>list_skill_resources</code> tool in each integration and as an MCP resource.</li> </ul>"},{"location":"issues/v0.3/#open-question","title":"Open question","text":"<p>Adding a required abstract method is a breaking change for any third-party <code>SkillProvider</code>. Options:</p> <ol> <li>Required abstract method \u2014 clean contract, breaks implementors.</li> <li>Default implementation returning <code>{}</code> plus a <code>supports_resource_listing</code> capability flag \u2014 backward-compatible, weaker contract.</li> </ol> <p>Recommend (2) pre-1.0, revisiting at the API freeze.</p>"},{"location":"issues/v0.3/#acceptance-criteria_3","title":"Acceptance criteria","text":"<ul> <li>[x] Decision recorded as an ADR before implementation</li> <li>[x] <code>list_resources()</code> on the provider contract</li> <li>[x] Filesystem implementation, with traversal guards</li> <li>[x] HTTP behaviour decided and implemented</li> <li>[x] Exposed through LangChain, Agent Framework, and MCP integrations</li> <li>[x] Documented in each package README</li> </ul>"},{"location":"issues/v0.3/#implementation-notes_3","title":"Implementation notes","text":"<p>Deviation from option 2: the default raises, it does not return <code>{}</code>. A default returning <code>{}</code> makes \"I cannot enumerate this skill\" indistinguishable from \"this skill has no resources\". That is the same failure class as the binary-corruption bug in item 5 \u2014 a silently wrong answer is worse than a loud one, because nothing downstream can detect it. The default now raises <code>ResourceListingNotSupportedError</code>, which subclasses both <code>AgentSkillsError</code> and <code>NotImplementedError</code> so callers can catch it from either hierarchy. The capability flag from option 2 is kept, so callers can branch before paying for a call. Recorded in ADR 0002 along with the alternatives rejected (mixin ABCs cannot express a per-instance capability, which the HTTP provider needs).</p> <p>The flag is a plain class attribute, not a <code>ClassVar</code>. The HTTP provider's capability depends on its constructor argument, not on its type.</p> <p>Filesystem: skip, do not raise. Dotfiles, subdirectories, and symlinks resolving outside the root are silently omitted. Raising would let one stray <code>.DS_Store</code> or editor swapfile make a whole skill unlistable. The stricter invariant that matters is the inverse one: listing must never return a name that a subsequent <code>get_reference()</code> would reject.</p> <p>HTTP: manifest is opt-in, and a missing manifest is also \"not supported\". <code>index.json</code> is read only when the provider is constructed with <code>resource_manifest=True</code>. If that is set but a given skill has no manifest, the 404 is translated to <code>ResourceListingNotSupportedError</code> rather than an empty listing \u2014 same reasoning as above. Manifest entries are host-supplied data that get interpolated into URLs, so names failing the existing identifier check are dropped rather than trusted.</p> <p>Integrations return, not raise, on unsupported. The <code>list_skill_resources</code> tool catches <code>ResourceListingNotSupportedError</code> and returns <code>{\"supported\": false, \"note\": \"...\"}</code>. For an LLM caller, \"this cannot be listed\" is actionable information (fall back to the skill body), whereas a tool error invites a retry loop. The MCP server exposes the same data as a resource template, <code>skills://{skill_id}/resources</code>.</p> <p>Observation for v0.4. Three of the six tools in every integration are now near-identical <code>skill_id</code>/<code>name</code> wrappers over <code>encode_resource_content()</code>. The tool layer is ready to be generated from a single table rather than hand-written per framework.</p>"},{"location":"issues/v0.3/#5-stop-corrupting-binary-skill-resources","title":"5. Stop corrupting binary skill resources","text":"<p>Labels: <code>theme:correctness</code>, <code>type:bug</code>, <code>package:core</code>, <code>package:langchain</code>, <code>package:agentframework</code>, <code>package:mcp-server</code></p>"},{"location":"issues/v0.3/#problem_4","title":"Problem","text":"<p>All three integrations decode provider bytes the same way:</p> <pre><code>return (await skill.get_asset(name)).decode(\"utf-8\", errors=\"replace\")\n</code></pre> <p>For any non-text resource \u2014 a PNG, a PDF, a zip \u2014 <code>errors=\"replace\"</code> turns the payload into a run of U+FFFD replacement characters. The data is destroyed, no exception is raised, and the agent receives garbage it cannot distinguish from a legitimately odd document. The package READMEs currently document this as a \"Note\", which presents silent data loss as intended behaviour.</p> <p>The skill spec allows <code>assets/</code> to hold arbitrary files, so this is reachable through the documented happy path rather than an edge case.</p>"},{"location":"issues/v0.3/#proposal_4","title":"Proposal","text":"<p>A shared <code>encode_resource_content()</code> in <code>agentskills-core</code>, used by all three integrations so the behaviour cannot drift apart again:</p> <ol> <li>Sniff for binary content (null byte within the first 8 KiB).</li> <li>If it looks like text, attempt a strict UTF-8 decode and return the string on success.</li> <li>Otherwise return a JSON envelope carrying the media type (via <code>mimetypes</code>) and base64 content:</li> </ol> <pre><code>{\n  \"name\": \"architecture.png\",\n  \"media_type\": \"image/png\",\n  \"size_bytes\": 20481,\n  \"encoding\": \"base64\",\n  \"content\": \"iVBORw0KGgo...\"\n}\n</code></pre> <p>Base64 costs roughly 1.37x the raw bytes in characters, and a 1 MiB image would swamp a context window. Above a <code>max_inline_binary_bytes</code> ceiling (default 64 KiB, caller-overridable) return the same envelope with <code>\"encoding\": \"none\"</code> and a <code>note</code> explaining the omission, so the agent learns the resource exists and why it was not inlined.</p> <p>The strict decode in step 2 matters: it is what converts a silent corruption into a detected one. Falling back to the envelope on <code>UnicodeDecodeError</code> means no input path can produce replacement characters.</p>"},{"location":"issues/v0.3/#open-question_1","title":"Open question","text":"<p>MCP supports typed binary content blocks and resource links natively, so the JSON envelope is a lowest-common-denominator choice there. Options: ship the envelope everywhere first for consistency and specialise the MCP server afterwards, or diverge immediately. The envelope is the smaller change and keeps one code path; the MCP specialisation is worth doing but is a separate piece of work.</p>"},{"location":"issues/v0.3/#acceptance-criteria_4","title":"Acceptance criteria","text":"<ul> <li>[x] No integration calls <code>.decode(\"utf-8\", errors=\"replace\")</code> on resource bytes</li> <li>[x] A PNG asset round-trips: <code>base64.b64decode(envelope[\"content\"]) == original_bytes</code></li> <li>[x] <code>media_type</code> inferred from the resource name</li> <li>[x] UTF-8 text resources unchanged, including non-ASCII</li> <li>[x] Text that is valid UTF-8 but contains a null byte still returns intact</li> <li>[x] Oversized binaries return <code>\"encoding\": \"none\"</code> with an explanatory note, not truncated content</li> <li>[x] <code>max_inline_binary_bytes</code> overridable on <code>get_tools()</code> and <code>create_mcp_server()</code></li> <li>[x] READMEs describe the envelope; the existing \"replacement characters\" note is removed</li> </ul>"},{"location":"issues/v0.3/#implementation-notes_4","title":"Implementation notes","text":"<p>Status: implemented, awaiting release.</p> <p>The null-byte sniff in step 1 of the proposal was dropped. It contradicts the fifth acceptance criterion \u2014 valid UTF-8 containing a null byte would have been classified binary and wrapped in an envelope. The implementation is decode-first: attempt a strict UTF-8 decode, fall back to the envelope on <code>UnicodeDecodeError</code>. That is also cheaper than it looks, because a strict decode fails at the first invalid byte rather than scanning the whole payload. One code path, and no input can produce a replacement character.</p> <p>The ceiling comparison is <code>len(data) &gt; max_inline_binary_bytes</code>, so a resource exactly at the limit is still inlined. <code>max_inline_binary_bytes=0</code> never inlines; negative values raise <code>ValueError</code>.</p> <p>Two existing tests asserted the corruption was intended \u2014 <code>test_binary_content_decoded_with_replacement</code> in the LangChain and Agent Framework suites asserted <code>\"\\ufffd\" in result</code>. Both were replaced with round-trip assertions. Worth noting as a pattern: the bug was covered by tests, documented in three READMEs, and still a bug.</p>"},{"location":"issues/v0.3/#follow-up","title":"Follow-up","text":"<p>Once the envelope lands, have <code>create_mcp_server()</code> return binary resources as native MCP binary content blocks \u2014 or a resource link when oversized \u2014 rather than a JSON string the client has to parse. Text behaviour unchanged, and <code>encode_resource_content()</code> stays as-is for the non-MCP integrations.</p>"},{"location":"issues/v0.3/#6-optional-version-field-in-skill-frontmatter","title":"6. Optional <code>version</code> field in skill frontmatter","text":"<p>Labels: <code>theme:correctness</code>, <code>type:feature</code>, <code>package:core</code></p> <p>Status: implemented. Ships as a breaking change for skills that already carry a non-semver <code>version</code> \u2014 see the implementation notes.</p>"},{"location":"issues/v0.3/#problem_5","title":"Problem","text":"<p>Skills have no version. Consumers cannot pin, compare, or detect drift, and Agent Skills Hub needs versioned publishing and subscription pinning.</p>"},{"location":"issues/v0.3/#proposal_5","title":"Proposal","text":"<p>Optional <code>version</code> frontmatter field, validated as semver when present, surfaced through <code>get_metadata()</code> and included in the catalog. Fully backward-compatible \u2014 absence is valid.</p> <p>Raise the field with the upstream spec rather than shipping a silent proprietary extension.</p>"},{"location":"issues/v0.3/#acceptance-criteria_5","title":"Acceptance criteria","text":"<ul> <li>[x] <code>version</code> validated as semver when present, ignored when absent</li> <li>[x] Returned by <code>get_metadata()</code></li> <li>[x] Invalid semver fails registration with a clear message</li> <li>[x] Documented as an optional, non-spec field pending upstream adoption</li> </ul>"},{"location":"issues/v0.3/#pre-implementation-notes","title":"Pre-implementation notes","text":"<p>The criteria contradict each other. \"Fully backward-compatible\" and \"invalid semver fails registration\" cannot both hold: any skill already carrying a non-semver <code>version</code> field registers today and will stop registering. That is the right trade-off, but it is a breaking change for those skills and should be stated as one rather than described as compatible.</p> <p>YAML coerces the field before we ever see it. <code>version: 1.0</code> parses as a <code>float</code>, <code>version: 1</code> as an <code>int</code>, and <code>version: 2024-01-15</code> as a <code>datetime.date</code>. A validator that assumes <code>str</code> will raise an opaque <code>TypeError</code> on the three most likely authoring mistakes. Non-<code>str</code> values need an explicit error telling the author to quote the value.</p> <p>No new dependency. <code>agentskills-core</code> depends only on <code>pyyaml</code>; use the official semver regex from semver.org rather than pulling in a package for one match.</p>"},{"location":"issues/v0.3/#implementation-notes_5","title":"Implementation notes","text":"<p>This is a breaking change, and the issue text was wrong to call it compatible. Skills with no <code>version</code> are unaffected \u2014 that part holds. But <code>version</code> was previously an unknown key: <code>validate_skill()</code> logged a warning and registered the skill anyway. Anything carrying <code>version: 1.0</code>, <code>version: v2</code>, or a date now fails registration outright. The alternative \u2014 warn and continue \u2014 was rejected. A version that may or may not be parseable cannot be used for pinning or comparison, which is the entire reason for the field; a lenient validator would ship the syntax without the guarantee.</p> <p><code>get_metadata()</code> needed no change. It already returns the parsed frontmatter dict verbatim, so the field surfaced the moment it became legal. The criterion was satisfied by validation alone. Worth noting for future frontmatter fields: the only real work is validation and catalog rendering.</p> <p>Type errors are more likely than syntax errors, so they get the better message. A non-<code>str</code> value reports the actual parsed type and its repr, names YAML coercion as the cause, and shows the corrected line \u2014 <code>version: \"1.0.0\"</code>. Reporting \"expected str, got float\" would have been technically accurate and practically useless, because the author wrote something that looks like a version and has no reason to suspect the parser. This is the same failure mode as the existing <code>_OPTIONAL_FIELDS</code> type loop, which is why <code>version</code> is validated separately rather than added to that table.</p> <p>Catalog rendering is conditional. <code>&lt;version&gt;</code> and <code>- **Version**:</code> are emitted only when the field is set. The catalog goes into the system prompt on every turn, so unversioned skills must not pay for a feature they do not use.</p> <p><code>validate_version()</code> is exported. The Hub needs to validate a version at publish time, before a <code>Skill</code> object exists. Keeping the rule in one place is cheaper than having the Hub reimplement the regex and drift from it.</p>"},{"location":"issues/v0.3/#observations","title":"Observations","text":"<p>The semver regex is inlined, and that is a deliberate maintenance liability. It is stable \u2014 semver 2.0.0 has not changed since 2013 \u2014 but if the SDK ever needs comparison (range matching, \"is this newer than that\") rather than validation, a regex will not do it and the <code>semver</code> package should be adopted then. Validation alone does not justify the dependency.</p> <p><code>_KNOWN_KEYS</code> and <code>_OPTIONAL_FIELDS</code> are now out of step. <code>_KNOWN_KEYS</code> is derived from <code>_OPTIONAL_FIELDS</code> plus a literal set that now carries <code>version</code>. A third specially-validated field will make this genuinely confusing. When that happens the field table should become <code>{name: validator}</code> rather than <code>{name: type}</code>, which would fold the required fields, the optional type checks, and <code>version</code> into one declaration.</p>"},{"location":"issues/v0.3/#7-distinguish-not-found-from-unavailable-in-the-http-provider","title":"7. Distinguish \"not found\" from \"unavailable\" in the HTTP provider","text":"<p>Labels: <code>theme:resilience</code>, <code>type:bug</code>, <code>theme:security</code>, <code>package:core</code>, <code>package:http</code></p> <p>Status: implemented. Scope grew to include <code>agentskills-core</code> (the new <code>SkillUnavailableError</code>) and a credential leak confirmed during the work.</p>"},{"location":"issues/v0.3/#problem_6","title":"Problem","text":"<p><code>HTTPStaticFileSkillProvider</code> maps every non-2xx response \u2014 and every transport failure \u2014 to <code>SkillNotFoundError</code>. A <code>503</code>, a DNS failure, and a genuine <code>404</code> are indistinguishable to the caller, so nobody can implement sensible retry or alerting. A transient blip currently looks like a missing skill.</p>"},{"location":"issues/v0.3/#proposal_6","title":"Proposal","text":"<ul> <li>New <code>SkillUnavailableError(AgentSkillsError)</code> in core for transient/transport failures.</li> <li><code>404</code>/<code>410</code> \u2192 <code>SkillNotFoundError</code>. <code>5xx</code>, <code>429</code>, timeouts, connection errors \u2192 <code>SkillUnavailableError</code>.</li> <li><code>401</code>/<code>403</code> get a distinct message that does not echo credentials.</li> <li>Bounded retry with exponential backoff and jitter for retryable classes only; honour <code>Retry-After</code>. Retry count and timeout configurable on the constructor.</li> </ul>"},{"location":"issues/v0.3/#acceptance-criteria_6","title":"Acceptance criteria","text":"<ul> <li>[x] Error taxonomy implemented and documented in the package README error table</li> <li>[x] Retries only on retryable classes, with a cap</li> <li>[x] <code>Retry-After</code> honoured</li> <li>[x] No credential material in any exception message</li> <li>[x] Tests per status class via <code>respx</code></li> </ul>"},{"location":"issues/v0.3/#pre-implementation-notes_1","title":"Pre-implementation notes","text":"<p>The \"no credential material\" criterion is already violated today, via exception chaining. The provider raises <code>AgentSkillsError(...) from exc</code>, and the chained <code>httpx</code> exception's repr contains the full request URL \u2014 including the query string. Any deployment passing a SAS token or signed-URL credential through <code>params</code> leaks it into every traceback and every log line that renders one. Sanitising only the message we construct is not sufficient; the cause has to be suppressed or the URL redacted before chaining. This is a live issue, not a future one, and it is the reason this item should be treated as security-relevant rather than purely ergonomic.</p> <p>Retry must not swallow 304. <code>_stream_bytes</code> returns <code>(None, headers)</code> on a conditional-request hit. A retry wrapper that treats \"no body\" as a failure would break revalidation, and 304 must not be classified as unavailable.</p> <p>Retries interact with the manifest path. A missing <code>index.json</code> is an expected 404 that is translated to <code>ResourceListingNotSupportedError</code>; it must stay on the non-retryable branch.</p>"},{"location":"issues/v0.3/#implementation-notes_6","title":"Implementation notes","text":"<p>The credential leak was real and is fixed. Confirmed: <code>raise_for_status()</code> produces an <code>httpx.HTTPStatusError</code> whose message embeds the full URL, query string included, and chaining it put that into every rendered traceback. The cause is now suppressed with <code>from None</code> throughout the request path. Two regression tests assert on <code>traceback.format_exception(...)</code> output rather than on <code>str(exc)</code> \u2014 testing only the message would not have caught the original bug, since the message was already clean.</p> <p>Messages carry a base-relative path, not a redacted absolute URL. The first attempt redacted the query string and kept scheme and host, which is the conventional fix. It was abandoned because an existing security test asserts the base URL never appears in a message at all, and that guarantee is worth keeping: the host is the one piece an operator can always recover from configuration, whereas a leaked path in a shared log is not recoverable. Errors now read <code>HTTP 503 from /incident-response/SKILL.md</code>, which is enough to debug.</p> <p><code>401</code>/<code>403</code> are <code>AgentSkillsError</code>, not <code>SkillUnavailableError</code>. Retrying a rejected credential is pointless and, against hosts with lockout policies, harmful. They get a message naming <code>headers</code>/<code>params</code> as the thing to check, without echoing either.</p> <p>A <code>Retry-After</code> beyond <code>max_retry_delay</code> fails immediately rather than sleeping. Honouring a literal <code>Retry-After: 3600</code> would park a request path for an hour. The advice is not discarded \u2014 it is attached to the exception as <code>retry_after</code> so a scheduler can act on it \u2014 but the decision to wait that long belongs to the caller, not to a provider.</p> <p>Full jitter, not equal jitter. <code>random.uniform(0, delay)</code>. The registry builds its catalog concurrently, so without jitter every skill fetch retries in lockstep and hits the recovering server as a single wave. The tests pin the bound by stubbing <code>random.uniform</code>, not the value.</p> <p>304 stayed on the success path, as flagged above; the revalidation tests already covered it and continue to pass unchanged.</p> <p>Observation. <code>SkillUnavailableError.retry_after</code> is currently only ever populated by the HTTP provider. If a second provider gains transient failures, the backoff loop itself is the next thing to lift into core \u2014 it is provider-agnostic apart from the status-code mapping.</p>"},{"location":"issues/v0.3/#8-structured-logging-across-the-sdk","title":"8. Structured logging across the SDK","text":"<p>Labels: <code>theme:operability</code>, <code>type:feature</code>, <code>package:core</code>, <code>package:fs</code>, <code>package:http</code></p> <p>Status: implemented. Scope grew to <code>agentskills-mcp-server</code>, whose existing logger sat outside the namespace.</p>"},{"location":"issues/v0.3/#problem_7","title":"Problem","text":"<p>Outside one warning in <code>validation.py</code>, the SDK is silent. There is no way to see which skills were fetched, how long a fetch took, or why a provider fell back \u2014 which makes production support guesswork.</p>"},{"location":"issues/v0.3/#proposal_7","title":"Proposal","text":"<ul> <li>Module-level loggers under a consistent <code>agentskills.*</code> namespace.</li> <li>Library attaches no handlers and adds <code>NullHandler</code>; the host application configures output.</li> <li><code>DEBUG</code> for fetch/parse/cache events, <code>INFO</code> for registration outcomes, <code>WARNING</code> for degraded behaviour.</li> <li>A redaction helper applied to anything containing a URL, header, or query string so <code>Authorization</code> values and SAS tokens never reach a log record. Pairs with the error messages in issue 7.</li> </ul>"},{"location":"issues/v0.3/#acceptance-criteria_7","title":"Acceptance criteria","text":"<ul> <li>[x] Consistent logger namespace, <code>NullHandler</code> attached</li> <li>[x] No secret material in any log record (test with a provider configured with a bearer token and a SAS-style query param)</li> <li>[x] Logging conventions documented in <code>docs/DEVELOPMENT.md</code></li> </ul>"},{"location":"issues/v0.3/#implementation-notes_7","title":"Implementation notes","text":"<p><code>logging.getLogger(__name__)</code> does not produce a namespace, and that was the actual bug. The two existing loggers were named <code>agentskills_core.validation</code> and <code>agentskills_mcp_server.config</code>. Those are siblings, not descendants of a common root, so <code>logging.getLogger(\"agentskills\").setLevel(DEBUG)</code> reached neither. Distribution names use underscores; logger hierarchies use dots. <code>get_logger(__name__)</code> rewrites the prefix \u2014 <code>agentskills_http.static</code> \u2192 <code>agentskills.http.static</code> \u2014 which keeps the logger name tied to the module it lives in while giving the whole SDK one dial. Hand-written logger strings were rejected: six packages' worth of literals rot silently when a file moves.</p> <p>The <code>NullHandler</code> is attached once to the namespace root, not per module. The standard library documents the per-module form, but that only exists because most libraries have no common root. We do.</p> <p>There is no <code>ERROR</code> level anywhere in the SDK, deliberately. Every failure path already raises. Logging it as well reports one event twice and quietly takes the severity decision away from the caller, who is the only party who knows whether a missing skill was fatal. The one <code>WARNING</code> that is not a retry \u2014 an unrecognised metadata key \u2014 is a case where nothing is raised at all.</p> <p>Redaction reuses issue 7's work rather than inventing a second mechanism. The HTTP provider already had a private <code>_describe()</code> returning a base-relative path. That is strictly stronger than the \"strip the query string\" helper this issue proposed, and building a second, weaker sanitiser for logs would have guaranteed drift between what an exception says and what a log line says. <code>_describe()</code> was lifted into core as <code>redact_url(url, relative_to=...)</code>, now tested directly, and <code>_describe()</code> is a one-line call through to it.</p> <p>One real bug surfaced while lifting it: the old implementation ended <code>or url</code>, falling back to the raw URL when the relative path came out empty \u2014 which happens when the fetched URL equals the base. That fallback leaked the query string in exactly the situation it was meant to protect. <code>redact_url()</code> now falls back to a host-and-path form with the query replaced, never the raw input.</p> <p>Headers are not logged, redacted or otherwise. A <code>redact_headers()</code> helper was considered and dropped. No operational question is answered by a header value, and a redactor that only works if every future caller remembers to invoke it is a trap \u2014 the safe default is to have nothing to redact. Same reasoning drove the explicit ban on <code>exc_info</code> for transport failures: <code>httpx</code> exception reprs embed the full request URL, so <code>_logger.warning(..., exc_info=exc)</code> in the retry loop would have re-opened the exact leak issue 7 closed. There is a comment at that line saying so.</p> <p>The retry loop is where the real value is. Before this change a provider could burn three attempts and several seconds of backoff with no external trace whatsoever. That is now a <code>WARNING</code> naming the sanitised path, the sleep, and the attempt number.</p>"},{"location":"issues/v0.3/#observations_1","title":"Observations","text":"<p><code>agentskills_core/logging.py</code> shadows a standard-library module name. It is safe \u2014 Python 3 absolute imports mean <code>import logging</code> inside the package still resolves to the stdlib \u2014 and it matches the package's existing noun-per-module convention (<code>encoding</code>, <code>parsing</code>, <code>validation</code>). Flagged because it reads like a bug at a glance and will attract at least one drive-by \"fix\".</p> <p>The secret-leak tests assert on <code>caplog.text</code>, and one of them asserts the log is non-empty. A test that greps for a token in an empty buffer passes for the wrong reason. Worth copying to any future test of the same shape.</p> <p>Timing is still not instrumented. The issue's problem statement asked how long a fetch took; nothing here answers that. Emitting durations means either wrapping every provider call in a timer or exposing metrics properly, and the second is the honest answer. Deferred to the observability work rather than half-solved with <code>time.monotonic()</code> deltas sprinkled through the providers.</p>"},{"location":"issues/v0.3/#9-enforce-a-coverage-floor-in-ci","title":"9. Enforce a coverage floor in CI","text":"<p>Labels: <code>theme:project-health</code>, <code>type:chore</code>, <code>good-first-issue</code></p> <p>Status: implemented.</p>"},{"location":"issues/v0.3/#problem_8","title":"Problem","text":"<p><code>pytest-cov</code> is not a dev dependency and CI has no coverage gate, so regressions in test coverage are invisible.</p>"},{"location":"issues/v0.3/#proposal_8","title":"Proposal","text":"<p>Add <code>pytest-cov</code>, wire <code>python scripts/dev.py test:cov</code> into the CI test job, set a floor (start at current measured coverage, ratchet upward), publish the report, and add a badge.</p>"},{"location":"issues/v0.3/#acceptance-criteria_8","title":"Acceptance criteria","text":"<ul> <li>[x] Coverage measured per package and in aggregate</li> <li>[x] CI fails below the floor</li> <li>[x] Badge in the root README</li> </ul>"},{"location":"issues/v0.3/#implementation-notes_8","title":"Implementation notes","text":"<p>The pre-existing <code>test:cov</code> task measured the wrong thing. It passed <code>--cov=packages</code>, a path, which counts the test files as covered code. A test file is trivially covered by the act of running it, so 2,822 of the 3,712 measured statements were tests marking themselves 100%. Reported coverage was 99%; real library coverage was 96.42%. Coverage is now configured with <code>source_pkgs</code> \u2014 import names, not paths \u2014 so only shipped code is measured. Branch coverage is on, which the old configuration also lacked.</p> <p>An aggregate floor alone is not a gate, and the issue's first criterion is why. With six packages summed, <code>agentskills-mcp-server</code> can sit at 91% indefinitely because <code>agentskills-core</code> at 99% carries the average. Floors are therefore enforced at two levels: the aggregate through <code>[tool.coverage.report] fail_under</code>, so that even a bare <code>coverage report</code> is gated, and per package through <code>COVERAGE_FLOORS</code> in <code>scripts/dev.py</code>, which runs <code>coverage report --include</code> once per distribution against the same <code>.coverage</code> file. This was verified rather than assumed: adding five uncovered statements to <code>agentskills_core</code> fails the per-package gate at 97% against its floor of 99, while the aggregate does not move off 96%. An aggregate-only gate would have passed that change.</p> <p>Floors start at the measured value, truncated to an integer \u2014 core 99, fs 97, http 96, langchain 100, agentframework 100, mcp_server 91, aggregate 96. Setting them lower \"for headroom\" bakes in slack that nobody ever removes. Two packages sit at 100, which means any new uncovered line fails CI; for a package that is currently fully covered that is the correct signal, not an inconvenience.</p> <p>Floors live in exactly one place each. The aggregate is in <code>pyproject.toml</code> because coverage enforces it natively; the per-package values are in <code>dev.py</code> because nothing enforces those natively. The aggregate is deliberately not repeated in <code>dev.py</code> \u2014 the final <code>coverage report</code> call passes no <code>--fail-under</code>, so it reads the value from config. Two copies of a threshold is two thresholds.</p> <p>The badge states the enforced floor, not a measurement. <code>coverage \u226596%</code> is guaranteed true by CI on every commit. A live per-commit percentage needs Codecov or Coveralls, which means an account, a repository authorisation, and usually a token \u2014 a decision for the maintainer, not something to slip into a chore PR. A hand-updated static number would have been the worst of both: it can silently claim 96% while the truth is 80%. The floor formulation cannot drift misleadingly, because raising the floor and updating the badge are the same commit by construction. The live number is still visible: the CI job writes the full report to the GitHub Actions job summary of every run, and uploads <code>coverage.xml</code> and <code>htmlcov/</code> as an artifact.</p> <p>Coverage runs as its own job, not inside the test matrix. Running it three times across 3.12/3.13/3.14 would triple the cost to answer the same question, and a version-dependent coverage difference would be a finding in its own right rather than something to average away.</p>"},{"location":"issues/v0.3/#observations_2","title":"Observations","text":"<p><code>scripts/dev.py</code> claimed to be \"Development task runner for Pallium\". Copied from a sibling repository and never corrected. Fixed in passing.</p> <p><code>scripts/</code> is not in the lint scope. CI runs <code>ruff</code> over <code>packages/ examples/</code> only, so <code>dev.py</code> has drifted from the formatter \u2014 <code>ruff format</code> wants to rewrite lines this change did not touch. Left alone rather than mixing a whole-file reformat into a coverage PR, but the scope should be widened and the file formatted once, deliberately.</p> <p>The gate's floor sits just below three known soft spots, which is worth naming rather than quietly accepting. In order of value: <code>agentskills_mcp_server/__main__.py</code> at 75% \u2014 the documented way to launch the server, with its config-loading error paths and the entire <code>_build</code>/<code>run</code> path untested; <code>agentskills_mcp_server/context_provider.py</code> at 83%, where the uncovered block is the import guard added by issue 11, so the two carefully worded upgrade messages have never been executed; and the <code>_stream_bytes</code> size-limit branches in the HTTP provider. These are the ratchet's first targets.</p> <p>Output ordering in <code>dev.py</code> was scrambled. Its banners are printed by Python and buffered, while subprocess output goes straight to the terminal, so every banner appeared after every command's output \u2014 unreadable in a CI log with eight sequential invocations. Fixed with an explicit flush.</p>"},{"location":"issues/v0.3/#10-automate-pypi-publishing-with-trusted-publishing","title":"10. Automate PyPI publishing with Trusted Publishing","text":"<p>Labels: <code>theme:project-health</code>, <code>type:chore</code></p> <p>Status: implemented. Requires one-time Trusted Publishing configuration on PyPI and TestPyPI (six projects each) plus two GitHub Environments before the first tag; see DEVELOPMENT.md. Until that is done, the workflow runs and fails at the upload step.</p>"},{"location":"issues/v0.3/#problem_9","title":"Problem","text":"<p>Releasing means a maintainer running <code>scripts/publish.ps1</code> locally with PyPI credentials, in dependency order, across six packages. It is manual, unauditable, requires a long-lived token on a workstation, and is easy to get half-done.</p>"},{"location":"issues/v0.3/#proposal_9","title":"Proposal","text":"<p>GitHub Actions workflow triggered by the release tag, using PyPI Trusted Publishing (OIDC, no stored token):</p> <ul> <li>Build all six packages</li> <li>Publish in dependency order: <code>core</code> \u2192 providers \u2192 integrations</li> <li>Gate on a GitHub Environment requiring manual approval</li> <li>Dry-run to TestPyPI on release-candidate tags</li> <li>Attach build provenance attestations (prerequisite for artifact signing in v1.0)</li> </ul> <p>Keep <code>publish.ps1</code> for local/emergency use.</p>"},{"location":"issues/v0.3/#acceptance-criteria_9","title":"Acceptance criteria","text":"<ul> <li>[x] Tag push publishes all six packages with no stored credentials</li> <li>[x] Dependency order enforced; a failure mid-sequence is clearly reported</li> <li>[ ] TestPyPI path verified end-to-end \u2014 blocked on the one-time setup below. The routing,       the build loop and the version guard were exercised locally; the upload itself cannot be       verified until the TestPyPI trusted publishers exist. Run the workflow manually once they       do, before the first real tag.</li> <li>[x] Release process in <code>docs/DEVELOPMENT.md</code> updated</li> </ul>"},{"location":"issues/v0.3/#implementation-notes_9","title":"Implementation notes","text":"<p>Tag routing allow-lists rather than deny-lists. The obvious reading of \"dry-run to TestPyPI on release-candidate tags\" is to match <code>-rc</code> and send those to TestPyPI. That fails open: a typo'd tag, a <code>nightly</code>, or a <code>v0.3</code> with a missing component all match nothing and go straight to production. The condition is inverted instead \u2014 only <code>^v[0-9]+\\.[0-9]+\\.[0-9]+$</code> reaches PyPI, and everything else falls through to TestPyPI. Verified against <code>v0.3.0</code>, <code>v0.3.0rc1</code>, <code>v0.3.0-rc.1</code>, <code>v1.2.3</code>, <code>nightly</code> and <code>v0.3</code>. The worst outcome of a bad tag is now a wasted TestPyPI version, not a burnt PyPI one.</p> <p>One approval, not six. GitHub environment reviewers approve per job, so the natural one-job-per-package layout would have demanded five or six approvals per release. Building is therefore split from publishing: one build job produces all six distributions in parallel, and one publish job uploads them as six sequential steps behind a single environment gate. Sequential steps also satisfy \"a failure mid-sequence is clearly reported\" better than a matrix \u2014 the step name is the package name, and matrix ordering is not guaranteed even at <code>max-parallel: 1</code>.</p> <p><code>skip-existing: true</code> on both targets. The issue's own complaint is that a release is \"easy to get half-done\"; that is only true if resuming is impossible. With skip-existing, re-running the failed job finishes the release instead of erroring on the packages that already went up. The failure mode this normally introduces \u2014 silently publishing nothing because the version was never bumped \u2014 is closed by the version guard rather than by refusing to resume.</p> <p>A version guard runs before anything is built. <code>scripts/check_release_version.py</code> reads all six <code>pyproject.toml</code> files, asserts they agree, and asserts the agreed version equals the tag minus its <code>v</code>. Exact string comparison rather than PEP 440 parsing, so the tag, the wheel filename, and the PyPI page are spelled identically; the cost is that a pre-release must be tagged <code>v0.3.0rc1</code>, not <code>v0.3.0-rc.1</code>. The script is useful on its own before tagging.</p> <p>Dependency order is a user-visible property, not a PyPI constraint. PyPI will happily accept <code>agentskills-fs</code> before <code>agentskills-core</code> exists \u2014 dependencies resolve at install time. The order matters only for the window during a release when someone runs <code>pip install</code>. Recorded in a comment, because the natural assumption is that the ordering is technically required, and someone will otherwise \"optimise\" it into a parallel matrix.</p> <p>Script injection was avoided in the shell steps. Tag names reach the runner as data, so <code>github.ref_name</code> is passed through <code>env:</code> and referenced as <code>\"$REF\"</code> rather than interpolated into the script body with <code>${{ }}</code>.</p>"},{"location":"issues/v0.3/#observations_3","title":"Observations","text":"<p>The GitHub Release was being created whether or not publishing worked. <code>release.yml</code> fired on the same tag, independently and immediately, so it would have produced a release announcing 0.3.0 even if every upload failed \u2014 and, once the approval gate existed, it would have announced the release before the reviewer approved it. The job moved into <code>publish.yml</code> behind <code>needs: publish</code>, and <code>release.yml</code> is deleted. Tags that route to TestPyPI now produce a GitHub pre-release rather than a full one.</p> <p>Six PyPI projects \u00d7 two indexes = twelve trusted-publisher configurations, none of which can be created from the repository. This is the one part of the release path that stays manual, and it fails at the last step of the first release if it is skipped. Called out at the top of this issue and in DEVELOPMENT.md rather than left to be discovered.</p> <p><code>publish.ps1</code> is now emergency-only and should be labelled as such in the script itself. It is the only remaining path that needs a long-lived token, and the whole point of this change was to stop those existing. Left in place per the proposal, but it will drift.</p> <p>Found while preparing 0.3.0: the inter-package constraints would have shipped issue 11 again. Every dependent required <code>agentskills-core = \"&gt;=0.1.0,&lt;1.0\"</code>, and <code>agentskills-mcp-server</code> had no upper bound at all. But <code>agentskills_mcp_server/config.py</code> imports <code>get_logger</code>, which this milestone added \u2014 so <code>pip install agentskills-mcp-server==0.3.0</code> could legally resolve core 0.1.0 and fail at import, exactly as the published 0.2.3 packages did. Floors raised to <code>&gt;=0.3.0,&lt;1.0</code>; <code>check_release_version.py</code> now refuses to build unless every dependent requires the core version being released, and <code>bump-version.ps1</code>/<code>.sh</code> rewrite the constraint alongside the version so it cannot drift again. The six ship in lockstep, so exact-match is the right rule.</p>"},{"location":"issues/v0.3/#11-restore-compatibility-with-agent-framework-1x","title":"11. Restore compatibility with Agent Framework 1.x","text":"<p>Labels: <code>theme:correctness</code>, <code>type:bug</code>, <code>package:agentframework</code>, <code>package:mcp-server</code></p> <p>Status: implemented.</p>"},{"location":"issues/v0.3/#problem_10","title":"Problem","text":"<p><code>agent-framework-core</code> renamed <code>BaseContextProvider</code> to <code>ContextProvider</code>. Both integrations still import the old name:</p> <pre><code>from agent_framework import BaseContextProvider, FunctionTool\n</code></pre> <p>Our constraint is <code>agent-framework-core = \"&gt;=1.0.0rc3,&lt;2.0\"</code>, which admits 1.12.1, so a fresh <code>pip install agentskills-agentframework</code> resolves the new version and raises <code>ImportError</code> at import time. The published 0.2.3 packages are broken for anyone installing today. CI and local runs pass only because <code>poetry.lock</code> pins 1.0.0rc3.</p> <p>Confirmed on a clean Python 3.14 environment: 23 failures across <code>agentskills-mcp-server/tests/test_context_provider.py</code>, plus collection errors in both <code>agentskills-agentframework</code> test modules. Every one traces to this rename.</p> <p>A second defect compounds it. The lazy import in <code>context_provider.py</code> catches <code>ImportError</code> and re-raises:</p> <p><code>AgentSkillsMcpContextProvider requires agent-framework. Install it with: pip install agentskills-mcp-server[agentframework]</code></p> <p>The package is installed. The advice is wrong, and it sends the user to reinstall a dependency they already have instead of revealing the version incompatibility.</p>"},{"location":"issues/v0.3/#proposal_10","title":"Proposal","text":"<ul> <li>Import <code>ContextProvider</code>, falling back to <code>BaseContextProvider</code> so both generations work,   or drop the fallback and raise the floor to the release that introduced the new name.</li> <li>Decide the supported range deliberately and encode it. <code>&lt;2.0</code> was never verified against   anything past the release candidate.</li> <li>Re-raise the original exception as <code>__cause__</code> so the real error survives, and only claim   the dependency is missing when it genuinely is.</li> <li>Add a CI job that resolves dependencies without the lock file, so upstream breaking changes   surface here rather than in a user's install.</li> </ul>"},{"location":"issues/v0.3/#acceptance-criteria_10","title":"Acceptance criteria","text":"<ul> <li>[x] Both integrations import cleanly against <code>agent-framework-core</code> 1.12.1</li> <li>[x] Full suite passes on a lock-free install</li> <li>[x] Version constraint reflects the range actually tested</li> <li>[x] The \"requires agent-framework\" message appears only when the package is absent</li> <li>[x] Unlocked-resolution job added to CI</li> </ul>"},{"location":"issues/v0.3/#open-question_2","title":"Open question","text":"<p><code>allow-prereleases</code> and its <code>TODO: Remove once a stable 1.0+ is available</code> are now obsolete \u2014 1.12.1 is stable. Removing the flag belongs with this fix.</p>"},{"location":"issues/v0.3/#12-support-python-314","title":"12. Support Python 3.14","text":"<p>Labels: <code>theme:project-health</code>, <code>type:bug</code>, <code>package:all</code></p> <p>Status: merged, awaiting release.</p> <p>Every package pinned <code>python = \"&gt;=3.12,&lt;3.14\"</code>, so installation failed outright on current stable Python. The README attributed this to upstream dependencies; none of them justified it. <code>pyyaml</code>, <code>httpx</code>, <code>langchain-core</code>, <code>mcp</code>, <code>pydantic</code>, <code>pyjwt</code> and <code>agent-framework-core</code> all declare 3.14 support. The cap was inherited across all six packages, including <code>agentskills-core</code>, whose only dependency is <code>pyyaml</code>.</p> <p>Ceiling raised to <code>&lt;4.0</code> rather than removed, because <code>langchain-core</code> declares <code>&lt;4.0.0</code> and Poetry requires a project range that is a subset of its dependencies. 3.14 added to the CI matrix and the classifiers.</p> <p>Verified on Python 3.14.6: all six packages install, and 265 tests pass. The only failures are issue 11 above, which is unrelated to the Python version.</p> <p>The fix has no effect until a release goes out \u2014 the <code>&lt;3.14</code> cap is baked into the 0.2.3 metadata already on PyPI and cannot be changed retroactively.</p>"},{"location":"issues/v0.3/#unblocked-side-note","title":"Unblocked side note","text":"<p><code>mypy</code> is referenced by <code>scripts/dev.py</code> and <code>docs/DEVELOPMENT.md</code> but is not installed in the dev environment, so <code>dev.py typecheck</code> and <code>dev.py check</code> fail. Worth folding into issue 9 or filing as a separate <code>type:chore</code>.</p>"},{"location":"issues/v0.4/","title":"v0.4 \"Developer Experience\"","text":"<p>Specifications for the v0.4 roadmap items, in the same order as the table in the roadmap. Milestone: <code>v0.4</code>.</p> <p>Add the GitHub issue number to a heading once the item is filed.</p>"},{"location":"issues/v0.4/#1-agentskills-cli","title":"1. <code>agentskills</code> CLI","text":"<p>Labels: <code>theme:dx</code>, <code>type:feature</code>, <code>package:tools</code></p> <p>Status: implemented. Both open questions were resolved toward the dependency-light answer; see the implementation notes.</p>"},{"location":"issues/v0.4/#problem","title":"Problem","text":"<p>There is no way to work with a skill without writing Python. Validation only happens inside <code>registry.register()</code>, at runtime, in someone's application \u2014 so a malformed <code>SKILL.md</code> is discovered by an agent failing to start rather than by the author who wrote it. Authoring a new skill means copying an existing folder and hoping the frontmatter is right.</p>"},{"location":"issues/v0.4/#proposal","title":"Proposal","text":"<p>A new <code>agentskills-tools</code> package exposing an <code>agentskills</code> entry point.</p> Command Behaviour <code>agentskills init &lt;name&gt;</code> Scaffold <code>SKILL.md</code> with valid frontmatter plus empty <code>references/</code>, <code>scripts/</code>, <code>assets/</code> folders. <code>agentskills validate &lt;path&gt;</code> Run <code>validate_skill()</code> against one skill or a directory of them. Human-readable output by default, <code>--format json</code> for machines. Exit non-zero on failure \u2014 this is the contract CI depends on. <code>agentskills lint &lt;path&gt;</code> Non-fatal quality warnings: description too long for a catalog, body over a token budget, resources present but never referenced from the body, missing <code>version</code>. <code>agentskills inspect &lt;path&gt;</code> Render what an agent would actually see \u2014 the catalog entry, the metadata, the body \u2014 so authors can check token cost before shipping. <code>agentskills serve &lt;path&gt;</code> Run the MCP server over a folder of skills without hand-writing a <code>server.json</code>. <p>Keep the dependency footprint small; the CLI should install without pulling in any framework integration.</p>"},{"location":"issues/v0.4/#open-questions","title":"Open questions","text":"<ul> <li>Separate package, or an extra on <code>agentskills-core</code> (<code>pip install agentskills-core[cli]</code>)? A separate package keeps core dependency-light, which is a stated product principle.</li> <li>Which argument parser \u2014 stdlib <code>argparse</code> (no dependency) or <code>typer</code>/<code>click</code> (nicer UX, extra dependency)?</li> </ul>"},{"location":"issues/v0.4/#acceptance-criteria","title":"Acceptance criteria","text":"<ul> <li>[x] All five commands implemented with <code>--help</code></li> <li>[x] <code>validate</code> exits non-zero on any failure and zero on success</li> <li>[x] <code>validate</code> handles both a single skill folder and a directory of skills</li> <li>[x] <code>--format json</code> output is stable and documented</li> <li>[x] Installs without any framework integration dependency</li> <li>[x] Package README with examples</li> </ul>"},{"location":"issues/v0.4/#implementation-notes","title":"Implementation notes","text":"<p>Both open questions were answered the same way, for the same reason. A separate <code>agentskills-tools</code> package rather than <code>agentskills-core[cli]</code>, and stdlib <code>argparse</code> rather than <code>click</code> or <code>typer</code>. Item 2 installs this CLI into other people's CI, so every dependency it carries is a supply-chain edge those repositories inherit for the privilege of checking a markdown file. <code>argparse</code> costs some polish in <code>--help</code>; that is the whole price.</p> <p>The MCP server is an optional <code>serve</code> extra for the same reason: <code>mcp</code> and <code>pydantic</code> have no business being installed by a job that only runs <code>validate</code>.</p> <p>The forgiving parser was the actual problem worth solving. <code>split_frontmatter()</code> returns <code>({}, raw)</code> on a YAML error rather than raising \u2014 a deliberate choice in core, since a provider should not explode on one bad skill. But it means a mistyped colon arrives at <code>validate_skill()</code> as \"metadata missing required 'name'\" and \"metadata missing required 'description'\", which tells an author nothing about what is wrong. The CLI therefore parses the frontmatter itself first and reports the real cause: missing delimiter, unclosed block, oversized block, invalid YAML, or a sequence where a mapping belongs. Spec checks are skipped when that fails, because they can only restate the damage.</p> <p>This is also where line numbers come from. <code>problem_mark.line + 1</code> maps a YAML error back to a <code>SKILL.md</code> line, which is exactly what item 2 needs to annotate a pull request diff inline \u2014 without it the Action can only annotate at file level.</p> <p>Exit code 2 exists to keep \"found a problem\" apart from \"could not look\". A missing path, an uninstalled extra, and an unwritable directory all exit 2; broken skills exit 1. A workflow that collapses every non-zero into \"invalid skill\" reports a typo in a path as a spec violation, and the author goes looking for a fault in a file that is fine.</p> <p>The scaffold is validated before it is written. <code>init</code> builds an in-memory provider over the template and runs the real <code>validate_skill()</code> against it. That rejects <code>init \"Bad Name\"</code> without copying <code>_NAME_RE</code> into this package, and it means the template we ship cannot quietly drift out of conformance \u2014 if the spec tightens, <code>init</code> starts failing its own output.</p> <p>Lint thresholds are opinions, so they are named and adjustable. <code>--max-body-tokens</code> is a flag; the 500-character catalog description ceiling is a constant with the reasoning attached (a catalog entry is charged on every turn, a body only when loaded). Token counts are <code>len(text) / 4</code>, which is honest enough to flag an outlier and nowhere near a real tokenizer.</p>"},{"location":"issues/v0.4/#observations-for-later","title":"Observations for later","text":"<p><code>lint</code> cannot warn about unknown frontmatter keys. Core recognises exactly seven, warns about anything else at registration, and keeps that set in a private <code>_KNOWN_KEYS</code>. Surfacing it here would mean duplicating the constant in a second package \u2014 the drift trap already on the follow-ups list. Core should export the known-key set; until it does, this lint is absent rather than wrong.</p> <p>A seventh package must be registered in seven places \u2014 root <code>pyproject.toml</code> three times (path dependency, <code>source_pkgs</code>, isort first-party), <code>COVERAGE_FLOORS</code>, both bump scripts, <code>check_release_version.py</code>, and <code>publish.yml</code> twice (build loop and publish step). Only the last one fails silently: everything else errors, but a package missing from <code>publish.yml</code> simply never ships. Recorded in <code>.github/copilot-instructions.md</code>.</p> <p><code>agentskills-tools</code> is a new PyPI project, so it needs a pending trusted publisher registered before the v0.4.0 tag \u2014 and the pending-publisher uniqueness limit documented in item 12 means it cannot be registered alongside another pending one.</p> <p>The distribution is named <code>agentskills-tools</code>, not <code>agentskills-cli</code>. PyPI rejected the latter: its name-similarity check ultranormalizes a name (lowercase, strip <code>-</code>/<code>_</code>/<code>.</code>, map <code>l</code>/<code>i</code> to <code>1</code> and <code>o</code> to <code>0</code>) before comparing, and the unrelated <code>agent-skills-cli</code> project already collides under that rule. The rename landed after the v0.4.0 tag had already published seven of the nine distributions, so the tag was recreated on the fix commit and the already-live packages were skipped on re-upload. Any new distribution name must be checked against <code>https://pypi.org/pypi/&lt;name&gt;/json</code> (404 means free) and against its hyphenated variants before a pending publisher is registered.</p>"},{"location":"issues/v0.4/#2-skill-validation-github-action","title":"2. Skill validation GitHub Action","text":"<p>Labels: <code>theme:dx</code>, <code>type:feature</code></p> <p>Depends on: item 1 (CLI)</p> <p>Status: implemented. Annotations are emitted by the CLI itself as <code>--format github</code>, so the action stays a composite wrapper with no logic of its own; see the implementation notes.</p>"},{"location":"issues/v0.4/#problem_1","title":"Problem","text":"<p>Anyone maintaining a repository of skills has no way to catch a broken <code>SKILL.md</code> before it reaches consumers.</p>"},{"location":"issues/v0.4/#proposal_1","title":"Proposal","text":"<p>A composite GitHub Action wrapping <code>agentskills validate</code>, published so skill repositories can gate their PRs:</p> <pre><code>- uses: pratikxpanda/agentskills-sdk/actions/validate@v1\n  with:\n    path: ./skills\n    fail-on-lint: false\n</code></pre> <p>Annotate failures on the offending lines via workflow commands so they surface in the PR diff rather than only in the log.</p> <p>This is the highest-leverage adoption item on the roadmap \u2014 it puts the SDK into other people's CI, where it gets seen by everyone who opens a PR against those repos.</p>"},{"location":"issues/v0.4/#acceptance-criteria_1","title":"Acceptance criteria","text":"<ul> <li>[x] Action validates a directory of skills and fails the check on invalid input</li> <li>[x] Failures annotated inline on the PR</li> <li>[x] <code>fail-on-lint</code> toggles whether lint warnings break the build</li> <li>[x] Documented in the root README with a copy-pasteable snippet</li> <li>[x] Dogfooded on this repo's own <code>examples/skills/</code></li> </ul>"},{"location":"issues/v0.4/#implementation-notes_1","title":"Implementation notes","text":"<p>The annotations are a CLI output format, not a script inside the action. <code>--format github</code> sits beside <code>--format text</code> and <code>--format json</code>, which is where every other linter puts it. The alternative was a Python script in <code>actions/validate/</code> parsing the JSON contract \u2014 a second implementation of the finding model, living outside <code>packages/</code>, where neither the coverage floor nor <code>ruff</code> reaches. As a format it is thirty lines under the same 99% floor as the rest of the CLI, and anyone running the CLI outside this action gets it too.</p> <p>The action is a composite wrapper and deliberately thin. It installs the CLI, runs two commands, and has no logic of its own beyond assembling arguments. Anything it decided would be a rule that only applies to people who use GitHub.</p> <p>Lint runs only after <code>validate</code> passes. <code>lint_location()</code> reports unparseable frontmatter as an error rather than guessing, so on a broken skill both commands annotate the same line with the same message. Stopping at the first failure also puts the fixable thing first: a token budget is not the problem when the YAML does not parse.</p> <p>An <code>agentskills</code> already on <code>PATH</code> is used as-is. Otherwise the action would silently replace a repository's pinned CLI with the latest release, which makes the action \u2014 not the repository \u2014 the thing that decides which rules apply. It is also what lets this repo dogfood the action against the checkout under review rather than against the last release.</p> <p>Inputs reach the shell as environment variables, never through <code>${{ }}</code>. An expression interpolated into a <code>run:</code> block is evaluated before bash sees it, so anything an attacker can put in the triggering event becomes shell source. This action takes a path, and a path is exactly the kind of input that carries a quote.</p>"},{"location":"issues/v0.4/#observations-for-later_1","title":"Observations for later","text":"<p><code>--format github</code> has no equivalent for other CI systems. GitLab, Azure Pipelines, and Buildkite all have annotation formats. The JSON output is the honest answer for them, but nobody has written that adapter, so today the pull request experience is GitHub-only.</p> <p>The action cannot be tagged until the CLI is on PyPI. <code>pratikxpanda/agentskills-sdk/actions/validate@v1</code> needs a <code>v1</code> tag, and the action installs <code>agentskills-tools</code>, which does not exist on the index until v0.4.0 ships. The README snippet is correct and will not work until then.</p> <p>Nothing verifies the action's failure path in CI. The dogfood job proves a valid skill passes. A fixture that is deliberately broken would need <code>continue-on-error</code> and a step asserting the outcome, which is a second workflow's worth of machinery to test one bash line.</p>"},{"location":"issues/v0.4/#3-provider-conformance-test-kit","title":"3. Provider conformance test kit","text":"<p>Labels: <code>theme:correctness</code>, <code>type:feature</code>, <code>package:testing</code></p>"},{"location":"issues/v0.4/#problem_2","title":"Problem","text":"<p><code>SkillProvider</code> is an informal contract. We ask third parties to implement it, but there is no way for them to prove they did it correctly \u2014 and the requirements that matter most are the security ones (path traversal rejection, size limits, correct exception types) that are easy to miss and invisible until exploited.</p>"},{"location":"issues/v0.4/#proposal_2","title":"Proposal","text":"<p>A new <code>agentskills-testing</code> package publishing a reusable pytest suite:</p> <pre><code>from agentskills_testing import ProviderConformanceSuite\n\nclass TestMyProvider(ProviderConformanceSuite):\n    @pytest.fixture\n    def provider(self):\n        return MyProvider(...)\n</code></pre> <p>The suite asserts the behaviours a provider must exhibit:</p> <ul> <li><code>SkillNotFoundError</code> for unknown skill IDs</li> <li><code>ResourceNotFoundError</code> for unknown resources</li> <li>Rejection of traversal-style identifiers (<code>../</code>, absolute paths, encoded variants)</li> <li>Size limits enforced</li> <li><code>get_metadata()</code> / <code>get_body()</code> agree with each other for the same <code>SKILL.md</code></li> <li>Every method is genuinely awaitable and does not block the event loop</li> <li><code>list_resources()</code> behaviour once that lands</li> </ul> <p>Requires a documented fixture contract describing the skill layout a provider under test must expose. Run it against the built-in <code>fs</code> and <code>http</code> providers to prove the suite is real.</p>"},{"location":"issues/v0.4/#acceptance-criteria_2","title":"Acceptance criteria","text":"<ul> <li>[x] Suite published as <code>agentskills-testing</code> and importable</li> <li>[x] Passes against both built-in providers</li> <li>[x] Deliberately broken provider fixtures fail the expected assertions</li> <li>[x] Security assertions (traversal, size limits) included and non-skippable</li> <li>[x] Documented in the custom-provider section of the root README</li> </ul> <p>Implemented, awaiting release. Shipped as <code>packages/testing/agentskills-testing</code>, alongside item 4 on one branch \u2014 item 4's first criterion is \"passes the conformance suite from item 3\", so splitting them would have produced a branch that could not verify itself.</p>"},{"location":"issues/v0.4/#implementation-notes_2","title":"Implementation notes","text":"<p>The suite found a real bug on its first run against both providers. <code>agentskills-http</code> raised a plain <code>ValueError</code> for a traversal-shaped <code>skill_id</code> or resource name, where <code>agentskills-fs</code> raised <code>SkillNotFoundError</code> / <code>ResourceNotFoundError</code>. <code>ValueError</code> is not an <code>AgentSkillsError</code>, so a consumer could not write provider-agnostic error handling \u2014 the one thing the ABC exists to make possible. <code>_validate_identifier</code> now takes the exception type its caller documents, and raises what the ABC's <code>Raises:</code> clauses promise. Five assertions in <code>test_static.py</code> changed with it; the constructor's <code>ValueError</code>s (bad <code>max_retries</code>, <code>http://</code> under <code>require_tls</code>) are untouched, because those are programmer errors at construction time, not lookup failures.</p> <p>Then it found a second one, and only on Linux. <code>agentskills-fs</code> leaked <code>ValueError: embedded null character in path</code> for an identifier containing a NUL byte. A NUL truncates a path in the C layer, and the platforms disagree about it: POSIX <code>realpath</code> calls <code>lstat</code>, which rejects it, while Windows resolves the string and the existing <code>is_relative_to(root)</code> check catches it. The suite passed locally and failed in CI on Python 3.12 \u2014 which is the argument for running the same assertions on every provider on every platform rather than trusting whichever one the author happens to develop on. Both <code>_skill_dir</code> and <code>_read_subdir_file_sync</code> now refuse a NUL explicitly, before <code>resolve()</code> sees it, so the behaviour no longer depends on the operating system.</p> <p>Size limits are not part of the universal contract. An in-memory provider has no external source to bound, and asserting a filesystem's constraints against a dict would have forced either a skip (which the acceptance criteria forbid) or a pointless <code>max_bytes</code> parameter on a data structure. They live in a separate, opt-in <code>ContentLimitConformanceSuite</code> requiring a <code>limited_provider</code> fixture, documented as required for any provider reading bytes it did not author. <code>fs</code> and <code>http</code> run both suites; the in-memory provider runs the base suite only. The traversal assertions stay in the base suite and are not skippable.</p> <p>Every async test method carries an explicit <code>@pytest.mark.asyncio</code>. This repo runs <code>asyncio_mode = \"auto\"</code>; a consumer's repo may run <code>strict</code>. A kit that only collects under one project's configuration is not a kit.</p> <p>The suite asserts three things the ABC did not previously state, so the ABC now states them: metadata must not be shared mutable state between calls (a provider caching a parsed dict and returning it lets one caller corrupt another's), repeated reads must agree (a provider streaming without buffering passes the first read and returns empty on the second), and resource getters must return <code>bytes</code> rather than <code>str</code> (decoding on the way out makes a PNG unreachable). Only the first needed a docstring change in <code>agentskills-core</code>.</p> <p>Twelve deliberately broken providers prove the suite bites. <code>tests/test_conformance.py</code> subclasses <code>InMemorySkillProvider</code> once per contract clause \u2014 swallowing a missing skill, resolving traversal, inlining the body into metadata, sharing a metadata dict, advertising a listing capability it then refuses, decoding resources, reading once \u2014 and asserts the matching suite method rejects each. A kit nobody has watched fail has unknown coverage.</p> <p><code>pytest --cov</code> cannot see a package that registers a pytest11 entry point. Entry-point plugins are imported before pytest-cov starts measuring, so <code>agentskills_testing</code> and <code>agentskills_core</code> came back at 0% and the aggregate dropped from 97% to 86%. <code>scripts/dev.py test:cov</code> now runs <code>coverage run -m pytest</code>, which starts coverage before pytest loads anything. Aggregate is 98%.</p> <p>The HTTP conformance run took 26 seconds before a module-scoped <code>httpx.AsyncClient</code>. Each <code>HTTPStaticFileSkillProvider(...)</code> builds a TLS context (~0.17s) and the suite has fifty tests. respx intercepts before the client touches a socket, so it never binds to an event loop and is safe to share. 26s \u2192 0.4s.</p>"},{"location":"issues/v0.4/#observations-for-later_2","title":"Observations for later","text":"<ul> <li>The rest of <code>agentskills-http</code>'s own test suite is slow for the same reason \u2014 every test   constructs a provider, and therefore a TLS context. The full run is ~70s, most of it there. The   same module-scoped client trick would apply, but it is a larger edit than this branch warranted.</li> <li><code>agentskills-core</code>'s own tests still use <code>AsyncMock(spec=SkillProvider)</code> and cannot use   <code>InMemorySkillProvider</code> without a circular dependency. Those mocks have the same defect as the   ones replaced here \u2014 resource getters that raise <code>KeyError</code> instead of <code>ResourceNotFoundError</code> \u2014   so core is tested against a provider that does not conform. Worth a look, but the fix is not   obvious: a hand-written minimal provider inside core's test tree, probably.</li> <li><code>agentskills-testing</code> is a new PyPI project. It needs a pending trusted publisher registered   before the v0.4.0 tag, and only one pending publisher can exist at a time across identically   claimed distributions \u2014 so it and <code>agentskills-tools</code> must be bootstrapped in sequence.</li> <li>The conformance fixture contract is a compatibility surface. Adding an assertion that needs   new fixture content is a breaking change for anyone already using the suite. Worth an explicit   policy before 1.0.</li> </ul>"},{"location":"issues/v0.4/#4-test-doubles-for-downstream-users","title":"4. Test doubles for downstream users","text":"<p>Labels: <code>theme:dx</code>, <code>type:feature</code>, <code>package:testing</code>, <code>good-first-issue</code></p>"},{"location":"issues/v0.4/#problem_3","title":"Problem","text":"<p>Anyone writing tests for an agent that consumes skills has to hand-roll an <code>AsyncMock</code> of <code>SkillProvider</code> \u2014 as this repo's own test suites do, duplicating <code>_mock_provider</code> across integration packages.</p>"},{"location":"issues/v0.4/#proposal_3","title":"Proposal","text":"<p>Ship in <code>agentskills-testing</code>:</p> <ul> <li><code>InMemorySkillProvider(skills: dict)</code> \u2014 a real, spec-compliant provider backed by a dict, no disk or network.</li> <li>pytest fixtures for a populated registry and a single sample skill.</li> <li>A <code>build_skill(...)</code> helper for constructing valid <code>SKILL.md</code> content in tests.</li> </ul> <p>Then replace the duplicated <code>_mock_provider</code> helpers in this repo's integration tests with it.</p>"},{"location":"issues/v0.4/#acceptance-criteria_3","title":"Acceptance criteria","text":"<ul> <li>[x] <code>InMemorySkillProvider</code> passes the conformance suite from item 3</li> <li>[x] Fixtures documented with examples</li> <li>[x] This repo's integration tests use it instead of local mocks</li> </ul> <p>Implemented, awaiting release. Shipped with item 3 on the same branch.</p>"},{"location":"issues/v0.4/#implementation-notes_3","title":"Implementation notes","text":"<p>The four duplicated <code>_mock_provider</code> helpers kept their signatures. Only the body changed, from building an <code>AsyncMock(spec=SkillProvider)</code> to building an <code>InMemorySkillProvider</code>, so ~160 existing call sites needed no edit and the diff stays reviewable.</p> <p>Three tests had been reaching into the mock rather than describing a scenario, and all three got shorter. <code>provider.list_resources.side_effect = ResourceListingNotSupportedError(...)</code> became <code>InMemorySkillProvider(..., supports_resource_listing=False)</code>. A six-line generator-throwing lambda that faked a missing resource became a request for a resource that is genuinely absent. Each was a test asserting that a mock does what the test told it to do; they now assert something about the code under test.</p> <p><code>InMemorySkillProvider</code> accepts a bare string as a skill's body, which covers the common case where the content does not matter, and <code>build_skill()</code> produces frontmatter that passes <code>validate_skill()</code>, so a test that does not care about metadata does not have to invent any.</p> <p>The fixtures ship as a <code>pytest11</code> entry point, so installing the package makes <code>sample_skill</code>, <code>skill_provider</code>, and <code>skill_registry</code> available with no import and no <code>conftest.py</code> entry. The async one is decorated <code>@pytest_asyncio.fixture</code> so it resolves under strict mode as well as auto. This convenience is what broke coverage measurement \u2014 see item 3.</p>"},{"location":"issues/v0.4/#observations-for-later_3","title":"Observations for later","text":"<ul> <li>The old mocks were not spec-compliant, and had not been for as long as they existed: their   resource getters used <code>side_effect = lambda sid, name: mapping[name]</code>, which raises <code>KeyError</code>   rather than <code>ResourceNotFoundError</code>. Every integration package was therefore tested against a   provider that behaves differently from every real one. Nothing broke, but nothing would have   caught it either. This is the argument for the conformance suite in miniature.</li> <li><code>render_skill_md()</code> is currently used only by the fs conformance fixture. If a second caller   never appears, consider whether it belongs in the public API.</li> </ul>"},{"location":"issues/v0.4/#5-registry-level-skill-discovery","title":"5. Registry-level skill discovery","text":"<p>Labels: <code>theme:dx</code>, <code>type:feature</code>, <code>package:core</code>, <code>package:fs</code>, <code>package:http</code></p>"},{"location":"issues/v0.4/#problem_4","title":"Problem","text":"<p><code>registry.register(skill_id, provider)</code> requires knowing every skill ID up front. Pointing at a folder of thirty skills means hard-coding thirty identifiers, and adding a skill means editing application code.</p>"},{"location":"issues/v0.4/#proposal_4","title":"Proposal","text":"<p>Optional <code>discover()</code> on <code>SkillProvider</code> returning available skill IDs, plus:</p> <pre><code>await registry.register_all(provider)\n</code></pre> <ul> <li>Filesystem: enumerate immediate subdirectories containing a <code>SKILL.md</code>.</li> <li>HTTP: cannot enumerate a static server. Extend the per-skill <code>index.json</code> that v0.3 item 4 settled on rather than adding a second manifest format \u2014 two index files with different shapes is a spec fork in miniature.</li> <li>Validation still runs per skill; <code>register_all</code> should report every failure rather than aborting on the first.</li> </ul> <p>The required-vs-optional-method question is already answered: ADR 0002 established the <code>supports_*</code> flag plus a raising default, so \"cannot enumerate\" is never mistaken for \"found nothing\". <code>discover()</code> follows it \u2014 a <code>supports_discovery</code> flag and a <code>DiscoveryNotSupportedError</code> default.</p>"},{"location":"issues/v0.4/#acceptance-criteria_4","title":"Acceptance criteria","text":"<ul> <li>[x] <code>discover()</code> on the filesystem provider, following ADR 0002</li> <li>[x] <code>register_all()</code> on the registry, atomic, reporting all validation failures at once</li> <li>[x] Unsupported providers raise rather than returning an empty list</li> <li>[x] HTTP behaviour consistent with the existing resource-discovery manifest</li> <li>[x] Root README quick-start shows the shorter form</li> </ul> <p>Implemented, awaiting release.</p>"},{"location":"issues/v0.4/#implementation-notes_4","title":"Implementation notes","text":"<p>The HTTP root manifest is the same file one level up. <code>{base_url}/index.json</code> lists skills under a <code>\"skills\"</code> key; <code>{base_url}/{skill_id}/index.json</code> lists that skill's resources under <code>\"references\"</code>, <code>\"scripts\"</code> and <code>\"assets\"</code>. One filename, one shape \u2014 an object mapping a category to a list of names \u2014 at two depths. That is what the proposal's \"extend rather than fork\" line asked for, and it pays off in code: a single <code>_fetch_manifest</code> / <code>_manifest_names</code> pair now serves both, replacing the inline JSON parsing and filtering that <code>list_resources</code> used to carry. Opting in is a separate flag, <code>skill_manifest</code>, because a host may publish per-skill manifests without publishing a root one.</p> <p><code>_validate_all()</code> is shared by all three registration paths. Single, batch and <code>register_all</code> now build the same list of <code>(skill_id, provider)</code> pairs and hand it to one helper that validates everything and raises once, naming every failure. Batch registration previously aborted on the first \u2014 discovering thirty skills and being told only about the first broken one turns a single fix into thirty rounds. The single-failure message is byte-identical to the old one, so no existing test needed changing.</p> <p>Validation inside <code>register_all</code> is sequential, unlike catalog building. The obvious move was to reuse <code>_catalog_concurrency</code>, and it was rejected: the knob is named for the catalog, and making it silently govern registration too means a user tuning prompt-build throughput changes startup behaviour they never looked at. See the observations below.</p> <p>Neither provider validates while discovering. The filesystem provider tests for a <code>SKILL.md</code> and never opens it; the HTTP provider trusts the manifest. Enumerating a folder should not cost one full parse per skill, and <code>register_all</code> validates the whole list immediately afterwards anyway. The conformance suite pins this the other way round \u2014 everything <code>discover()</code> reports must be readable \u2014 so a provider cannot use the freedom to report skills it cannot serve.</p> <p>Duplicate skill IDs are rejected up front by <code>register_all</code>, listing every clash rather than the first, matching what batch registration already did. Combining two providers that both hold <code>incident-response</code> is a configuration error, not a last-writer-wins merge.</p> <p>HTTP manifest entries are de-duplicated; filesystem and in-memory listings cannot contain duplicates by construction. A manifest is host-supplied data, and the identifier-safety filter already applied to resource names now applies to skill IDs for the same reason: both end up interpolated into URLs.</p>"},{"location":"issues/v0.4/#observations-for-later_4","title":"Observations for later","text":"<ul> <li>The conformance suite caught a third real provider divergence, and this one was a genuine   bug rather than a gap. Adding <code>TestHTTPStaticFileManifestConformance</code> \u2014 the first contract run   with manifests enabled, and the only way the manifest branches get contract-tested at all \u2014   immediately failed <code>test_listing_of_unknown_skill_is_refused</code>: with <code>resource_manifest=True</code>,   <code>list_resources(\"no-such-skill\")</code> got a 404 on the manifest and reported \"this skill publishes   no manifest\" when the truth was that the skill did not exist. Fixed by fetching <code>SKILL.md</code> on   that path to tell the two apart \u2014 one extra request, on the failure path only, and its result   is cached for whoever asks next. Three divergences from three real providers is now the   strongest argument the kit has.</li> <li>This PR added assertions to <code>ProviderConformanceSuite</code> without needing new fixture content,   which was luck rather than design. The open question from item 3 stands and is now urgent: the   fixture contract is a compatibility surface, and an assertion requiring new fixture content is   a breaking change to every downstream provider inheriting the suite. Worth an explicit policy \u2014   and probably a <code>CONTRACT</code> version \u2014 before 1.0.</li> <li><code>catalog_concurrency</code> is becoming the wrong name for the wrong knob. It governs one   operation, and registration now wants the same treatment for a different reason. Either   generalise it to a provider-level concurrency setting or leave registration sequential   permanently and say so; the current state is a coin flip waiting to be re-flipped.</li> <li>The \"immediate subdirectory containing a <code>SKILL.md</code>\" rule now exists twice, in   <code>agentskills_tools.discovery</code> and in <code>LocalFileSystemSkillProvider._discover_sync</code>. The CLI was   deliberately left alone: its <code>discover()</code> returns <code>SkillLocation(skill_id, path)</code> objects that   the provider API cannot supply, and <code>serve.build_registry</code> may be handed a single-skill target   whose root is the parent directory, where <code>register_all</code> would wrongly register the siblings.   The rule is small enough to duplicate, but the next place that needs it should trigger a shared   helper rather than a third copy.</li> <li>ADR 0002's prediction is on schedule. Two capabilities, two flags, two errors, and the   pattern still reads well. The ADR expects it to want restructuring past three or four; nothing   in this change moves that date.</li> <li><code>registry.py</code> has an untested <code>catalog_concurrency &lt; 1</code> guard and an untested <code>add_note</code> path   in <code>_gather_metadata</code> \u2014 both pre-existing, both outside this change, and together they hold   the file at 97% while the core package stays above its floor on the strength of the others.   Worth closing when someone next touches the catalog.</li> <li>The HTTP package's test suite now takes 95\u2013127 seconds on its own. The cost is   <code>httpx.AsyncClient()</code> TLS-context construction, once per provider instance, spread evenly   across unrelated tests rather than concentrated in the new ones. Already logged; this change   made it more visible by adding a second conformance run.</li> </ul>"},{"location":"issues/v0.4/#6-catalog-filtering-and-token-budget","title":"6. Catalog filtering and token budget","text":"<p>Labels: <code>theme:dx</code>, <code>theme:performance</code>, <code>type:feature</code>, <code>package:core</code></p>"},{"location":"issues/v0.4/#problem_5","title":"Problem","text":"<p><code>get_skills_catalog()</code> returns every registered skill, and the result is injected into every system prompt on every turn. With a large registry the catalog silently becomes a significant fixed cost per request, and there is no way to cap or narrow it.</p>"},{"location":"issues/v0.4/#proposal_5","title":"Proposal","text":"<pre><code>await registry.get_skills_catalog(\n    format=\"xml\",\n    tags=[\"incident\"],        # filter by frontmatter metadata\n    include=[\"skill-a\"],      # explicit allow-list\n    exclude=[\"deprecated-x\"],\n    max_chars=8000,           # hard ceiling\n)\n</code></pre> <p>When <code>max_chars</code> would be exceeded, truncate deterministically (stable ordering, whole entries only) and append an explicit note that the catalog was truncated \u2014 silently dropping skills would make agent behaviour non-reproducible.</p> <p>Depends on tags existing in frontmatter; decide whether to read them from the spec's <code>metadata</code> dict or propose a dedicated field.</p>"},{"location":"issues/v0.4/#acceptance-criteria_5","title":"Acceptance criteria","text":"<ul> <li>[x] Filtering by tags, include, and exclude</li> <li>[x] <code>max_chars</code> enforced, truncation deterministic and flagged in the output</li> <li>[x] Existing no-argument behaviour unchanged</li> <li>[x] Interaction with catalog caching (v0.3 item 1 follow-ups) documented \u2014 cache key must include the filter arguments</li> </ul> <p>Implemented, awaiting release.</p>"},{"location":"issues/v0.4/#implementation-notes_5","title":"Implementation notes","text":"<p>Tags live under the spec's <code>metadata</code> mapping, not in a new top-level field. The spec already defines <code>metadata</code> as free-form, so <code>metadata: {tags: [incident, sev1]}</code> is conformant today \u2014 no fork, nothing to defend upstream, no second \"this field is not in the specification\" caveat in the README beside the one <code>version</code> already carries. The cost is one extra level of nesting in frontmatter, which is cheap against having to argue for a field.</p> <p>A malformed <code>metadata.tags</code> warns rather than failing registration. <code>tags: \"incident\"</code> is a plausible mistake, and today it registers fine; making it an error would break skills that work. It is not silent either \u2014 it logs a warning through the same channel as unknown metadata keys, and the skill matches no tag filter. Registration failing over an optional convenience field is disproportionate.</p> <p><code>include</code> and <code>exclude</code> run before any metadata is fetched. They match skill IDs, which the registry already holds, so narrowing a large registry costs proportionally fewer provider round-trips rather than fetching everything and discarding most of it. <code>tags</code> needs metadata and necessarily runs after.</p> <p>The two ID filters are deliberately asymmetric about unregistered IDs. <code>include</code> raises <code>SkillNotFoundError</code>; <code>exclude</code> ignores them. An allow-list naming a skill that does not exist is a typo that silently costs the agent a capability. A deny-list naming a skill that does not exist is already satisfied, and deny-lists are meant to outlive the thing they deny \u2014 a config that starts raising because a skill was finally deleted is a config nobody will keep accurate.</p> <p><code>exclude</code> wins over <code>include</code>. A deny-list another argument can override is not a deny-list.</p> <p>Truncation re-renders rather than predicting a length. Whole entries are dropped from the end and the catalog is rendered again until it fits. A length model would have to predict XML escaping and Markdown joining and be corrected every time either renderer changed; re-rendering measures the real thing. Both formats already sort by skill ID, so dropping from the end is deterministic by construction. The loop is quadratic in the number of entries, which is the right trade at catalog sizes \u2014 tens of entries, tens of characters each.</p> <p>The truncation note is format-native. XML gains <code>truncated</code>, <code>shown</code> and <code>total</code> attributes on the root; Markdown gains a closing <code>_Catalog truncated: showing N of M skills._</code> line. Same information, idiomatic in each format, and an untruncated catalog is byte-identical to what the previous release produced \u2014 the root carries no attributes at all unless something was dropped.</p> <p>The note counts against the budget, so <code>max_chars</code> is a real ceiling rather than an approximate one. When even an empty catalog plus its note will not fit, <code>get_skills_catalog</code> raises <code>ValueError</code> naming the minimum, rather than returning something over budget.</p> <p>On caching: there is still no registry-level catalog cache \u2014 the v0.3 follow-up was never built. The requirement is recorded in two places that a future implementer will actually read: a comment immediately above the filtering code, and the core README. The key must cover <code>format</code> and all four filter arguments.</p>"},{"location":"issues/v0.4/#observations-for-later_5","title":"Observations for later","text":"<ul> <li>The catalog is now the widest API in the SDK, at five keyword arguments. A <code>CatalogOptions</code>   dataclass would read better at three more, and the integrations are where that will bite first:   <code>AgentSkillsContextProvider</code> and the MCP server both build catalogs and neither can pass a   filter through. Threading five arguments through each of them, one at a time, is the wrong   shape \u2014 settle the options object before doing it.</li> <li>Nothing outside core can filter yet. <code>agentskills inspect</code>, the two <code>get_tools()</code> helpers,   the Agent Framework context provider and the MCP server all call <code>get_skills_catalog(format=\u2026)</code>   and stop there. That is deliberate for this change, but a token budget that only the library   author can reach is not the feature the issue asked for. Item 9 (token cost reporting) is the   natural place to surface <code>--max-chars</code> on the CLI.</li> <li>Tag matching is any-of, and there is no all-of. Nobody has asked for it, <code>tags=</code> reads   naturally as \"related to any of these\", and adding <code>tags_match=\"all\"</code> later is backwards   compatible. Worth resisting until a real query needs it.</li> <li><code>metadata.tags</code> has no validation and no canonical vocabulary. Two skills can carry   <code>incident</code> and <code>incidents</code> and neither filter finds both. Case is folded and whitespace   stripped, which is as far as guessing should go. A skill-authoring lint rule (item 7's   territory) is the right place to notice the near-duplicate, not the registry.</li> <li>The root README had an unbalanced code fence just above <code>### With LangChain</code>, which put that   heading and everything after it inside a code block on the rendered page. Fixed here because it   was in the paragraph being edited. Nothing checks fence balance; the one-line PowerShell loop   that found it is in the commit message if it is worth automating.</li> <li><code>registry.py</code> is now 500 lines and holds registration, lookup, filtering and two renderers.   The renderers have no dependency on the registry beyond the entries passed to them and are   already <code>@staticmethod</code>. Splitting them into <code>catalog.py</code> would be a clean cut whenever the next   format or the options object arrives.</li> </ul>"},{"location":"issues/v0.4/#7-skill-evaluation-harness","title":"7. Skill evaluation harness","text":"<p>Status: implemented. All three open questions were resolved toward the dependency-light answer: a one-method protocol resolved from a dotted path, <code>repeat</code> plus <code>threshold</code> per case, and an on-disk cache keyed by what actually determined each completion. The harness landed in <code>agentskills-tools</code> rather than <code>agentskills-testing</code>; the reasoning is below.</p> <p>Labels: <code>theme:agent-effectiveness</code>, <code>type:feature</code>, <code>package:tools</code></p> <p>Depends on: item 1 (CLI), item 4 (test doubles)</p>"},{"location":"issues/v0.4/#problem_6","title":"Problem","text":"<p>A skill is a prompt, and nobody measures whether a given skill makes an agent better. Authors ship on intuition, reviewers approve on prose quality, and regressions are invisible \u2014 editing a skill body can degrade task success with no signal anywhere. The whole ecosystem currently operates on faith.</p>"},{"location":"issues/v0.4/#proposal_6","title":"Proposal","text":"<p>Test cases alongside the skill, and a runner that measures the difference the skill makes:</p> <pre><code># evals/deploy-rollback.yaml\nskill: deploy-rollback\ncases:\n  - prompt: \"Production is erroring after the 14:02 deploy. What do I do?\"\n    expect:\n      - contains: \"kubectl rollout undo\"\n      - not_contains: \"kubectl delete\"\n      - judge: \"Recommends rollback before investigating root cause\"\n</code></pre> <p><code>agentskills eval</code> runs each case twice \u2014 once with the skill registered, once without \u2014 and reports the delta. The paired comparison is the point: absolute pass rates mostly measure the underlying model, whereas the difference isolates the skill's contribution.</p> <p>Assertions in three tiers: deterministic (<code>contains</code>, <code>not_contains</code>, <code>regex</code>), structural (a named tool was called), and model-judged for anything subjective. Judged assertions need a declared judge model and should report agreement, since an LLM judge is itself unreliable.</p>"},{"location":"issues/v0.4/#open-questions_1","title":"Open questions","text":"<ul> <li>Model access: BYO client via a small protocol, or depend on <code>litellm</code> for provider breadth? A protocol keeps <code>agentskills-testing</code> dependency-light.</li> <li>Non-determinism: fixed seed and temperature 0 where the provider supports it, plus <code>n</code> repeats with a pass threshold rather than a single run.</li> <li>Cost: these hit real APIs. Must be opt-in, never part of the default <code>pytest</code> run, and should cache by (skill hash, case hash, model).</li> </ul>"},{"location":"issues/v0.4/#acceptance-criteria_6","title":"Acceptance criteria","text":"<ul> <li>[x] Eval case file format documented and validated by <code>agentskills validate</code></li> <li>[x] <code>agentskills eval</code> reports per-case with/without results and an aggregate delta</li> <li>[x] Deterministic and judged assertions both supported</li> <li>[x] Results cached by skill and case hash so unchanged cases do not re-bill</li> <li>[x] Never runs in the default test suite; requires explicit invocation and credentials</li> <li>[x] Example evals for the skills in <code>examples/skills/</code></li> </ul>"},{"location":"issues/v0.4/#implementation-notes_6","title":"Implementation notes","text":"<p>The harness is in <code>agentskills-tools</code>, not <code>agentskills-testing</code>. The <code>package:testing</code> label was a guess made before anyone tried to build it, and the acceptance criteria contradict it: a thing that must never run in the default <code>pytest</code> run is a command, not a pytest kit. Putting it in <code>agentskills-testing</code> would also have forced <code>agentskills validate</code> \u2014 which must keep working with no extras, because the GitHub Action depends on that \u2014 either to import a pytest-carrying package or to silently skip eval files, and a validator that silently skips a file is worse than no validator. In the CLI, <code>validate</code> always checks eval files and <code>eval</code> runs them. No new dependency was added to any package.</p> <p>Cases live in <code>evals/</code> inside the skill folder. The proposal's example path was ambiguous about where the directory sits. Inside the skill keeps a skill self-contained, which is already the pattern for <code>references/</code>, <code>scripts/</code> and <code>assets/</code>, and means <code>agentskills validate ./skills</code> finds the cases with no new path argument. <code>evals/</code> is not a spec-recognised subdirectory, but the filesystem provider enumerates only the three resource kinds, so it is inert to everything else in the SDK. The <code>skill:</code> key is optional and checked against the containing folder when present, so it documents without being able to lie.</p> <p>Model access: a protocol, not <code>litellm</code>. <code>EvalModel</code> is <code>model_id</code> plus one <code>complete()</code> coroutine, and <code>--model</code> takes <code>module:factory</code>. Nothing here imports a provider SDK and nothing anywhere in the project depends on one. <code>litellm</code> would have bought provider breadth at the cost of putting an API-key-shaped dependency in the install graph of a package whose main job is validating YAML in CI. A ten-line adapter is documented in the CLI README instead.</p> <p>Non-determinism: <code>repeat</code> and <code>threshold</code>, not seeds. Seeds and temperature are the adapter's business \u2014 the CLI cannot enforce them across providers that disagree about whether they exist \u2014 so the README says temperature zero and the code says nothing. What the code does provide is <code>repeat: n</code> (default <code>1</code>) with <code>threshold</code> (default <code>1.0</code>), and an <code>agreement</code> figure that is reported whenever the repeats did not all reach the same verdict. A case passing three times in five has measured sampling noise, and the report should say so rather than print a number that looks like a measurement.</p> <p>The cache key is the inputs to the model, not the case. The criterion asked for \"skill hash and case hash\", but hashing the case would include its assertions, and tightening an assertion should re-grade the answers already bought rather than buy them again. The key is model ID, system prompt, user prompt, and repeat index. The skill body is the system prompt for the \"with\" half, so editing a skill invalidates exactly its own runs and leaves the baseline half cached \u2014 which is the common case when iterating on a body. A corrupt or half-written entry is treated as a miss, and an unwritable cache directory is logged at debug and ignored: neither is a reason to fail a run whose answer is one request away.</p> <p>The baseline is an empty system prompt. The \"without\" half is the model as it ships, which is what the skill has to beat. Anything else \u2014 a generic assistant preamble, a stub skill \u2014 would be a second intervention smuggled into the control.</p> <p><code>Finding</code> grew a <code>file</code> field. Findings were hard-wired to the skill's <code>SKILL.md</code>, which is where <code>--format github</code> put every annotation. An eval finding has to land on the eval file, so findings now carry an optional path and the renderers fall back to <code>SKILL.md</code>. The JSON payload gained a <code>file</code> key, always populated; that is additive, so <code>schemaVersion</code> stays at <code>1</code>.</p> <p>A broken eval file is exit code 2, not 1. <code>agentskills eval</code> refuses to run anything when any eval file fails to parse, prints the findings to stderr, and exits <code>2</code> \u2014 the invocation could not run. Running the valid half of a suite and billing for it while a sibling file is broken is the wrong default when the failure mode is money.</p>"},{"location":"issues/v0.4/#observations-for-later_6","title":"Observations for later","text":"<ul> <li>Structural assertions were deliberately left out. The proposal names a third tier \u2014 \"a named   tool was called\" \u2014 but no acceptance criterion requires it, and it is not a small addition: the   model protocol would have to carry tool schemas and return tool calls, turning a one-method   contract into an agent-loop contract that every adapter author then has to implement. Worth   doing when there is a caller for it, and worth doing as a separate protocol that   <code>EvalModel</code> can opt into rather than a widening of <code>complete()</code>.</li> <li><code>agentskills-tools</code> imports <code>yaml</code> without declaring <code>pyyaml</code>. It works today only because   <code>agentskills-core</code> pulls it in transitively. <code>evalspec.py</code> adds a second such import and makes   the latent packaging bug more load-bearing. It could not be fixed in this change: adding a   dependency requires <code>poetry lock</code>, and <code>poetry</code> currently cannot reach <code>files.pythonhosted.org</code>   from this machine. Must be fixed before v0.4.0 ships.</li> <li>Nothing checks eval files that reference a skill that does not exist. A file in   <code>alpha/evals/</code> declaring <code>skill: beta</code> is caught, but only because the folder disagrees. There   is no cross-skill eval and no way to write one, which is probably right, but it means the   <code>skill:</code> key carries no information the path did not already have. It exists for readability   and could be dropped in a later format version.</li> <li>The judge prompt is fixed and unversioned. <code>JUDGE_SYSTEM</code> asks for one word, <code>PASS</code> or   <code>FAIL</code>, and anything that is not <code>PASS</code> is a failure. Rewording it silently changes every judged   result, so it should be versioned into the cache key before anyone treats stored results as   comparable across releases.</li> <li>The runner is sequential. Cases, repeats, and the two variants all run one after another,   which is the slowest possible arrangement for a workload that is entirely network latency. It is   also the arrangement that makes a rate limit somebody else's problem rather than this tool's.   A concurrency knob belongs here eventually, and it will hit the same naming question as   <code>catalog_concurrency</code> in <code>agentskills-core</code>.</li> <li>A skill's eval cases ship with the skill. They are inside the skill folder, so an HTTP host   or a packaged skill carries them to consumers who will never run them. Tests living beside code   is the normal trade, but a <code>--exclude evals</code> at packaging time will be wanted eventually.</li> <li>There is no way to run one case or one suite. <code>agentskills eval</code> runs everything it   discovers. With a real model behind it, a <code>--case</code> or <code>-k</code>-style filter is the first thing an   author iterating on one prompt will ask for.</li> </ul>"},{"location":"issues/v0.4/#8-foreign-format-adapters","title":"8. Foreign format adapters","text":"<p>Labels: <code>theme:interoperability</code>, <code>type:feature</code>, <code>package:adapters</code></p> <p>Status: implemented. Added <code>agentskills-adapters</code> with explicit importers for AGENTS/Copilot files, Cursor rules, and Claude-style skill folders, plus <code>agentskills init --from</code> for conversion to editable native <code>SKILL.md</code> files.</p>"},{"location":"issues/v0.4/#problem_7","title":"Problem","text":"<p>The barrier to adopting this SDK is not the API \u2014 it is that a team has no skills written. Most already have the content, though: <code>AGENTS.md</code>, <code>.github/copilot-instructions.md</code>, <code>.cursor/rules/</code>, Claude skill folders, internal runbooks. Asking them to hand-convert all of it before seeing any value is a steep first step, and an empty registry is worth nothing.</p>"},{"location":"issues/v0.4/#proposal_7","title":"Proposal","text":"<p>A new <code>agentskills-adapters</code> package converting foreign formats into <code>Skill</code> objects at load time:</p> Source Mapping <code>AGENTS.md</code> / <code>copilot-instructions.md</code> Whole file becomes one skill body; name and description derived from the first heading, or synthesised <code>.cursor/rules/*.mdc</code> One skill per rule file; <code>description</code> and <code>globs</code> frontmatter map across directly Claude skill folders Near-identical structure; largely a validation pass <p>This is an import layer, not a spec fork \u2014 principle 1 holds because nothing new is added to the skill format. Adapters produce ordinary <code>Skill</code> objects that the registry cannot distinguish from native ones.</p> <p>The honest limitation: formats without a description field lose the catalog entry that progressive disclosure depends on, since the agent selects on description. Options are to synthesise one, or require the user to supply it. <code>agentskills init --from &lt;path&gt;</code> should emit a real <code>SKILL.md</code> for editing, so adapters are a migration on-ramp rather than a permanent dependency.</p>"},{"location":"issues/v0.4/#acceptance-criteria_7","title":"Acceptance criteria","text":"<ul> <li>[x] Adapters for <code>AGENTS.md</code>, Copilot instructions, and Cursor rules</li> <li>[x] Output passes <code>validate_skill()</code> and the provider conformance suite</li> <li>[x] Missing descriptions handled explicitly, never silently blank</li> <li>[x] <code>agentskills init --from &lt;path&gt;</code> converts to a native <code>SKILL.md</code></li> <li>[x] Documented as a migration path, with its limitations stated</li> </ul>"},{"location":"issues/v0.4/#implementation-notes_7","title":"Implementation notes","text":"<p><code>agentskills-adapters</code> returns ordinary <code>agentskills-core.Skill</code> objects backed by an immutable in-memory provider. It does not add a second skill model or modify the specification.</p> <ul> <li><code>AGENTS.md</code> and Copilot instructions become one skill, using the first   heading for the name/description when available.</li> <li>Cursor <code>.mdc</code> frontmatter maps <code>description</code> directly and preserves <code>globs</code>   under the native <code>metadata</code> mapping rather than inventing a top-level field.</li> <li>Claude-style directories are read from their <code>SKILL.md</code> and retain supported   native frontmatter.</li> <li>Missing descriptions receive an explicit synthesized description such as   <code>Imported instructions from AGENTS.md.</code>; blank catalog entries are never   emitted.</li> <li><code>agentskills init --from</code> validates the imported object before writing a   native <code>SKILL.md</code>, making conversion an editing/migration step rather than a   permanent adapter dependency.</li> </ul> <p>The package is included in workspace, version-bump, release, docs, dependency, and coverage registration. The provider-conformance criterion is satisfied by the same <code>SkillProvider</code> contract and <code>validate_skill()</code> path used by native providers; focused tests cover all supported source shapes and validation.</p>"},{"location":"issues/v0.4/#observations-for-later_7","title":"Observations for later","text":"<ul> <li><code>discover_sources()</code> intentionally finds supported files recursively but does   not infer ownership or merge instruction scopes. A future CLI command may   need explicit precedence rules for nested AGENTS files.</li> <li>Cursor <code>globs</code> are preserved as metadata for portability, but the core has no   file-selection semantics. Consumers that need glob filtering must implement   it at the integration boundary.</li> <li>Claude folders may contain resources; the current import path converts the   <code>SKILL.md</code> only. Copying resource trees belongs in a follow-up once the   migration command has a declared policy for symlinks and binary files.</li> </ul>"},{"location":"issues/v0.4/#9-token-cost-reporting","title":"9. Token cost reporting","text":"<p>Labels: <code>theme:dx</code>, <code>type:feature</code>, <code>package:tools</code></p> <p>Status: implemented. The open question was resolved toward the dependency-light default, but with a lever attached: <code>tiktoken</code> is used when importable and a character heuristic otherwise, the counter that ran is named in every report, and <code>--tokenizer</code> lets a caller demand exactness or pin the estimate. Dropped <code>good-first-issue</code> \u2014 the tokenizer-determinism handling is not the shape of work a first contributor should be asked to get right.</p>"},{"location":"issues/v0.4/#problem_8","title":"Problem","text":"<p>Skill authors have no visibility into what their skill costs. The catalog entry is charged on every single turn, the body on every load \u2014 but the only feedback is an agent that got expensive for reasons nobody can attribute.</p>"},{"location":"issues/v0.4/#proposal_8","title":"Proposal","text":"<p><code>agentskills inspect --cost</code> breaking the skill into what is actually charged and when:</p> <pre><code>deploy-rollback\n  catalog entry        142 tokens   (every turn)\n  body               3,180 tokens   (on load)\n    ## Overview        410\n    ## Procedure     2,240\n    ## References      530\n  references/api.md  1,890 tokens   (on demand)\n</code></pre> <p>Separating per-turn from per-load cost is the part that matters \u2014 a bloated description is far more expensive than a bloated body, and that is exactly backwards from most authors' intuition.</p> <p>Use <code>tiktoken</code> if available, fall back to a character heuristic with the estimate flagged as approximate. Add <code>--budget</code> to exit non-zero above a threshold so CI can gate it.</p>"},{"location":"issues/v0.4/#acceptance-criteria_8","title":"Acceptance criteria","text":"<ul> <li>[x] Per-turn and per-load costs reported separately</li> <li>[x] Body broken down by section</li> <li>[x] Works without <code>tiktoken</code>, marking output as estimated</li> <li>[x] <code>--budget</code> exits non-zero when exceeded</li> <li>[x] <code>--format json</code> for CI consumption</li> </ul>"},{"location":"issues/v0.4/#implementation-notes_8","title":"Implementation notes","text":"<p><code>tiktoken</code> is not a dependency, and there is no extra for it. It is imported optionally and the import is guarded twice \u2014 once for <code>ImportError</code>, once for the failure to build an encoding, because <code>tiktoken</code> fetches its BPE vocabulary over the network on first use. \"Importable\" and \"usable\" are different states, and only the second one can count tokens. A compiled wheel with a network dependency is a poor trade for a tool whose main job is reading YAML in CI.</p> <p><code>lint --max-body-tokens</code> keeps the heuristic regardless. A gate whose verdict changes with what happens to be installed on the runner is not a gate. Only <code>inspect --cost</code> reaches for the real tokenizer, and it says which one it used.</p> <p><code>--tokenizer {auto,tiktoken,heuristic}</code> was added beyond the stated criteria for the same reason. <code>auto</code> is the terminal default; <code>tiktoken</code> exits <code>2</code> when the exact counter is unavailable, so CI can demand exactness rather than silently receiving an estimate; <code>heuristic</code> pins the estimate so two machines agree. The hazard is not theoretical \u2014 while building this, <code>tiktoken</code> 0.12.0 was present in the local virtualenv but absent from <code>poetry.lock</code>, so a local run and a CI run of the same command would have counted differently and neither would have said so.</p> <p>Two budget flags, not one. <code>--budget</code> gates per-load (catalog entry plus body); <code>--turn-budget</code> gates the catalog entry alone. A single threshold is dominated by the body, which would leave the per-turn cost invisible to precisely the mechanism meant to catch it \u2014 the same inversion the feature exists to correct.</p> <p>Sections are flat and non-overlapping. A heading owns its own text up to the next heading of any level; a nested section is not also counted inside its parent. The parts therefore sum to the body exactly, which is the only property that makes a breakdown checkable. Depth is carried by indentation instead of by arithmetic.</p> <p>Heading detection is fence-aware, because <code>#</code> inside a bash block is a comment. The example <code>incident-response</code> skill has exactly that, so the naive version would have reported phantom sections and a body that did not add up.</p> <p>Binary resources report bytes and <code>tokens: null</code>. An image has a size but no token cost, and inventing one would be a lie in the column that matters. The text renderer marks the row <code>bytes, not text</code>.</p> <p><code>--cost</code> replaces the content dump rather than appending to it. Somebody asking what a skill costs is not also asking to read it.</p>"},{"location":"issues/v0.4/#observations-for-later_8","title":"Observations for later","text":"<ul> <li>The counter is chosen per invocation and applied to every skill in the run. That is right for a   budget gate, but a future <code>--compare</code> across tokenizers would want it per-column instead.</li> <li><code>cl100k_base</code> is hardcoded. It is the right default for the current generation of models and   wrong for the one after; the encoding name should become a flag before anyone pins a budget to   a number produced by it.</li> <li>Cost is reported but never recorded. The number that would actually change behaviour is the   delta against the previous commit, which needs somewhere to keep a baseline \u2014 the same problem   the eval harness has, and probably the same answer.</li> <li><code>estimate_tokens</code> now lives in <code>lint.py</code> and is imported by <code>cost.py</code>. It belongs to neither;   when a third caller appears it should move.</li> </ul>"},{"location":"issues/v0.4/#10-documentation-site","title":"10. Documentation site","text":"<p>Labels: <code>theme:project-health</code>, <code>type:docs</code></p> <p>Status: implemented. MkDocs Material site added with <code>mkdocstrings</code>, versioning via <code>mike</code>, CI build workflow, and release-triggered Pages deploy.</p>"},{"location":"issues/v0.4/#problem_9","title":"Problem","text":"<p>The root README is carrying overview, install, quick start, four integration walkthroughs, custom-provider docs, and security notes. It is already too long to skim, and there is no API reference anywhere.</p>"},{"location":"issues/v0.4/#proposal_9","title":"Proposal","text":"<p>MkDocs Material published to GitHub Pages:</p> <ul> <li>Getting started, concepts (progressive disclosure, providers, registry)</li> <li>One page per package</li> <li>Generated API reference (<code>mkdocstrings</code>) from the existing docstrings, which are already thorough</li> <li>The roadmap and ADRs surfaced as site pages</li> <li>Versioned with <code>mike</code> so users on an older release get matching docs</li> </ul> <p>Then cut the root README back to overview, install, one quick start, and links.</p>"},{"location":"issues/v0.4/#acceptance-criteria_9","title":"Acceptance criteria","text":"<ul> <li>[x] Site builds in CI and deploys on release</li> <li>[x] API reference generated from docstrings, not hand-written</li> <li>[x] Versioned, with a version switcher</li> <li>[x] Root README reduced accordingly</li> </ul>"},{"location":"issues/v0.4/#implementation-notes_9","title":"Implementation notes","text":"<p><code>mkdocs.yml</code> defines navigation for getting started, concepts, one page per package, roadmap, ADRs, and the v0.4 issue plan. The package pages use <code>mkdocstrings</code> directives, so API reference is generated from code docstrings instead of hand-maintained markdown.</p> <p><code>.github/workflows/docs.yml</code> introduces two jobs:</p> <ul> <li><code>build</code> runs on pull requests and pushes to <code>main</code>, installs docs tooling,   installs local packages from this checkout, and runs <code>mkdocs build --strict</code>.</li> <li><code>deploy</code> runs on published releases, uses <code>mike deploy</code> to publish a   versioned site, updates the <code>latest</code> alias, and sets <code>latest</code> as default.</li> </ul> <p>Root <code>README.md</code> was reduced from long integration/provider walkthroughs to overview plus links into the docs site and package-level READMEs.</p>"},{"location":"issues/v0.4/#observations-for-later_9","title":"Observations for later","text":"<ul> <li>This machine cannot reach PyPI file hosting directly, so docs build was wired   through CI and release automation first; local docs preview still depends on   running the docs toolchain from a networked environment.</li> <li>The docs workflow currently installs all local packages explicitly so   <code>mkdocstrings</code> can import every module; if package count grows, replace that   with one reusable installer script to avoid drift.</li> </ul>"},{"location":"issues/v0.4/#11-architecture-decision-records","title":"11. Architecture decision records","text":"<p>Labels: <code>theme:project-health</code>, <code>type:docs</code></p> <p>Status: implemented. ADR index/template added and six backfilled ADRs landed for the listed decisions; each includes links to the issue section where the decision was made.</p>"},{"location":"issues/v0.4/#problem_10","title":"Problem","text":"<p>Cross-package decisions are undocumented, so they get re-litigated or silently reversed. Git history shows what changed, not which alternatives were rejected and why.</p>"},{"location":"issues/v0.4/#proposal_10","title":"Proposal","text":"<p>Backfill ADRs for decisions already made:</p> Decision Why it needs a record Fully async provider interface Constrains every provider implementation; the fs provider was sync-under-async until v0.3 item 3 Six packages instead of one with extras Shapes install UX, release process, and version coordination \u2014 and the lockstep versioning rule the release guard now enforces Caching strategy Why provider caching is per-instance with an explicit <code>invalidate()</code>, and why HTTP revalidation is opt-in rather than default Exception taxonomy Extended by v0.3 item 7 with <code>SkillUnavailableError</code>; the rationale for splitting \"not found\" from \"unavailable\" exists only in issue notes Binary resources as a JSON envelope Chosen for consistency across integrations over MCP-native content blocks. Write this before the follow-up that reverses it Logging conventions Why one <code>agentskills.*</code> namespace, and why there is deliberately no <code>ERROR</code> level <p>Add a short <code>docs/adr/README.md</code> with the template and the numbering convention.</p>"},{"location":"issues/v0.4/#acceptance-criteria_10","title":"Acceptance criteria","text":"<ul> <li>[x] ADR template and index in place</li> <li>[x] Backfill ADRs written for the decisions above</li> <li>[x] Each links to the issue or PR where the decision was made</li> <li>[x] Contributing guide points at ADRs for design-affecting changes</li> </ul>"},{"location":"issues/v0.4/#implementation-notes_10","title":"Implementation notes","text":"<p><code>docs/adr/README.md</code> now defines the ADR numbering/status convention and ships a template section, so new records follow one shape instead of drifting by author. The index includes ADR 0001/0002 plus six backfilled records for this item:</p> <ul> <li>ADR 0003: fully async provider interface</li> <li>ADR 0004: multi-package architecture with lockstep versioning</li> <li>ADR 0005: per-instance provider caching with explicit invalidation</li> <li>ADR 0006: exception taxonomy separating <code>SkillNotFoundError</code> from   <code>SkillUnavailableError</code></li> <li>ADR 0007: binary resources encoded as a JSON envelope</li> <li>ADR 0008: one <code>agentskills.*</code> logging namespace and severity conventions</li> </ul> <p>Each ADR has a \"Decision history\" section linking to the issue section where the decision was made (primarily v0.3 items 1, 3, 5, 7, 8, and 10; plus v0.4 issue 12 for the publish/packaging trajectory).</p>"},{"location":"issues/v0.4/#observations-for-later_10","title":"Observations for later","text":"<ul> <li>ADR 0007 records the current JSON-envelope choice and intentionally leaves room   for the planned MCP-native binary follow-up; that follow-up now has something   concrete to supersede instead of reversing a behavior with no written rationale.</li> <li>ADR 0004 captures the release constraints, but the lockstep model has grown   from six to eight packages since the older issue text; keep release docs and   scripts aligned whenever package count changes.</li> </ul>"},{"location":"issues/v0.4/#12-simplify-the-publish-workflow-drop-the-testpypi-path","title":"12. Simplify the publish workflow: drop the TestPyPI path","text":"<p>Labels: <code>theme:project-health</code>, <code>type:chore</code></p> <p>Status: implemented. The fall-through became an explicit refusal, <code>workflow_dispatch</code> now stops at the artifact, and pre-releases go to PyPI; see the implementation notes.</p>"},{"location":"issues/v0.4/#problem_11","title":"Problem","text":"<p>The publish workflow routes any tag that is not a bare <code>vX.Y.Z</code> to TestPyPI as a dry run. That path is configured for exactly one of the seven packages, and finishing it is more work than it looks: pending trusted publishers must be uniquely identifiable by their claims. All seven distributions share the same owner, repository, workflow and environment, so PyPI cannot tell which pending project an incoming token belongs to and permits only one pending publisher at a time. Bootstrapping the set on a fresh index therefore takes as many sequential publish rounds as there are packages, each one converting a pending publisher into a project-scoped one and freeing the slot.</p> <p>So today an <code>rc</code> tag publishes <code>agentskills-core</code> and then fails. That is worse than having no dry run, because it fails in a way that looks like a real problem.</p> <p>The routing has a second weakness that only shows up once TestPyPI is gone. A malformed tag does not fail \u2014 it falls through to the other index. The safety property was never \"unexpected tags are rejected\", it was \"unexpected tags land somewhere else\", and that is only safe while somewhere else exists.</p>"},{"location":"issues/v0.4/#proposal_11","title":"Proposal","text":"<ul> <li>Replace the fall-through with an explicit refusal: a tag that does not match the version   pattern fails the run with a message naming the expected shape.</li> <li>Make <code>workflow_dispatch</code> a build-only dry run \u2014 run the version guard, build all six, upload   the artifact, publish nothing.</li> <li>Let pre-release tags publish to PyPI, which handles them natively; <code>pip</code> will not install a   pre-release unless <code>--pre</code> is passed, so TestPyPI was never required for this.</li> <li>Delete the <code>testpypi</code> environment and its trusted publisher.</li> </ul>"},{"location":"issues/v0.4/#acceptance-criteria_11","title":"Acceptance criteria","text":"<ul> <li>[x] A tag of an unexpected shape fails loudly instead of publishing anywhere</li> <li>[x] <code>workflow_dispatch</code> validates and builds without uploading</li> <li>[x] Pre-release tags publish to PyPI</li> <li>[x] <code>DEVELOPMENT.md</code> documents the pending-publisher uniqueness limit</li> </ul>"},{"location":"issues/v0.4/#observations-to-carry-in","title":"Observations to carry in","text":"<p>The 0.3.0 release proved the OIDC exchange, the claim set, the build loop and the version guard end-to-end against production PyPI \u2014 which is what the TestPyPI rehearsal existed to prove. What would remain untested without it needs no index at all.</p> <p>The pending-publisher limit should be documented regardless of the outcome of this issue, because it applies to any new index and to adding a seventh package.</p>"},{"location":"issues/v0.4/#implementation-notes_11","title":"Implementation notes","text":"<p>The tag pattern is an allow-list of two shapes, not a rejection of one. <code>vX.Y.Z</code> releases, <code>vX.Y.Z</code> plus <code>a1</code>/<code>b1</code>/<code>rc1</code> pre-releases, and everything else fails with a message naming what was expected. The previous routing was also an allow-list, but its <code>else</code> branch was another index; once that index is gone the same structure fails open, and the fix is a branch that exits non-zero rather than a different destination.</p> <p>Pre-release is now a property of the tag rather than of the destination. The GitHub Release's <code>prerelease</code> flag used to be derived from <code>environment == 'testpypi'</code>, which coupled a release-notes detail to a publishing target that no longer exists. The <code>guard</code> job emits it directly.</p> <p><code>workflow_dispatch</code> stops after the artifact. It still runs the version guard and builds all seven distributions, which is what a dry run was ever able to catch here. What it cannot exercise is the upload \u2014 and that was true of the TestPyPI path too, because it only ever uploaded one package before failing.</p> <p><code>repository-url</code> is gone from all seven publish steps rather than hardcoded to PyPI. The action defaults there. A URL repeated seven times is seven places to be wrong about the same fact.</p>"},{"location":"issues/v0.4/#observations-for-later_11","title":"Observations for later","text":"<p><code>scripts/publish.{ps1,sh}</code> still offer <code>--test-pypi</code>. They are the token-based emergency path for releasing when Actions is down, and a scratch index is genuinely useful when you are already off the rails. Left alone deliberately: nothing about them depends on the trusted publisher this issue removes.</p> <p>Nothing now exercises an upload before a real one. That is the honest cost of this change and it was already the situation, since the dry run could only ever publish one package out of seven. The mitigation is <code>skip-existing</code>, which makes a failed release re-runnable rather than unrepeatable.</p>"},{"location":"issues/v0.4/#carried-over-follow-ups","title":"Carried-over follow-ups","text":"<p>Surfaced during v0.3 and too small for a roadmap row. Good first issues unless noted.</p> Item Notes MCP native binary content blocks <code>encode_resource_content()</code> wraps non-UTF-8 in a base64 JSON envelope. MCP has native binary content blocks; using them removes a layer the client must unwrap. Explicit follow-up from v0.3 item 5, and it needs the ADR in item 11 first. Generate the tool layer from a table Three of the six tools in each integration are near-identical wrappers. Three copies is where a table beats hand-written duplicates. <code>mypy</code> is referenced but not installed <code>scripts/dev.py</code> and <code>DEVELOPMENT.md</code> both invoke it; it is not in the dev group. Install it or delete the task \u2014 a documented command that cannot run is worse than neither. Poetry metadata deprecations <code>[tool.poetry]</code> <code>name</code>/<code>description</code>/<code>license</code> should move to <code>[project]</code>. Warned on every <code>poetry lock</code>. <code>validate_skill()</code> parses each skill twice It calls <code>get_body()</code> and <code>get_metadata()</code> independently, and both providers duplicate <code>_get_skill_md()</code> \u2192 <code>split_frontmatter()</code>. Caching hid the I/O cost but not the parse. Widen the lint scope to <code>scripts/</code> CI lints <code>packages/</code> and <code>examples/</code> only, so <code>scripts/dev.py</code> has drifted from the formatter. Widen the scope and reformat once, deliberately. Coverage ratchet targets <code>mcp_server/__main__.py</code> at 75% \u2014 the documented launch path is untested. <code>mcp_server/context_provider.py</code> at 83% \u2014 the import guard's two upgrade messages have never executed. <code>_KNOWN_KEYS</code> and <code>_OPTIONAL_FIELDS</code> are out of step A third specially-validated field should trigger converting the table to <code>{name: validator}</code>. Export the known frontmatter keys from core <code>agentskills lint</code> cannot warn about unknown keys without duplicating a private constant in a second package. Exporting the set makes the lint a few lines; until then it is absent. From v0.4 item 1. Semver comparison The inlined regex validates but cannot compare. Adopt the <code>semver</code> package only if range matching is actually needed. No timing instrumentation v0.3 item 8 asked how long a fetch took, and nothing answers that. Deferred deliberately to OpenTelemetry in v0.6 rather than hand-rolled <code>time.monotonic()</code> deltas."},{"location":"issues/v0.5/","title":"v0.5 \"Agent Effectiveness\"","text":"<p>Specifications for the v0.5 roadmap items, in the same order as the table in the roadmap. Milestone: <code>v0.5</code>.</p> <p>Add the GitHub issue number to a heading once the item is filed.</p> <p>Items 1 and 2 change contracts the rest of the milestone builds on \u2014 the frontmatter schema and the shape of a body fetch. They are ordered first so the others are specified against a settled surface rather than a guessed one. Items 3, 5 and 6 are leaf work that can proceed in parallel once those land.</p>"},{"location":"issues/v0.5/#1-selection-metadata-when_to_use-and-when_not_to_use","title":"1. Selection metadata: <code>when_to_use</code> and <code>when_not_to_use</code>","text":"<p>Labels: <code>theme:correctness</code>, <code>type:feature</code>, <code>package:core</code></p> <p>Status: shipped in v0.5.0. All four open questions were resolved; see the implementation notes. The upstream proposal was evaluated and deferred \u2014 see the acceptance criteria.</p>"},{"location":"issues/v0.5/#problem","title":"Problem","text":"<p><code>description</code> is the only signal an agent has when choosing a skill, and it carries positive evidence only. There is no way to say where a skill stops applying \u2014 that <code>incident-response</code> is for a production outage and not for a failing test on a laptop.</p> <p>False activation is not a lesser failure than non-activation. It is worse. Non-activation costs the tokens of a catalog entry the agent ignored; false activation costs a full body load and puts instructions written for a different situation in front of the model, which then follows them. The SDK currently has no vocabulary for expressing the boundary.</p> <p>Authors work around this by folding negative conditions into <code>description</code>. That is the wrong place for three reasons: <code>description</code> is capped at 1024 characters by <code>validate_skill()</code>, so the negative signal competes for room with the positive one; it is charged on every turn for every registered skill, whether or not that skill is ever used; and a description that reads \"use for X, but not Y, unless Z\" is worse at its primary job of being discriminative.</p> <p>This item is first in the milestone because item 3 ranks skills over a text corpus. Deciding what fields exist after the ranking is built means specifying the corpus twice.</p>"},{"location":"issues/v0.5/#proposal","title":"Proposal","text":"<p>Two optional frontmatter fields, each a list of short strings:</p> <pre><code>---\nname: incident-response\ndescription: Triage and mitigate production incidents.\nwhen_to_use:\n  - A production service is degraded or down\n  - An alert has fired and needs triage\nwhen_not_to_use:\n  - Debugging a failing test locally\n  - Planning capacity for a future launch\n---\n</code></pre> <p>Implementation is deliberately small. Add both to <code>_OPTIONAL_FIELDS</code> in <code>agentskills_core.validation</code> with type <code>list</code>, which extends <code>_KNOWN_KEYS</code> for free, and add an element-level check that every entry is a non-empty string. Nothing else in core needs to know they exist.</p> <p>They surface in the catalog next to the description, and only when set \u2014 the same rule <code>version</code> already follows, so an unversioned skill without selection metadata renders byte-identical to today:</p> <pre><code>&lt;skill&gt;\n  &lt;name&gt;incident-response&lt;/name&gt;\n  &lt;description&gt;Triage and mitigate production incidents.&lt;/description&gt;\n  &lt;when_to_use&gt;\n    &lt;case&gt;A production service is degraded or down&lt;/case&gt;\n  &lt;/when_to_use&gt;\n  &lt;when_not_to_use&gt;\n    &lt;case&gt;Debugging a failing test locally&lt;/case&gt;\n  &lt;/when_not_to_use&gt;\n&lt;/skill&gt;\n</code></pre> <p>These fields are charged on every turn, so they must be bounded and they must be droppable. Cap each entry at 200 characters and each list at five entries \u2014 a sixth condition is a sign the skill should be split. Add <code>get_skills_catalog(..., selection_hints=False)</code> for callers who want the cheap catalog back. Without both of those this feature is a way to quietly triple the per-turn cost of a registry.</p> <p>Tooling follows: <code>agentskills lint</code> warns when a skill has neither field and a description over some length (the author is probably already smuggling conditions into it), <code>agentskills init</code> scaffolds both as commented examples, and <code>agentskills inspect --turn-budget</code> must count them \u2014 a per-turn budget that ignores per-turn fields is not a budget.</p> <p>Per product principle 1 these stay optional and backward-compatible, and the design should be taken upstream to the Agent Skills specification rather than kept as a local extension.</p>"},{"location":"issues/v0.5/#open-questions","title":"Open questions","text":"<ul> <li><code>list[str]</code> or a single free-text string? A list renders and ranks cleanly and bounds   naturally; a string is nicer to write and does not push authors into telegraphic fragments.</li> <li>Top-level, or inside the free-form <code>metadata</code> mapping? <code>tags</code> went into <code>metadata</code>   precisely to avoid needing anything defended upstream. The same argument applies here and cuts   against a top-level field \u2014 but selection metadata is far more central than tags, and burying   it hurts discoverability.</li> <li>Should <code>when_not_to_use</code> be injected at all, or only used for ranking? Negative conditions   may be more valuable to a retrieval scorer than to the model, and keeping them out of the   prompt would halve the added per-turn cost.</li> <li>Does an empty list mean \"no restrictions\" or \"author has not thought about it\"? Lint needs   to tell those apart to say anything useful.</li> </ul>"},{"location":"issues/v0.5/#acceptance-criteria","title":"Acceptance criteria","text":"<ul> <li>[x] <code>when_to_use</code> / <code>when_not_to_use</code> validated as lists of non-empty strings, both optional</li> <li>[x] Per-entry and per-list limits enforced with a clear error naming the limit</li> <li>[x] Both rendered in the XML and markdown catalogs, omitted entirely when absent</li> <li>[x] <code>selection_hints=False</code> restores the current catalog output byte-for-byte</li> <li>[x] A skill written before this change validates and renders unchanged</li> <li>[x] <code>inspect --turn-budget</code> counts them; <code>lint</code> warns when both are missing</li> <li>[x] Fields documented in the concepts page</li> <li>[x] Upstream proposal evaluated \u2014 deferred, and deliberately not filed. The specification   already treats <code>description</code> as the mechanism for activation boundaries, and documents an   optimization loop for tuning it against near-miss queries. Proposing structured fields therefore   needs trigger-rate data showing they beat a tuned description, and we do not have that data.   Retained as an optional local extension; revisit with measurements rather than argument.</li> </ul>"},{"location":"issues/v0.5/#implementation-notes","title":"Implementation notes","text":"<p><code>list[str]</code>, top-level, both injected. The first two open questions were answered by the cost argument rather than the ergonomic one. A list bounds naturally \u2014 five entries of 200 characters is a ceiling an author can see, where a free-text string only has a length \u2014 and a renderer that emits one <code>&lt;case&gt;</code> per entry gives a ranker in item 3 a corpus it can score without re-parsing prose. Top-level rather than inside <code>metadata</code> for the reason the spec itself puts <code>description</code> at the top level: this is selection-critical, and burying it under a free-form mapping tells authors it is decoration.</p> <p><code>when_not_to_use</code> is injected, not ranking-only. Keeping it out of the prompt would halve the added per-turn cost, but it would also mean the model never sees the one signal that prevents false activation \u2014 which is the failure this item exists to fix. <code>selection_hints=False</code> is the escape hatch for callers who want the tokens back, and it turns off both fields together, because a catalog that shows where a skill applies but not where it stops is worse than one that shows neither.</p> <p>An empty list means \"the author considered it and found nothing.\" <code>validate_skill()</code> accepts <code>when_to_use: []</code>, and <code>lint</code> distinguishes an empty list from an absent key with <code>in metadata</code> rather than a truthiness check. <code>missing-selection-metadata</code> therefore fires only when neither key is present and the description is over 200 characters \u2014 short descriptions are not smuggling anything.</p> <p>Nothing downstream needed changing. Every integration builds its catalog through <code>SkillRegistry.get_skills_catalog()</code>, and <code>inspect --turn-budget</code> costs the string that call returns, so both the new fields and the <code>selection_hints</code> switch propagate for free.</p> <p>The renderer drops malformed values instead of raising. Registration validates these fields, so a bad value in the renderer means the catalog is being built over metadata that never passed through <code>register()</code>. <code>_cases_of()</code> warns and omits, on the principle that a malformed hint should cost a skill its hint, not its place in the catalog \u2014 the same shape as <code>_tags_of()</code>.</p>"},{"location":"issues/v0.5/#observations-for-later","title":"Observations for later","text":"<ul> <li>The 200-character entry cap is a guess. It is enforced with a clear message and easy to   raise, but nothing has measured what length actually helps a model discriminate.</li> <li>Item 3 should rank over <code>when_to_use</code> entries directly. They are already normalised,   bounded, and one condition per string, which is a better corpus than the description.</li> <li><code>missing-selection-metadata</code> overlaps <code>description-too-long-for-catalog</code> above 500   characters, where both fire. That is deliberate \u2014 they recommend different fixes \u2014 but if the   pair proves noisy, the longer rule should suppress the shorter one.</li> </ul>"},{"location":"issues/v0.5/#2-section-level-disclosure","title":"2. Section-level disclosure","text":"<p>Labels: <code>theme:agent-effectiveness</code>, <code>type:feature</code>, <code>package:core</code>, <code>package:langchain</code>, <code>package:agentframework</code>, <code>package:mcp-server</code></p> <p>Status: shipped in v0.5.0. All four open questions were resolved; see the implementation notes. One documented deviation from the proposal: <code>get_outline()</code> returns a <code>SkillOutline</code>, not a bare <code>list[SectionRef]</code>.</p>"},{"location":"issues/v0.5/#problem_1","title":"Problem","text":"<p>Progressive disclosure currently has two levels and then a cliff. An agent sees a catalog entry (tens of tokens), and the only way to learn anything more is <code>get_skill_body()</code>, which returns the entire markdown body. A thorough 4,000-token skill charges all 4,000 to answer a question covered by one 300-token section.</p> <p>This penalises exactly the skills that are worth writing. An author who documents six scenarios properly produces a skill that is more expensive to consult than a shallow one, so the format quietly rewards thin content. That inverts the point of the product.</p> <p>The building block already exists in the wrong package: <code>split_sections()</code> in <code>agentskills_tools.cost</code> splits a body at its headings, respects fenced code blocks so a <code>#</code> shell comment is not read as a heading, and guarantees the parts sum to the whole. <code>agentskills inspect --cost</code> already shows authors a per-section breakdown \u2014 the SDK can measure what a section costs but cannot serve one.</p>"},{"location":"issues/v0.5/#proposal_1","title":"Proposal","text":"<p>Promote sectioning into <code>agentskills-core</code> and expose it one level below the body.</p> <ul> <li>Move <code>Section</code> and <code>split_sections()</code> into <code>agentskills_core</code>, and have <code>agentskills-tools</code>   import them rather than keep its own copy. The v0.4 notes already flagged cross-package   duplication of parsing constants as a drift trap; this is the same trap with an algorithm.</li> <li><code>Skill.get_outline() -&gt; list[SectionRef]</code> and <code>SkillRegistry.get_skill_outline(skill_id)</code>,   returning title, level, an addressable key, and an estimated size per section.</li> <li><code>Skill.get_section(key) -&gt; str</code> and <code>SkillRegistry.get_skill_section(skill_id, key)</code>, raising a   <code>SectionNotFoundError</code> in the existing <code>LookupError</code> family.</li> <li>Two new tools \u2014 <code>get_skill_outline</code>, <code>get_skill_section</code> \u2014 in all three integrations.</li> </ul> <p>This must not become a provider capability. It is computed on top of <code>get_body()</code>, which providers already cache, so no third-party provider has to grow a markdown parser and the ADR 0002 capability-flag machinery stays out of it. The outline of a skill is a property of its content, not of where the content is stored.</p> <p>A section fetch is not automatically cheaper than a body fetch. Each one costs a tool call, a model turn, and the tokens of the outline that preceded it. For a 600-token body the round trip is pure loss. The outline tool should therefore report the body's total size and say plainly when fetching the whole thing is cheaper, using the same token counter as <code>inspect --cost</code> so the two never disagree. Shipping this without that guidance makes the common case worse in order to improve the rare one.</p>"},{"location":"issues/v0.5/#open-questions_1","title":"Open questions","text":"<ul> <li>Addressing. Headings are not unique \u2014 <code>## Setup</code> can legitimately appear twice.   Slug-plus-ordinal is simple and matches the flat, non-overlapping model <code>split_sections()</code>   already uses. A hierarchical path (<code>installation/setup</code>) reads better but implies a tree the   splitter does not build.</li> <li>The outline reports <code>level</code> but addressing is flat. Showing nesting the address space does   not model is at best confusing. Either the splitter grows real nesting or the outline stops   advertising depth.</li> <li>Where does the size threshold live \u2014 a core constant, a registry argument, or purely   advisory text in the tool description?</li> <li>Should <code>get_skill_body</code> gain a <code>sections=[...]</code> argument instead of a separate tool, so an   agent can fetch three sections in one call rather than three?</li> </ul>"},{"location":"issues/v0.5/#acceptance-criteria_1","title":"Acceptance criteria","text":"<ul> <li>[x] <code>Section</code> / <code>split_sections()</code> live in core; tools re-exports rather than reimplements</li> <li>[x] <code>get_skill_outline</code> and <code>get_skill_section</code> on <code>Skill</code> and <code>SkillRegistry</code></li> <li>[x] Unknown section key raises a <code>LookupError</code> subclass, consistent with the taxonomy</li> <li>[x] No change to the <code>SkillProvider</code> ABC and no new capability flag</li> <li>[x] Both tools exposed in the LangChain, Agent Framework, and MCP integrations</li> <li>[x] Outline reports total body size and identifies when a whole-body fetch is cheaper</li> <li>[x] A body with no headings yields a single section and still works end to end</li> <li>[x] Conformance suite unchanged \u2014 this is not a provider concern</li> </ul>"},{"location":"issues/v0.5/#implementation-notes_1","title":"Implementation notes","text":"<p>Addressing is a flat slug with an ordinal on collision. <code>## Rolling Back a Deploy</code> becomes <code>rolling-back-a-deploy</code>; a second <code>## Setup</code> becomes <code>setup-2</code>. A hierarchical path was rejected because it would advertise a tree the splitter does not build \u2014 and because depth does not disambiguate anyway: <code>## Setup</code> followed by <code>### Setup</code> still collides, which is exactly what the flat model says should happen.</p> <p>The outline shows depth as indentation and states plainly that sections do not nest. The alternative \u2014 growing real nesting in the splitter \u2014 would break the property that the parts sum to the whole, which <code>agentskills inspect --cost</code> depends on to report a body's cost as a breakdown rather than an overlapping double-count. So the rendered text carries the caveat in words: \"Sections do not nest \u2014 a section covers only its own text, up to the next heading of any level, so fetching a parent does not include what is indented under it.\"</p> <p>The size threshold is a core constant, <code>WHOLE_BODY_CHEAPER_TOKENS = 1000</code>, surfaced as <code>SkillOutline.whole_body_is_cheaper</code> and stated in the rendered text. Not a registry argument: nobody would tune it, and an untuned knob is just a bigger API. Not advisory prose in three tool descriptions: that is precisely the kind of duplicated constant the v0.4 notes flagged as a drift trap.</p> <p><code>get_skill_body</code> did not gain a <code>sections=[...]</code> argument. A separate single-key tool keeps each tool's contract one sentence long, and the outline's \"past about three sections, call <code>get_skill_body</code> instead\" guidance already answers the N-round-trips problem \u2014 at three sections of a body you are usually better off buying the whole body.</p> <p>Deviation: <code>get_outline()</code> returns <code>SkillOutline</code>, not <code>list[SectionRef]</code>. The \"which is cheaper\" judgement needs the whole-body figure next to the per-section figures, and it needs to be rendered identically everywhere. A bare list would have pushed both into the three integrations, which is how they would come to quote three different costs for the same skill. <code>SkillOutline.render()</code> is the single agent-facing rendering; <code>.sections</code>, <code>.total_tokens</code> and <code>.whole_body_is_cheaper</code> remain available to callers who want to build their own.</p> <p><code>estimate_tokens</code> moved from <code>agentskills_tools.lint</code> into core for the same reason: the outline's numbers and <code>inspect --cost</code>'s heuristic counter must not be able to disagree. tiktoken stays a tools-only refinement layered on top, since the runtime path cannot take that dependency.</p>"},{"location":"issues/v0.5/#observations-for-later_1","title":"Observations for later","text":"<ul> <li>The 1,000-token threshold is a considered guess, not a measurement. It should be revisited   against real traces of agents choosing between an outline and a body.</li> <li>Batch section fetch (<code>get_skill_sections(skill_id, keys)</code>) was deliberately deferred. If   traces show agents routinely pulling two or three sections, it becomes worth the second tool.</li> <li>Indentation-versus-flat-addressing remains a live tension. The outline shows a shape it does   not let you address. If agents are observed fetching a parent expecting its children, the   answer is probably to stop indenting, not to start nesting.</li> </ul>"},{"location":"issues/v0.5/#3-semantic-skill-selection","title":"3. Semantic skill selection","text":"<p>Labels: <code>theme:agent-effectiveness</code>, <code>type:feature</code>, <code>package:retrieval</code></p> <p>Depends on: item 1 (selection metadata supplies the corpus)</p> <p>Status: shipped in v0.5.0. All five open questions were resolved; see the implementation notes. One documented deviation from the proposal: this did require a change to <code>agentskills-core</code> \u2014 a single optional <code>total=</code> keyword on <code>get_skills_catalog()</code> \u2014 because two of the acceptance criteria could not otherwise both hold. The reasoning is in the notes.</p>"},{"location":"issues/v0.5/#problem_2","title":"Problem","text":"<p>The catalog is injected on every turn, so per-turn prompt cost is linear in the number of registered skills. <code>get_skills_catalog()</code> already offers <code>include</code>, <code>exclude</code>, <code>tags</code> and <code>max_chars</code>, but every one of those requires the caller to know the answer in advance. Nothing narrows the catalog based on what the user actually asked.</p> <p>Two costs compound. The obvious one is tokens: at fifty skills the catalog alone is thousands of tokens on every turn of every conversation. The less obvious one is accuracy \u2014 selection quality degrades as the candidate list grows, so a large registry is worse at picking the right skill and charges more for the privilege. <code>max_chars</code> is not a substitute; it drops entries from the end, which is arbitrary with respect to relevance.</p>"},{"location":"issues/v0.5/#proposal_2","title":"Proposal","text":"<p>A new <code>agentskills-retrieval</code> package. A <code>SkillSelector</code> protocol with one method returning scored skill IDs, and two implementations:</p> <ul> <li><code>LexicalSelector</code> \u2014 zero-dependency BM25 over description, <code>when_to_use</code>, <code>when_not_to_use</code>   and tags. The default, so the package is useful with nothing else installed.</li> <li><code>EmbeddingSelector</code> \u2014 embeds each skill's text once and ranks by cosine similarity. The   embedder is a one-method protocol resolved from a dotted path, exactly as the v0.4 eval harness   resolves models, so no embedding SDK becomes a dependency of anything.</li> </ul> <p>The integration point already exists. A selector returns skill IDs; those IDs feed the existing <code>include=</code> filter, which runs before any metadata is fetched \u2014 so narrowing to five of fifty skills also avoids forty-five provider round trips. Core needs no new code and gains no knowledge of retrieval. If a design for this requires changing <code>agentskills-core</code>, it is the wrong design.</p> <p>Embeddings are computed once per skill and cached by content hash, so re-registering an unchanged skill is free and editing one invalidates only itself. In-memory by default with a hook for persistence; a hosted registry should not re-embed its corpus on every process start.</p> <p>Selection must be visible. This trades a deterministic prompt for a better one, and a skill that was scored out is indistinguishable, from inside the agent, from a skill that was never registered. So: log every selection with its scores, and report it in the catalog the way truncation already is \u2014 <code>selected=\"5\" total=\"50\"</code> on the XML root, a closing note in markdown.</p>"},{"location":"issues/v0.5/#open-questions_2","title":"Open questions","text":"<ul> <li>How is this measured? The v0.4 eval harness measures whether a skill helps once the model   has it; that is a different question from whether the right skill was chosen. Selection needs a   labelled fixture set \u2014 queries paired with the skill that should win \u2014 and a recall@k number   that has to clear a bar before the feature is recommended. The roadmap already warns that   catalog pruning must be evaluated before it becomes a default; shipping ranking without a   measurement is that mistake one item earlier.</li> <li>What is the query? The last user message is the obvious choice and is wrong for   conversations where the topic was established several turns ago.</li> <li>Does the selector see negative conditions? <code>when_not_to_use</code> is a demotion signal, not a   match signal, and BM25 has no natural way to express that.</li> <li>Separate package or a core extra? Precedent from v0.4 item 1 says separate package. Worth   restating explicitly rather than inheriting by accident.</li> <li>A floor as well as a top-k? Returning the five best of fifty irrelevant skills is worse   than returning nothing and letting the agent answer directly.</li> </ul>"},{"location":"issues/v0.5/#notes","title":"Notes","text":"<p><code>agentskills-retrieval</code> is a new distribution, so before the v0.5.0 tag it needs a tenth entry in every place a package must be registered (root <code>pyproject.toml</code> three times, <code>COVERAGE_FLOORS</code>, both bump scripts, <code>check_release_version.py</code>, <code>check_declared_dependencies.py</code>, <code>publish.yml</code> twice, <code>docs.yml</code> twice, <code>mkdocs.yml</code> nav, and a <code>docs/packages/</code> page) plus a pending trusted publisher on PyPI \u2014 and only one pending publisher can exist at a time, so it must be registered alone. The name and its ultranormalized neighbours were free on PyPI as of 2026-08-17.</p>"},{"location":"issues/v0.5/#acceptance-criteria_2","title":"Acceptance criteria","text":"<ul> <li>[x] <code>agentskills-retrieval</code> published, with <code>LexicalSelector</code> usable with zero extra dependencies</li> <li>[x] <code>EmbeddingSelector</code> with a dotted-path embedder protocol and no vendor SDK dependency</li> <li>[x] Selector output composes with <code>include=</code> and requires no change to <code>agentskills-core</code>       \u2014 composes with <code>include=</code>, but see the deviation below on <code>total=</code></li> <li>[x] Embeddings cached by content hash, with a documented persistence hook</li> <li>[x] Every selection logged with scores, and reported in both catalog formats</li> <li>[x] A labelled fixture set and a published recall@k for both selectors</li> <li>[x] Opt-in; the default path is byte-identical to today</li> </ul>"},{"location":"issues/v0.5/#implementation-notes_2","title":"Implementation notes","text":"<p>Deviation: one keyword was added to <code>agentskills-core</code>. Two criteria were in direct conflict. \"Composes with <code>include=</code> and requires no change to core\" and \"reported in both catalog formats\" cannot both hold as written, because core computed the <code>total</code> in <code>truncated=\"true\" shown=\"N\" total=\"M\"</code> after filtering. An <code>include=</code>-narrowed catalog therefore had <code>shown == total</code> and silently claimed to be the whole registry \u2014 the exact failure the visibility criterion exists to prevent.</p> <p>The resolution is <code>get_skills_catalog(total=...)</code>: an optional denominator, meaning \"the size of the set these entries were chosen from\". Core accepts a number and learns nothing about ranking, scoring, queries or selectors, which is what \"no change to core\" was actually protecting. The default is the post-filter count, so a deliberate static <code>tags=</code> filter does not pay for a \"truncated\" note on every turn, and a <code>total</code> below the shown count is ignored rather than rendering \"showing 2 of 1\". The alternative \u2014 teaching every integration to prepend its own note \u2014 would have put the same sentence in three packages, which is the drift trap the v0.4 notes already flagged twice.</p> <p>Measurement is a labelled fixture set with asserted floors. <code>tests/conftest.py</code> holds twelve skills with deliberately overlapping vocabulary and thirteen queries phrased as a user would phrase them, each paired with the skill that should win. <code>tests/test_recall.py</code> asserts recall@1 \u2265 0.85 and recall@3 = 1.0, so a change that makes ranking worse fails CI rather than being noticed in production. The numbers are published in the package README with the caveat that a small synthetic corpus is a regression guard, not a benchmark. The <code>EmbeddingSelector</code> is measured against a deterministic stub embedder, which tests the plumbing only \u2014 a recall number produced by a stub would be a claim about nothing, and the README says so rather than quoting one.</p> <p>The caller supplies the query; the package never derives one. Both obvious defaults are wrong in opposite directions: the last user message alone fails whenever the topic was set several turns ago (\"try that again\" ranks against nothing), and concatenating the history drags in every topic the conversation has touched. Guessing on the caller's behalf would turn an obvious integration decision into a silent accuracy regression, so <code>select()</code> takes a string.</p> <p>Negative conditions demote, they do not match. <code>when_not_to_use</code> is indexed as a separate field and its score subtracted at <code>NEGATIVE_WEIGHT = 0.5</code>. One bag of words would make a skill match the very query its author wrote it to disclaim \u2014 \"not for local test failures\" would make the skill more likely to win on a local test failure. The weight is below 1.0 because a disclaimer is weaker evidence than a description: authors write far fewer of them and phrase them loosely, and at 1.0 a single shared word could cancel a genuine description match. It is a constructor argument, and <code>0.0</code> restores the naive behaviour.</p> <p>Separate package, restated deliberately. Following v0.4 item 1's precedent rather than inheriting it by accident. A core extra would put a ranking algorithm behind an import guard in the one package everything else depends on, and the whole design here is that core stays ignorant of retrieval.</p> <p>Both a floor and a top-k, with different defaults for a reason. <code>DEFAULT_EMBEDDING_MIN_SCORE = 0.25</code> is meaningful because cosine is bounded and comparable across corpora \u2014 though still model-specific, hence a constructor argument. <code>DEFAULT_LEXICAL_MIN_SCORE = 0.0</code> is as high as BM25 honestly allows: the scores are unbounded and corpus-relative, so zero catches \"shares no term with any skill\" but cannot catch \"matched a common word and is still irrelevant\". That limit is stated in the README rather than papered over with an invented constant, and it is why recall is measured.</p> <p>An empty selection falls back to the full catalog. \"Selection has no opinion\" is not \"the agent should have no skills\". A wrong prune silently removes a capability, which is a worse and far harder-to-diagnose failure than a few wasted tokens. The fallback is logged, and <code>Selection.rejected</code> carries the near-misses so that \"scored just under the floor\" and \"never registered\" are distinguishable.</p> <p>No stemming. It would need a dependency, and the package's claim is that it is useful with none. The cost is real \u2014 \"deploys\" will not match \"deploy\" \u2014 and buying that back is the main thing <code>EmbeddingSelector</code> is for.</p>"},{"location":"issues/v0.5/#observations-for-later_2","title":"Observations for later","text":"<ul> <li>The recall floors are over a synthetic corpus that the same person wrote both halves of. The   honest next step is a fixture set drawn from real registries and real queries.</li> <li>BM25's inability to express a meaningful non-zero floor is the weakest part of the design. If   traces show irrelevant skills clearing zero on common words, the fix is probably score   normalisation against the corpus, not a hand-picked constant.</li> <li><code>total=</code> currently reuses the <code>truncated=\"true\"</code> vocabulary, which is not quite right: a   selection is not a truncation. If agents are observed treating a narrowed catalog as evidence   that skills were dropped arbitrarily, the attribute deserves its own name.</li> <li>The stub-embedder recall figure is a placeholder. A published number for at least one real   embedding model would make <code>EmbeddingSelector</code> recommendable rather than merely available.</li> </ul>"},{"location":"issues/v0.5/#4-session-aware-progressive-disclosure-in-the-context-provider","title":"4. Session-aware progressive disclosure in the context provider","text":"<p>Labels: <code>theme:agent-effectiveness</code>, <code>type:feature</code>, <code>package:agentframework</code></p> <p>Status: shipped in v0.5.0 \u2014 sub-items (a), (b), (f) and the load-driven half of (d). (c), (e), (g) and (h) are deferred with reasons; see the implementation notes.</p>"},{"location":"issues/v0.5/#problem_3","title":"Problem","text":"<p><code>AgentSkillsContextProvider.before_run()</code> re-injects the full catalog, the full usage instructions, and all five tools on every <code>agent.run()</code> call, regardless of what the agent already loaded. In a long session that is repeated token spend on information the model already has, plus repeated provider I/O.</p> <p>Agent Framework's <code>BaseContextProvider</code> gives us <code>before_run</code> / <code>after_run</code> hooks, a provider-scoped <code>state</code> dict that persists across runs within a session, a shared <code>session.state</code>, and a per-invocation <code>SessionContext</code> (<code>input_messages</code>, <code>instructions</code>, <code>tools</code>, <code>response</code>, <code>metadata</code>, <code>options</code>).</p>"},{"location":"issues/v0.5/#proposal_3","title":"Proposal","text":"<p>Ordered by value; each could ship independently.</p> <p>a. Cache the built prompt in <code>state</code>. Store the assembled catalog and instructions after the first <code>before_run()</code>; skip provider I/O on subsequent runs. Optional <code>cache_ttl</code>. Biggest win for HTTP-backed registries and the cheapest to implement.</p> <p>b. Track loaded skills. In <code>after_run()</code>, inspect <code>context.response</code> for skill tool calls and record IDs in <code>state[\"loaded_skills\"]</code>. Skip re-injecting content the agent already pulled.</p> <p>c. Conditional tool injection. First turn: inject only <code>get_skill_metadata</code> and <code>get_skill_body</code>. Once a skill body has been loaded, add <code>get_skill_reference</code> / <code>get_skill_script</code> / <code>get_skill_asset</code> for that skill. Framework-level progressive disclosure instead of a prompt instruction, and a smaller tool surface for the model to reason about.</p> <p>d. Catalog pruning. Inspect <code>context.input_messages</code> for topic signal and advertise only relevant skills, falling back to the full catalog when the topic is unclear. Highest risk \u2014 a wrong prune hides a skill the agent needed. Should be opt-in and evaluated before it becomes a default.</p> <p>e. Usage telemetry via <code>after_run</code>. Count skill and resource usage into <code>state</code>, and expose a callback so hosts can export it. Answers \"which skills are actually used\" and feeds Hub analytics.</p> <p>f. Cross-provider signalling. Publish <code>metadata[\"agentskills_loaded_skills\"]</code> on <code>context.metadata</code> so other context providers can adapt; optionally read another provider's topic classification to inform (d).</p> <p>g. Context messages instead of instructions. Use <code>context.extend_messages()</code> so injected skill content carries source attribution and can be filtered by other providers via <code>context.get_messages(sources={\"agentskills\"})</code>.</p> <p>h. Agent-aware formatting. Use the <code>agent</code> handle to pick catalog format by model family (XML for Claude, markdown elsewhere) rather than a fixed constructor argument.</p>"},{"location":"issues/v0.5/#notes_1","title":"Notes","text":"<ul> <li>(a) and (b) are the core of the issue; (d) needs an evaluation harness before it can be trusted.</li> <li>(d) is item 3 solved a second time, worse \u2014 with conversation state but without a measurement. Prefer running item 3's selector here over inventing a second ranker in an integration package.</li> <li>(c) is far more valuable once item 2 lands: the tool surface then grows per section as well as per skill.</li> <li>Anything here that generalises beyond Agent Framework should land in <code>agentskills-core</code> so the LangChain and MCP paths benefit too.</li> <li>API reference: <code>_sessions.py</code></li> </ul>"},{"location":"issues/v0.5/#acceptance-criteria_3","title":"Acceptance criteria","text":"<ul> <li>[x] Prompt/catalog cached across runs within a session, with opt-out</li> <li>[x] Loaded skills tracked in provider-scoped <code>state</code></li> <li>[x] Repeated <code>before_run()</code> calls remain idempotent</li> <li>[x] Token count for turn N+1 measurably lower than today for a multi-turn session</li> </ul>"},{"location":"issues/v0.5/#implementation-notes_3","title":"Implementation notes","text":"<p>Caching alone cannot satisfy the fourth criterion. Caching the assembled prompt saves provider I/O; it does not save a single token, because turn N+1 sends exactly the same text. The token reduction has to come from pruning. Once a skill's full body is in the conversation, its catalog entry is being paid for twice. So (a) and (b) shipped together, and (b) is what the fourth criterion actually rests on.</p> <p>Pruning is driven by full-body loads only. <code>BODY_TOOLS = {\"get_skill_body\"}</code>. <code>get_skill_section</code> and <code>get_skill_outline</code> deliberately do not mark a skill loaded: a fragment is not the skill, and pruning the entry after one section read would hide most of it while the agent still needs to know it exists. <code>get_skill_metadata</code> is likewise not a load \u2014 it restates the catalog entry rather than replacing it.</p> <p>Pruned entries are replaced by a compact reminder, so the IDs never disappear from the prompt. An agent that finds no trace of a skill will reasonably conclude it does not have one.</p> <p>Pruning is declined when it would not pay. The reminder is a fixed cost and a catalog entry is not, so for very terse skills the pruned prompt can be larger. The provider builds both, keeps the shorter, and logs at debug level when it declines. This is the one regression the feature exists to prevent, and asserting it away in a test would have been cheaper than honest. The extra render happens once per cache invalidation, not once per turn.</p> <p>Never prune to nothing. If every registered skill has been loaded, the full catalog is emitted. \"You have no skills\" is a worse lie than repeating entries.</p> <p>Item 3's <code>total=</code> keyword is reused so a pruned catalog reports the narrowing honestly (<code>shown=\"2\" total=\"3\"</code>) instead of quietly presenting itself as complete.</p> <p>No <code>cache_ttl</code>, despite (a) offering one. Within a session the invalidation signal is the registry's skill set and the loaded set, not the clock \u2014 both are in the cache key. A TTL would be an untuned knob whose only correct value is the one the key already computes.</p> <p>Idempotency lives in <code>context.metadata</code>, not <code>state</code>. <code>context</code> is fresh per run and <code>state</code> persists for the session, so a <code>state</code> marker would suppress injection on turn 2 entirely. The marker is <code>agentskills_injected:{source_id}</code>, which also lets two providers over two registries both contribute to one context.</p> <p><code>after_run</code> reads <code>context.response</code> rather than wrapping the tools. A caller who passed <code>get_tools()</code> output straight to the agent is then tracked identically to one who let the provider inject. Malformed tool arguments are skipped, never raised on: this is bookkeeping running after a successful turn, and it must not be the thing that fails it.</p> <p>State values are JSON-compatible \u2014 a sorted <code>list</code>, not a <code>set</code> \u2014 because a host is entitled to serialise and restore the session.</p> <p>(d) catalog pruning by topic is not implemented, per this issue's own note: it is item 3 solved a second time, worse. What shipped is pruning by what the agent read, which needs no ranker and cannot hide a skill the agent has not seen. Hosts wanting topic narrowing should compose <code>agentskills-retrieval</code> in front of the registry.</p> <p>(c), (e), (g), (h) deferred. (c) conditional tool injection is worth much more after item 5's fast path settles what a minimal tool surface looks like. (e) telemetry wants a designed export surface, not a counter dict. (g) <code>extend_messages</code> changes where skill content appears in the transcript and deserves its own evaluation. (h) agent-aware formatting means sniffing model families from a handle whose shape is not stable.</p>"},{"location":"issues/v0.5/#observations-for-later_3","title":"Observations for later","text":"<ul> <li>Pruning assumes the loaded body is still in the conversation. A host that compacts or   truncates history can drop it, leaving the agent with a reminder pointing at text that is no   longer there. <code>prune_loaded_skills=False</code> is the escape hatch; a real fix needs a signal from   the host that history was compacted, which the framework does not currently expose.</li> <li>The loaded set is per provider <code>state</code>, so it does not survive a process restart unless the   host persists session state. That is the host's call, and the values are JSON-safe for it.</li> <li><code>body_loads_in()</code> matches on tool name, so a host that renames the tools loses tracking   silently. Worth revisiting if renaming ever becomes supported.</li> </ul>"},{"location":"issues/v0.5/#5-single-skill-fast-path","title":"5. Single-skill fast path","text":"<p>Labels: <code>theme:performance</code>, <code>type:feature</code>, <code>package:langchain</code>, <code>package:agentframework</code>, <code>package:mcp-server</code></p> <p>Depends on: item 3 (the selected set, not just the registered set)</p> <p>Status: shipped in v0.5.0. All four open questions were resolved; see the implementation notes. One documented deviation: the opt-in is a resolved <code>FastPath</code> value rather than a boolean flag, because resolution is async and two of the three entry points are sync. The reasoning is in the notes.</p>"},{"location":"issues/v0.5/#problem_4","title":"Problem","text":"<p>An agent with exactly one registered skill pays the full discovery apparatus to reach content there was never a choice about. Today that means a catalog listing one skill, five tool definitions, a block of usage instructions describing a multi-step workflow, and then a complete model round trip while the agent calls <code>get_skill_body</code> and waits.</p> <p>Every part of that is waste in the single-skill case. A catalog exists to let a model choose; there is nothing to choose. Usage instructions describe a workflow with one branch. And the round trip adds latency plus a second billed model turn to deliver text that could have been in the first prompt.</p> <p>This is also the shape of most first contact with the SDK \u2014 one skill, one agent, one job \u2014 so the configuration people evaluate the library on is the one it handles least efficiently.</p>"},{"location":"issues/v0.5/#proposal_4","title":"Proposal","text":"<p>When the effective skill set is exactly one, inject that skill's body directly into the instructions and skip the catalog, the usage instructions, and the body-fetch tool.</p> <p>\"Effective\" means after filtering and selection, not merely <code>len(registry.list_skills()) == 1</code>. A registry of fifty narrowed to one by item 3 is the same situation and should take the same path \u2014 which is why this is specified after retrieval.</p> <p>Resource tools stay. References, scripts and assets are still genuinely progressive, and a skill that carries a 2 MB dataset must not have it inlined because the skill count happened to be one.</p> <p>Body size decides whether this is a win, so it has to be a named threshold rather than an assumption. Inlining a 20,000-token body on every turn of a long conversation is strictly worse than one tool call that happens once. Gate on a token ceiling using the same counter as item 2, default it conservatively, and log when the fast path was declined and why \u2014 silently taking a different code path based on content size is the kind of behaviour that makes token bills impossible to explain.</p> <p>Ship it opt-in. Promote it to the default only once the numbers are in.</p>"},{"location":"issues/v0.5/#open-questions_3","title":"Open questions","text":"<ul> <li>What is the ceiling, and is it tokens or turns? A body worth inlining for a three-turn   conversation may not be for a thirty-turn one, and the integration knows the former but not the   latter.</li> <li>Does the agent still need the usage instructions? They explain resource tools too, so   dropping them wholesale may leave the model unaware it can read references.</li> <li>Interaction with item 4(a). Once the prompt is cached per session, the repeated-injection   cost this item avoids is already partly paid down; the two need measuring together rather than   separately.</li> <li>Does the skill's identity still need to be visible so the agent can attribute its   behaviour, or does the body simply become part of the system prompt?</li> </ul>"},{"location":"issues/v0.5/#acceptance-criteria_4","title":"Acceptance criteria","text":"<ul> <li>[x] Opt-in flag on all three integrations with consistent naming</li> <li>[x] Triggers on the effective set of one, including a set narrowed by selection</li> <li>[x] Resource tools remain available; only the catalog, instructions and body tool are dropped</li> <li>[x] Token ceiling enforced, configurable, and logged when the fast path is declined</li> <li>[x] Measured saving published for a single-skill agent over a representative conversation</li> <li>[x] Default path unchanged when the flag is off</li> </ul>"},{"location":"issues/v0.5/#implementation-notes_4","title":"Implementation notes","text":"<p>The decision lives in <code>agentskills-core</code>, not in each integration. New <code>agentskills_core.fastpath</code> provides <code>resolve_fast_path()</code>, <code>FastPath</code>, <code>DEFAULT_FAST_PATH_MAX_TOKENS</code>, <code>FAST_PATH_DROPPED_TOOLS</code> and <code>FAST_PATH_RESOURCE_INSTRUCTIONS</code>. \"How many skills, how big is the body\" is identical everywhere, so three copies could only ever disagree \u2014 and the ceiling is the part that has to be tuned. One knob is tunable; three that must be kept in step are not. This also satisfies \"consistent naming\" structurally rather than by discipline.</p> <p>The opt-in is a value, not a boolean \u2014 a deviation from the spec. Resolution is async (it reads the body), but <code>get_tools()</code> and <code>create_mcp_server()</code> are sync. A <code>fast_path: bool</code> would have forced either an async variant of both or a second resolution path per integration. So the caller resolves once and passes the result:</p> <pre><code>fast_path = await resolve_fast_path(registry)\ntools    = get_tools(registry, fast_path=fast_path)                  # langchain, agentframework\nserver   = create_mcp_server(registry, name=..., fast_path=fast_path)\nprovider = AgentSkillsContextProvider(registry, fast_path=fast_path)\n</code></pre> <p>One keyword, one type, all three. <code>None</code> is the normal path, so the call is safe unconditionally, and the prompt an integration injects cannot disagree with the tools it drops about which skill was chosen.</p> <p>The ceiling default is derived, not guessed \u2014 this is the answer to \"tokens or turns\". The normal path pays the catalog and usage instructions every turn (~506 tokens measured together) and the body once. The fast path pays a ~200-token wrapper and the body every turn. Over <code>T</code> turns the fast path wins while <code>body \u00d7 (T \u2212 1) &lt; 306 \u00d7 T</code>:</p> Conversation length Fast path wins while body is 1 turn any size 2 turns &lt; 612 tokens 3 turns &lt; 459 tokens 10 turns &lt; 340 tokens 100 turns &lt; 309 tokens <p>The integration knows the body size but not the turn count, so <code>DEFAULT_FAST_PATH_MAX_TOKENS</code> is 300 \u2014 the value that needs no assumption about the latter. The first draft used 2000, which the measurement showed loses badly past a handful of turns. 300 is deliberately small: a skill elaborate enough to exceed it is one where re-sending the body every turn costs more than the single tool call the fast path removes, which is exactly when it should not fire. A core test pins the arithmetic and a LangChain test pins the ~506 it rests on, so neither can rot silently.</p> <p>Measured saving, for a four-section incident-response skill (119-token body) at the default ceiling: 49% at one turn, 42% at three, 38% at ten. The round trip the fast path also removes \u2014 a whole extra billed model turn \u2014 is not in those numbers.</p> <p>Four tools are dropped, not one \u2014 a second deviation. The criterion says \"the body tool\", which predates item 2 adding <code>get_skill_outline</code> and <code>get_skill_section</code>. All three index into a body the agent is already holding. <code>get_skill_metadata</code> goes too: it answers \"should I use this skill\", which is the question the fast path exists because nobody is asking. Leaving any of them registered offers the model three ways to re-fetch text it already has.</p> <p>Usage instructions are trimmed, not dropped \u2014 the answer to the third open question. Dropping them wholesale would leave the model unaware it can read references at all; keeping them would point it at a catalog that is no longer there. <code>FAST_PATH_RESOURCE_INSTRUCTIONS</code> is the resource half without the selection workflow, and it is embedded in <code>FastPath.prompt</code>.</p> <p>The skill keeps its identity \u2014 the answer to the fourth. The body is wrapped in <code>&lt;skill id=\"...\"&gt;</code> rather than becoming an anonymous slab of system prompt, so the agent can still attribute its behaviour to a named skill.</p> <p>MCP omits tools at construction rather than declining at call time. MCP has no way to hide a registered tool later, and a tool that exists but always refuses is worse than one that was never advertised. Both <code>skills://catalog/*</code> resources serve <code>FastPath.prompt</code>, so an existing client that already injects the catalog needs no change.</p> <p>Interaction with item 4(a) \u2014 the second open question. They do not overlap. Item 4's cache saves provider I/O on repeat turns; the fast path removes the catalog and a round trip altogether. When <code>fast_path</code> is set the provider skips caching and pruning entirely: the body is in the prompt, so there is nothing left to cache against and nothing to prune.</p>"},{"location":"issues/v0.5/#observations-for-later_4","title":"Observations for later","text":"<ul> <li><code>resolve_fast_path</code> is resolved once by the caller, so a registry that changes afterwards   leaves a stale decision. The Agent Framework provider could re-resolve in <code>before_run</code> since   that hook is async; it does not today, and the READMEs say to resolve again instead. Worth   revisiting if mid-session registration turns out to be common.</li> <li>The ~506 figure is measured against the integrations' own usage-instruction text. If that   text is rewritten, the derived default should be recomputed rather than left alone.</li> <li>Nothing wires <code>include=</code> to <code>agentskills-retrieval</code> automatically; the READMEs show the   two-line composition. An integration-level <code>selector=</code> argument would remove the step but   would also put retrieval in the dependency graph of all three integrations.</li> </ul>"},{"location":"issues/v0.5/#6-vision-native-assets","title":"6. Vision-native assets","text":"<p>Labels: <code>theme:agent-effectiveness</code>, <code>type:feature</code>, <code>package:core</code>, <code>package:langchain</code>, <code>package:agentframework</code>, <code>package:mcp-server</code></p> <p>Depends on: the binary-safe resource envelope shipped in v0.3</p> <p>Status: shipped in v0.5.0. All four open questions were resolved; see the implementation notes. Native delivery is opt-in via <code>vision=True</code>, so the default behaviour is byte-for-byte what it was and the return-type widening is not a breaking change. Recorded as ADR 0009.</p>"},{"location":"issues/v0.5/#problem_5","title":"Problem","text":"<p>Every integration returns resource content as <code>str</code>, so <code>encode_resource_content()</code> wraps anything that is not valid UTF-8 in a JSON envelope with base64 in a <code>content</code> field. For a PNG that means the model receives a description of an image instead of an image.</p> <p>The token economics are the worst available. Base64 inflates the payload by about a third, the result tokenizes as unstructured noise at roughly one token per few characters, and the model cannot see any of it \u2014 so a diagram costs thousands of tokens to convey exactly nothing. The 64 KiB inline cap makes it worse in a different way: most real screenshots exceed it, so they come back as a stub saying the file was too large to include. Either way an author cannot ship a skill that shows the agent an architecture diagram or a UI screenshot.</p> <p>v0.3 made binary resources safe \u2014 they are no longer silently corrupted by <code>decode(\"utf-8\", errors=\"replace\")</code> \u2014 and explicitly deferred making them useful. This is that follow-up.</p>"},{"location":"issues/v0.5/#proposal_5","title":"Proposal","text":"<p>Stop stringifying resources the host can render natively, and hand them to the model as content blocks:</p> Integration Native representation MCP <code>ImageContent</code> \u2014 already in the protocol, and the specific follow-up v0.3 named LangChain multimodal content blocks on the message Agent Framework the framework's binary/data content type <p>Core gains one small classifier \u2014 given a filename and bytes, report the media type and whether it is a candidate for native rendering \u2014 so the three integrations cannot drift on what counts as an image. <code>encode_resource_content()</code> stays exactly as it is and remains the fallback for everything else; this adds a branch above it rather than replacing it.</p> <p>The 64 KiB inline cap should become per-modality. It was chosen for base64 in a text field, where size maps directly to tokens. A natively-attached image is charged by tile count, so the old number is both too small for images and irrelevant to how they are billed.</p>"},{"location":"issues/v0.5/#open-questions_4","title":"Open questions","text":"<ul> <li>This changes a tool's return type from <code>str</code> to a union, in all three integrations. That is   a breaking change to the tool contract and needs either a version gate or an opt-in flag, and   the decision should be recorded as an ADR alongside   ADR 0007 rather than quietly amending it.</li> <li>What happens on a text-only model? Handing an image block to a model that cannot accept one   is an API error, not a degradation. The integration may not know the model's modality, in which   case this has to be caller-declared.</li> <li>Which formats qualify? PNG, JPEG, GIF and WebP are uncontroversial. PDF is supported   natively by some models and not others; SVG is text, so it already arrives readable and   arguably should not be converted to a raster block.</li> <li>Does <code>list_skill_resources</code> need to advertise renderability, so the agent knows an asset is   worth requesting before it spends a tool call finding out?</li> </ul>"},{"location":"issues/v0.5/#acceptance-criteria_5","title":"Acceptance criteria","text":"<ul> <li>[x] Media-type and renderability classifier in core, used by all three integrations</li> <li>[x] Images returned as native content blocks in MCP, LangChain, and Agent Framework</li> <li>[x] Non-renderable resources keep the existing JSON envelope, unchanged</li> <li>[x] Per-modality size limits, documented, replacing the single inline cap</li> <li>[x] Behaviour on a text-only model is defined and tested, not an exception from the provider API</li> <li>[x] ADR recorded for the tool return-type change</li> <li>[x] An example skill carrying an image, with a test proving the model receives it natively</li> </ul>"},{"location":"issues/v0.5/#implementation-notes_5","title":"Implementation notes","text":"<p>One classifier, in core. <code>classify_resource(name, data, *, max_inline_image_bytes)</code> returns a <code>ResourceMedia</code> \u2014 media type, size, whether it is text, whether it is worth rendering. All three integrations call it and differ only in how they wrap the result. <code>encode_resource_content()</code> is untouched and remains the fallback, so this is a branch above v0.3 rather than a replacement.</p> <p>Detection is by magic bytes, then by name. A name is a claim; bytes are evidence. A <code>.png</code> holding a ZIP is not renderable, and a real PNG called <code>.dat</code> is. The failure mode of the reverse order is promising an image and delivering bytes the provider rejects.</p> <p>The first version asked \"does it decode as UTF-8?\" before \"what is it?\", and a test caught the consequence: a short GIF or WebP header padded with NULs decodes cleanly, so real images were being classified as text. Sniffing now comes first.</p> <p>The return type widens for everyone; the runtime type only changes when asked. <code>vision</code> defaults to <code>False</code> on <code>get_tools()</code> and <code>create_mcp_server()</code>, so no existing caller sees anything new. A test in each integration pins that the tool set and the argument schema are identical either way \u2014 the widening is in the annotation, not the wire.</p> <p>Scripts were left alone. <code>get_skill_script</code> still returns <code>str</code>. A script is code; there is nothing to render, and widening it would buy nothing.</p>"},{"location":"issues/v0.5/#resolving-the-open-questions","title":"Resolving the open questions","text":"<p>Version gate or opt-in flag? Opt-in flag. A version gate forces every caller to change on a schedule we set, to get a feature many do not want. <code>vision=False</code> costs one keyword for those who do. Recorded as ADR 0009 alongside ADR 0007, which it extends rather than amends.</p> <p>What happens on a text-only model? The caller declares the capability, via the same flag, and the library never guesses. There is no reliable way to ask a model whether it can see, and the integrations are handed a registry rather than a model. Handing an image block to a text-only deployment is an API error from the provider, which is exactly why this cannot be inferred. Tested as the default path in all three integrations.</p> <p>Which formats qualify? PNG, JPEG, GIF, WebP. PDF is excluded: some models read it and others reject it, so including it would make the correctness of a tool call depend on which model is behind it. SVG is excluded because it is text the model can already reason about \u2014 rasterising it would trade something it can think about for something it can only look at.</p> <p>Should <code>list_skill_resources</code> advertise renderability? No. Answering honestly means fetching and classifying every resource's bytes to serve a listing call; answering from the extension means sometimes promising a renderable image that is not one. It saves a single tool call and costs a read of everything.</p> <p>Per-modality limits. <code>max_inline_image_bytes</code> defaults to 5 MiB against 64 KiB for opaque binaries. The binary cap tracks tokens, because base64 in a text field is billed per byte; a native image is billed by tile count, so the same ceiling would have turned nearly every real screenshot into a stub saying it was too large. 5 MiB is the lowest per-image limit among the major vision APIs, so it is the one that is safe everywhere.</p>"},{"location":"issues/v0.5/#observations-for-later_5","title":"Observations for later","text":"<ul> <li>The example skill now ships two diagrams of the same thing, <code>escalation-flowchart.mermaid</code>   and <code>severity-ladder.png</code>, and only one of them is worth rendering. That contrast is the point,   but it is also a hint that skill authors will want guidance on when to bundle a picture at all.</li> <li>Oversized images fall back silently to a JSON envelope with <code>\"encoding\": \"none\"</code>. The model   is told the file was too large, which is honest but not actionable. Downscaling to fit the   ceiling would be more useful and needs an image library the SDK does not currently depend on.</li> <li><code>vision=True</code> is per-<code>get_tools()</code> call, so an application talking to two models with   different modalities builds two tool lists. That is correct but slightly awkward, and worth   revisiting if per-call capability negotiation ever becomes available.</li> </ul>"},{"location":"packages/adapters/","title":"Format Adapters (<code>agentskills-adapters</code>)","text":"<p>Import common agent instruction formats as native <code>agentskills-core</code> <code>Skill</code> objects.</p> <pre><code>pip install agentskills-adapters\n</code></pre> <p>Supported sources include <code>AGENTS.md</code>, Copilot instructions, Cursor <code>.mdc</code> rules, and Claude-style folders containing <code>SKILL.md</code>. Use <code>agentskills init --from</code> to convert one source into an editable native skill.</p>"},{"location":"packages/adapters/#agentskills_adapters","title":"agentskills_adapters","text":"<p>Adapters from common agent instruction formats to Agent Skills.</p>"},{"location":"packages/adapters/#agentskills_adapters.ImportedSkill","title":"ImportedSkill  <code>dataclass</code>","text":"<p>A native <code>Skill</code> plus the source and rendered <code>SKILL.md</code> text.</p>"},{"location":"packages/adapters/#agentskills_adapters.adapt_path","title":"adapt_path","text":"<pre><code>adapt_path(path: str | Path, *, name: str | None = None, description: str | None = None) -&gt; ImportedSkill\n</code></pre> <p>Adapt one supported file or Claude-style skill directory.</p> <p>Parameters:</p> Name Type Description Default <code>path</code> <code>str | Path</code> <p>An <code>AGENTS.md</code>, Copilot instructions file, Cursor <code>.mdc</code> rule, or directory containing <code>SKILL.md</code>.</p> required <code>name</code> <code>str | None</code> <p>Optional native skill name override.</p> <code>None</code> <code>description</code> <code>str | None</code> <p>Optional explicit catalog description.</p> <code>None</code> <p>Returns:</p> Type Description <code>ImportedSkill</code> <p>Imported skill handle, source path, and native <code>SKILL.md</code> text.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the source shape is unsupported or metadata is invalid.</p>"},{"location":"packages/adapters/#agentskills_adapters.discover_sources","title":"discover_sources","text":"<pre><code>discover_sources(root: str | Path) -&gt; list[Path]\n</code></pre> <p>Find supported instruction sources below root without duplicates.</p>"},{"location":"packages/adapters/#agentskills_adapters.render_skill_md","title":"render_skill_md","text":"<pre><code>render_skill_md(imported: ImportedSkill) -&gt; str\n</code></pre> <p>Return the native <code>SKILL.md</code> text for an imported skill.</p>"},{"location":"packages/agentframework/","title":"Agent Framework Integration (<code>agentskills-agentframework</code>)","text":"<p>Integrates skills into Microsoft Agent Framework.</p> <pre><code>pip install agentskills-agentframework\n</code></pre>"},{"location":"packages/agentframework/#agentskills_agentframework","title":"agentskills_agentframework","text":"<p>Microsoft Agent Framework integration for Agent Skills.</p> <p>This package bridges :mod:<code>agentskills_core</code> and <code>Microsoft Agent Framework &lt;https://github.com/microsoft/agent-framework&gt;</code>_, providing:</p> <ul> <li>:class:<code>AgentSkillsContextProvider</code> -- a   :class:<code>agent_framework.ContextProvider</code> that automatically   injects skill awareness into an agent's session context.</li> <li>:func:<code>get_tools</code> -- generates eight   :class:<code>~agent_framework.FunctionTool</code> instances that let an   AI agent consume skills.</li> <li>:func:<code>get_tools_usage_instructions</code> -- returns agent-facing   instructions explaining how to use the tools.</li> <li>:data:<code>METADATA_LOADED_SKILLS</code> -- the <code>context.metadata</code> key under   which the provider publishes the skills loaded so far this session,   for other context providers to read.</li> </ul> <p>Install::</p> <pre><code>pip install agentskills-agentframework\n</code></pre>"},{"location":"packages/agentframework/#agentskills_agentframework.AgentSkillsContextProvider","title":"AgentSkillsContextProvider","text":"<p>               Bases: <code>ContextProvider</code></p> <p>Expose a :class:<code>~agentskills_core.SkillRegistry</code> to an Agent Framework agent.</p> <p>Wraps a registry of any-backend skills and hooks into the agent lifecycle to supply three things at the start of every run:</p> <ul> <li>Skills catalog \u2014 a compact listing of registered skill   names and descriptions, appended to the session prompt.</li> <li>Typed tool set \u2014 eight <code>FunctionTool</code> instances that let the   agent fetch metadata, full instructions, outlines, sections,   references, scripts, and assets individually.</li> <li>Tools usage instructions \u2014 guidance on the progressive-disclosure   workflow so the agent knows how and when to invoke each tool.</li> </ul> <p>Across a multi-turn session it also:</p> <ul> <li>Caches the assembled prompt in <code>state</code>, keyed by the registry's   skill set and the skills already loaded, so an unchanged session   does no registry I/O after the first turn.</li> <li>Records in <code>state[\"loaded_skills\"]</code> every skill whose full body   the agent fetched, and prunes those entries from the catalog on   later turns \u2014 their instructions are already in the conversation,   so advertising them again is paid for twice.</li> <li>Publishes the loaded set on <code>context.metadata</code> under   <code>\"agentskills_loaded_skills\"</code> so other context providers can   see what has been put in front of the model.</li> </ul> <p>The tool surface intentionally uses typed resource tools (<code>get_skill_reference</code>, <code>get_skill_script</code>, <code>get_skill_asset</code>) rather than a single generic reader so that the agent has clear semantic signal about the kind of content it is requesting.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The :class:<code>~agentskills_core.SkillRegistry</code> whose skills should be exposed.</p> required <p>Other Parameters:</p> Name Type Description <code>skills_instruction_prompt</code> <code>str | None</code> <p>Custom prompt template. Must contain <code>{skills_catalog}</code> and <code>{tools_usage_instructions}</code> placeholders. When <code>None</code>, a sensible default is used.</p> <code>skills_catalog_format</code> <code>Literal['xml', 'markdown']</code> <p>Format for the skills catalog \u2014 <code>\"xml\"</code> (default) or <code>\"markdown\"</code>.</p> <code>cache_prompt</code> <code>bool</code> <p>Reuse the assembled prompt across runs within a session instead of rebuilding it from the registry every turn. Pass <code>False</code> to rebuild each time.</p> <code>prune_loaded_skills</code> <code>bool</code> <p>Drop the catalog entry of any skill whose full body the agent has already fetched this session, replacing it with a one-line reminder. Pass <code>False</code> to advertise the whole catalog on every turn.</p> <code>fast_path</code> <code>FastPath | None</code> <p>A :class:<code>~agentskills_core.FastPath</code> from :func:<code>~agentskills_core.resolve_fast_path</code>. When given, the skill's body is injected directly and the catalog, the usage instructions and the four body-access tools are dropped \u2014 there is nothing to choose between, so there is no reason to spend a model turn choosing. Resource tools remain. Resolve it again if the registry changes.</p> <code>source_id</code> <code>str | None</code> <p>Unique identifier for this provider instance.</p> <p>Example::</p> <pre><code>from agentskills_agentframework import AgentSkillsContextProvider\nfrom agentskills_core import SkillRegistry\n\nregistry = SkillRegistry()\nawait registry.register(\"incident-response\", provider)\n\nskills_context_provider = AgentSkillsContextProvider(registry)\n\nagent = client.as_agent(\n    name=\"SREAssistant\",\n    instructions=\"You are an SRE assistant.\",\n    context_providers=[skills_context_provider],\n)\nresponse = await agent.run(\"What severity is a full DB outage?\")\n</code></pre>"},{"location":"packages/agentframework/#agentskills_agentframework.AgentSkillsContextProvider.after_run","title":"after_run  <code>async</code>","text":"<pre><code>after_run(*, agent: SupportsAgentRun, session: AgentSession, context: SessionContext, state: dict[str, Any]) -&gt; None\n</code></pre> <p>Record which skill bodies the agent pulled during this run.</p> <p>Reads the agent's own response rather than wrapping the tools, so a caller who passed :func:<code>~agentskills_agentframework.get_tools</code> output in directly is tracked the same way.</p> <p>Parameters:</p> Name Type Description Default <code>agent</code> <code>SupportsAgentRun</code> <p>The agent that ran this invocation.</p> required <code>session</code> <code>AgentSession</code> <p>The active session.</p> required <code>context</code> <code>SessionContext</code> <p>The run context, with <code>response</code> populated.</p> required <code>state</code> <code>dict[str, Any]</code> <p>Provider-scoped mutable state persisted across runs.</p> required"},{"location":"packages/agentframework/#agentskills_agentframework.AgentSkillsContextProvider.before_run","title":"before_run  <code>async</code>","text":"<pre><code>before_run(*, agent: SupportsAgentRun, session: AgentSession, context: SessionContext, state: dict[str, Any]) -&gt; None\n</code></pre> <p>Append skills catalog, tools, and usage instructions to the run context.</p> <p>Does nothing when the registry is empty, so agents without any registered skills pay no prompt-budget cost.</p> <p>Calling this twice with the same <code>context</code> is a no-op the second time: the context is marked once contributed to, so a pipeline that retries a run cannot double the prompt.</p> <p>Parameters:</p> Name Type Description Default <code>agent</code> <code>SupportsAgentRun</code> <p>The agent starting this run.</p> required <code>session</code> <code>AgentSession</code> <p>The active session.</p> required <code>context</code> <code>SessionContext</code> <p>Mutable run context to extend.</p> required <code>state</code> <code>dict[str, Any]</code> <p>Provider-scoped mutable state persisted across runs.</p> required"},{"location":"packages/agentframework/#agentskills_agentframework.get_tools","title":"get_tools","text":"<pre><code>get_tools(registry: SkillRegistry, *, max_inline_binary_bytes: int = DEFAULT_MAX_INLINE_BINARY_BYTES, fast_path: FastPath | None = None, vision: bool = False, max_inline_image_bytes: int = DEFAULT_MAX_INLINE_IMAGE_BYTES) -&gt; list[FunctionTool]\n</code></pre> <p>Build Agent Framework tools that expose an Agent Skills registry.</p> <p>Each tool wraps a :class:<code>~agentskills_core.SkillRegistry</code> or :class:<code>~agentskills_core.Skill</code> method, serialising the result as JSON (for dicts) or plain text (for content bodies). Tools are read-only -- they retrieve content but never execute scripts or modify state.</p> <p>Skill discovery is handled via the catalog in the system prompt, so no <code>list_skills</code> tool is included.</p> <p>All tools are async coroutines.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The :class:<code>~agentskills_core.SkillRegistry</code> whose skills should be exposed as tools.</p> required <code>max_inline_binary_bytes</code> <code>int</code> <p>Size ceiling for inlining binary resources as base64.  Larger resources are described but not returned.  See :func:<code>~agentskills_core.encode_resource_content</code>.</p> <code>DEFAULT_MAX_INLINE_BINARY_BYTES</code> <code>fast_path</code> <code>FastPath | None</code> <p>A :class:<code>~agentskills_core.FastPath</code> from :func:<code>~agentskills_core.resolve_fast_path</code>.  When given, the four body-access tools are omitted, because the body is already in the prompt.  Resource tools remain.</p> <code>None</code> <code>vision</code> <code>bool</code> <p>When <code>True</code>, bundled images small enough to inline are returned as native <code>data</code> content instead of a base64 JSON envelope.  Off by default: handing an image to a text-only model is an API error, not a degraded answer, so the caller declares the capability rather than the library guessing it.</p> <code>False</code> <code>max_inline_image_bytes</code> <code>int</code> <p>Size ceiling for native images.  Only consulted when <code>vision</code> is <code>True</code>.  Larger images fall back to the JSON envelope.</p> <code>DEFAULT_MAX_INLINE_IMAGE_BYTES</code> <p>Returns:</p> Type Description <code>list[FunctionTool]</code> <p>A list of :class:<code>~agent_framework.FunctionTool</code></p> <code>list[FunctionTool]</code> <p>instances ready to be passed to an Agent Framework agent.</p>"},{"location":"packages/agentframework/#agentskills_agentframework.get_tools_usage_instructions","title":"get_tools_usage_instructions","text":"<pre><code>get_tools_usage_instructions() -&gt; str\n</code></pre> <p>Return agent instructions for using the Agent Skills tools.</p> <p>This text explains to an AI agent how to use the skill tools (<code>get_skill_metadata</code>, <code>get_skill_body</code>, <code>get_skill_reference</code>, <code>get_skill_script</code>, <code>get_skill_asset</code>) following the progressive-disclosure workflow.</p> <p>Combine with the skill catalog produced by :meth:<code>SkillRegistry.get_skills_catalog</code> to give the agent both what skills are available and how to interact with them::</p> <pre><code>catalog = await registry.get_skills_catalog(format=\"xml\")\ninstructions = get_tools_usage_instructions()\nsystem_prompt = f\"{catalog}\\n\\n{instructions}\"\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>A multi-line instruction string ready for system-prompt</p> <code>str</code> <p>insertion.</p>"},{"location":"packages/core/","title":"Core (<code>agentskills-core</code>)","text":"<p>Registry, provider contract, validation, and shared exceptions.</p> <pre><code>pip install agentskills-core\n</code></pre>"},{"location":"packages/core/#agentskills_core","title":"agentskills_core","text":"<p>Core runtime model for the Agent Skills format.</p> <p>This package provides the foundational abstractions for registering, validating, and accessing <code>Agent Skills &lt;https://agentskills.io&gt;</code>_:</p> <ul> <li>:class:<code>SkillProvider</code> -- abstract base class for skill backends.</li> <li>:class:<code>Skill</code> -- lightweight runtime handle to a single skill.</li> <li>:class:<code>SkillRegistry</code> -- unified index with explicit registration   and built-in catalog builder.</li> <li>:func:<code>validate_skill</code> -- validates a skill against the specification.</li> <li>:func:<code>validate_version</code> -- validates an optional semver <code>version</code> field.</li> <li>:func:<code>get_logger</code> -- returns a logger in the shared <code>agentskills.*</code> namespace.</li> <li>:func:<code>redact_url</code> -- strips credentials from a URL before it is logged or raised.</li> <li>:func:<code>encode_resource_content</code> -- safely encodes resource bytes as tool output.</li> <li>:class:<code>SkillNotFoundError</code> -- raised when a skill does not exist.</li> <li>:class:<code>ResourceNotFoundError</code> -- raised when a resource within a skill   does not exist.</li> <li>:class:<code>ResourceListingNotSupportedError</code> -- raised when a provider cannot   enumerate a skill's resources.</li> <li>:class:<code>DiscoveryNotSupportedError</code> -- raised when a provider cannot   enumerate the skills it holds.</li> <li>:class:<code>SkillUnavailableError</code> -- raised when a backend is unreachable or   fails transiently, as distinct from a skill that does not exist.</li> <li>:class:<code>AgentSkillsError</code> -- base class for all library exceptions.</li> <li>:class:<code>AgentSkillsError</code> -- base class for all library exceptions.</li> </ul> <p>Install::</p> <pre><code>pip install agentskills-core\n</code></pre>"},{"location":"packages/core/#agentskills_core.AgentSkillsError","title":"AgentSkillsError","text":"<p>               Bases: <code>Exception</code></p> <p>Base exception for all Agent Skills library errors.</p>"},{"location":"packages/core/#agentskills_core.DiscoveryNotSupportedError","title":"DiscoveryNotSupportedError","text":"<p>               Bases: <code>AgentSkillsError</code>, <code>NotImplementedError</code></p> <p>The provider cannot enumerate the skills it holds.</p> <p>Raised by the default :meth:<code>SkillProvider.discover &lt;agentskills_core.SkillProvider.discover&gt;</code> implementation, and by providers whose backend offers no index -- a static HTTP host with no skill manifest, for instance.</p> <p>Like :class:<code>ResourceListingNotSupportedError</code>, this is deliberately not an empty list.  A caller told a backend holds no skills stops looking; a caller told the backend cannot be enumerated falls back to explicit registration.  Check :attr:<code>SkillProvider.supports_discovery &lt;agentskills_core.SkillProvider.supports_discovery&gt;</code> to avoid the exception entirely.</p> <p>Example::</p> <pre><code>if provider.supports_discovery:\n    await registry.register_all(provider)\n</code></pre>"},{"location":"packages/core/#agentskills_core.FastPath","title":"FastPath  <code>dataclass</code>","text":"<p>A resolved single-skill fast path.</p> <p>Passed to an integration's <code>fast_path=</code> argument.  Holding the decision in a value rather than re-deriving it means the prompt an integration injects and the tools it drops cannot disagree about which skill was chosen.</p>"},{"location":"packages/core/#agentskills_core.FastPath.body","title":"body  <code>instance-attribute</code>","text":"<pre><code>body: str\n</code></pre> <p>That skill's full body, frontmatter already stripped.</p>"},{"location":"packages/core/#agentskills_core.FastPath.prompt","title":"prompt  <code>property</code>","text":"<pre><code>prompt: str\n</code></pre> <p>The instruction block to inject instead of a catalog.</p>"},{"location":"packages/core/#agentskills_core.FastPath.skill_id","title":"skill_id  <code>instance-attribute</code>","text":"<pre><code>skill_id: str\n</code></pre> <p>The one skill whose body is being inlined.</p>"},{"location":"packages/core/#agentskills_core.FastPath.tokens","title":"tokens  <code>instance-attribute</code>","text":"<pre><code>tokens: int\n</code></pre> <p>Estimated cost of the body, by the counter the outline uses.</p>"},{"location":"packages/core/#agentskills_core.FastPath.keeps","title":"keeps","text":"<pre><code>keeps(tool_name: str) -&gt; bool\n</code></pre> <p>Whether tool_name should still be offered to the agent.</p>"},{"location":"packages/core/#agentskills_core.ResourceListingNotSupportedError","title":"ResourceListingNotSupportedError","text":"<p>               Bases: <code>AgentSkillsError</code>, <code>NotImplementedError</code></p> <p>The provider cannot enumerate a skill's resources.</p> <p>Raised by the default :meth:<code>SkillProvider.list_resources &lt;agentskills_core.SkillProvider.list_resources&gt;</code> implementation, and by providers whose backend offers no way to enumerate -- a static HTTP host with no manifest, for instance.</p> <p>This is deliberately not an empty result.  \"Cannot enumerate\" and \"enumerated, found nothing\" lead a caller to different behaviour, so they must be distinguishable.  Check :attr:<code>SkillProvider.supports_resource_listing &lt;agentskills_core.SkillProvider.supports_resource_listing&gt;</code> to avoid the exception entirely.</p> <p>Example::</p> <pre><code>if provider.supports_resource_listing:\n    resources = await provider.list_resources(\"incident-response\")\n</code></pre>"},{"location":"packages/core/#agentskills_core.ResourceMedia","title":"ResourceMedia  <code>dataclass</code>","text":"<p>What a resource is, and whether a model can be shown it.</p>"},{"location":"packages/core/#agentskills_core.ResourceMedia.is_text","title":"is_text  <code>instance-attribute</code>","text":"<pre><code>is_text: bool\n</code></pre> <p>Whether this should be treated as text needing no special handling.</p> <p>False for anything whose leading bytes identify a renderable image, whatever those bytes happen to decode to -- a file that starts <code>GIF89a</code> is a GIF, and a small one can decode as UTF-8 by coincidence.</p>"},{"location":"packages/core/#agentskills_core.ResourceMedia.media_type","title":"media_type  <code>instance-attribute</code>","text":"<pre><code>media_type: str\n</code></pre> <p>Detected media type, or <code>application/octet-stream</code>.</p>"},{"location":"packages/core/#agentskills_core.ResourceMedia.name","title":"name  <code>instance-attribute</code>","text":"<pre><code>name: str\n</code></pre> <p>The resource file name, as requested.</p>"},{"location":"packages/core/#agentskills_core.ResourceMedia.renderable","title":"renderable  <code>instance-attribute</code>","text":"<pre><code>renderable: bool\n</code></pre> <p>Whether this should be delivered as a native content block.</p> <p>False for text, for unrecognised binaries, and for images past :data:<code>DEFAULT_MAX_INLINE_IMAGE_BYTES</code> -- in every case the JSON envelope remains correct, so a caller can ignore this field and lose nothing but the improvement.</p>"},{"location":"packages/core/#agentskills_core.ResourceMedia.size_bytes","title":"size_bytes  <code>instance-attribute</code>","text":"<pre><code>size_bytes: int\n</code></pre> <p>Size of the resource.</p>"},{"location":"packages/core/#agentskills_core.ResourceNotFoundError","title":"ResourceNotFoundError","text":"<p>               Bases: <code>AgentSkillsError</code>, <code>LookupError</code></p> <p>A requested resource does not exist within a skill.</p> <p>Resources include scripts, assets, and reference documents bundled with a skill.</p> <p>Raised by <code>get_script</code>, <code>get_asset</code>, and <code>get_reference</code> on both :class:<code>~agentskills_core.SkillProvider</code> and :class:<code>~agentskills_core.Skill</code> when the named resource cannot be found.</p> <p>Example::</p> <pre><code>try:\n    data = await skill.get_reference(\"missing.md\")\nexcept ResourceNotFoundError:\n    print(\"Reference not found\")\n</code></pre>"},{"location":"packages/core/#agentskills_core.Section","title":"Section  <code>dataclass</code>","text":"<p>One heading of a body, with the text charged to it.</p> <p>Attributes:</p> Name Type Description <code>key</code> <code>str</code> <p>Stable, addressable identifier, unique within one body.</p> <code>title</code> <code>str</code> <p>The heading text, or :data:<code>PREAMBLE_TITLE</code>.</p> <code>level</code> <code>int</code> <p>Number of leading <code>#</code>, or <code>0</code> for the preamble.</p> <code>text</code> <code>str</code> <p>The heading line and everything up to the next heading.</p>"},{"location":"packages/core/#agentskills_core.SectionNotFoundError","title":"SectionNotFoundError","text":"<p>               Bases: <code>AgentSkillsError</code>, <code>LookupError</code></p> <p>A requested section does not exist within a skill's body.</p> <p>Raised by :meth:<code>Skill.get_section &lt;agentskills_core.Skill.get_section&gt;</code> and :meth:<code>SkillRegistry.get_skill_section &lt;agentskills_core.SkillRegistry.get_skill_section&gt;</code> when the given key does not match any heading.  Section keys are derived from the body, so they change when the body does; the message lists the keys that are currently valid.</p> <p>Example::</p> <pre><code>try:\n    text = await skill.get_section(\"rollbck\")\nexcept SectionNotFoundError as exc:\n    print(exc)   # names the keys that do exist\n</code></pre>"},{"location":"packages/core/#agentskills_core.SectionRef","title":"SectionRef  <code>dataclass</code>","text":"<p>One line of an outline: what a section is, and what it costs.</p>"},{"location":"packages/core/#agentskills_core.Skill","title":"Skill","text":"<p>Runtime handle to a single Agent Skill.</p> <p>All data access is delegated to the backing :class:<code>~agentskills_core.SkillProvider</code>.  The handle itself stores only the skill name and a provider reference -- no content is cached, and no execution logic is included.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name (must match the <code>name</code> field in the skill's YAML frontmatter).</p> required <code>provider</code> <code>SkillProvider</code> <p>The :class:<code>~agentskills_core.SkillProvider</code> that owns this skill.</p> required <p>Example::</p> <pre><code>skill = registry.get_skill(\"incident-response\")\nmeta = await skill.get_metadata()\nprint(meta[\"description\"])\nbody = await skill.get_body()\n</code></pre>"},{"location":"packages/core/#agentskills_core.Skill.supports_resource_listing","title":"supports_resource_listing  <code>property</code>","text":"<pre><code>supports_resource_listing: bool\n</code></pre> <p>Whether the backing provider can enumerate this skill's resources.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_asset","title":"get_asset  <code>async</code>","text":"<pre><code>get_asset(name: str) -&gt; bytes\n</code></pre> <p>Return the raw content of a bundled asset.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the asset to retrieve.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the asset does not exist.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_body","title":"get_body  <code>async</code>","text":"<pre><code>get_body() -&gt; str\n</code></pre> <p>Return the markdown instruction body for this skill.</p> <p>This represents the full skill instructions that an agent reads upon activation -- the content after the YAML frontmatter in the Agent Skills format.</p> <p>Returns:</p> Type Description <code>str</code> <p>Markdown text.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_id","title":"get_id","text":"<pre><code>get_id() -&gt; str\n</code></pre> <p>Return the unique skill name, matching the frontmatter <code>name</code>.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_metadata","title":"get_metadata  <code>async</code>","text":"<pre><code>get_metadata() -&gt; dict[str, Any]\n</code></pre> <p>Return the parsed YAML frontmatter for this skill.</p> <p>Always contains the required <code>name</code> and <code>description</code> keys. May also include optional keys such as <code>license</code>, <code>compatibility</code>, <code>metadata</code>, and <code>allowed-tools</code>.</p> <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary of frontmatter key-value pairs.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_outline","title":"get_outline  <code>async</code>","text":"<pre><code>get_outline() -&gt; SkillOutline\n</code></pre> <p>Return the body's sections without returning the body.</p> <p>Computed on top of :meth:<code>get_body</code>, which providers already cache, so this costs no extra round-trip on a warm provider and needs nothing from the :class:<code>~agentskills_core.SkillProvider</code> interface.</p> <p>Returns:</p> Name Type Description <code>A</code> <code>SkillOutline</code> <p>class:<code>~agentskills_core.SkillOutline</code> carrying one</p> <code>SkillOutline</code> <p>class:<code>~agentskills_core.SectionRef</code> per heading, the</p> <code>SkillOutline</code> <p>whole body's estimated size, and whether fetching that</p> <code>SkillOutline</code> <p>whole body is the cheaper move.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_reference","title":"get_reference  <code>async</code>","text":"<pre><code>get_reference(name: str) -&gt; bytes\n</code></pre> <p>Return the raw content of a bundled reference document.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the reference to retrieve.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the reference does not exist.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_script","title":"get_script  <code>async</code>","text":"<pre><code>get_script(name: str) -&gt; bytes\n</code></pre> <p>Return the raw content of a bundled script.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Name of the script to retrieve.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the script does not exist.</p>"},{"location":"packages/core/#agentskills_core.Skill.get_section","title":"get_section  <code>async</code>","text":"<pre><code>get_section(key: str) -&gt; str\n</code></pre> <p>Return one section of the body, addressed by key.</p> <p>Parameters:</p> Name Type Description Default <code>key</code> <code>str</code> <p>A key from :meth:<code>get_outline</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The heading line and everything up to the next heading of</p> <code>str</code> <p>any level.  Sections do not overlap, so a parent does not</p> <code>str</code> <p>include its subsections.</p> <p>Raises:</p> Type Description <code>SectionNotFoundError</code> <p>If no section carries that key.</p>"},{"location":"packages/core/#agentskills_core.Skill.list_resources","title":"list_resources  <code>async</code>","text":"<pre><code>list_resources() -&gt; dict[str, list[str]]\n</code></pre> <p>Return this skill's resource names, grouped by kind.</p> <p>Check :attr:<code>supports_resource_listing</code> first -- not every backend can enumerate.</p> <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>Mapping of <code>\"references\"</code> / <code>\"scripts\"</code> / <code>\"assets\"</code> to</p> <code>dict[str, list[str]]</code> <p>sorted resource names.</p> <p>Raises:</p> Type Description <code>ResourceListingNotSupportedError</code> <p>If the provider cannot enumerate resources.</p>"},{"location":"packages/core/#agentskills_core.SkillNotFoundError","title":"SkillNotFoundError","text":"<p>               Bases: <code>AgentSkillsError</code>, <code>LookupError</code></p> <p>A requested skill does not exist.</p> <p>Raised by :class:<code>~agentskills_core.SkillProvider</code> methods when the given skill_id cannot be resolved, and by :meth:<code>SkillRegistry.get_skill &lt;agentskills_core.SkillRegistry.get_skill&gt;</code> when the skill is not in the index.</p> <p>Example::</p> <pre><code>try:\n    skill = await registry.get_skill(\"nonexistent\")\nexcept SkillNotFoundError:\n    print(\"Skill not found\")\n</code></pre>"},{"location":"packages/core/#agentskills_core.SkillOutline","title":"SkillOutline  <code>dataclass</code>","text":"<p>A body's sections, their cost, and whether they are worth fetching.</p> <p>A section fetch is not automatically cheaper than a body fetch: it costs a tool call, a model turn, and the tokens of the outline that preceded it.  The outline therefore carries the whole-body figure and says plainly which is cheaper, because shipping the split without that guidance makes the common case worse to improve the rare one.</p>"},{"location":"packages/core/#agentskills_core.SkillOutline.whole_body_is_cheaper","title":"whole_body_is_cheaper  <code>property</code>","text":"<pre><code>whole_body_is_cheaper: bool\n</code></pre> <p>Whether the agent should skip sectioning and read the body.</p>"},{"location":"packages/core/#agentskills_core.SkillOutline.render","title":"render","text":"<pre><code>render() -&gt; str\n</code></pre> <p>Return the outline as text for an agent to read.</p> <p>One rendering, shared by every integration, so the three cannot drift into giving an agent three different cost estimates for the same skill.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider","title":"SkillProvider","text":"<p>               Bases: <code>ABC</code></p> <p>Abstract base class that every skill backend must implement.</p> <p>A :class:<code>SkillProvider</code> is a pure content accessor -- it serves skill metadata, body text, and resources by skill ID.  Enumerating the skills a backend holds is an optional capability: see :meth:<code>discover</code>.  Without it, registration is explicit via :meth:<code>SkillRegistry.register &lt;agentskills_core.SkillRegistry.register&gt;</code>.</p> <p>Implementations must enforce progressive disclosure: expensive I/O (reading file bodies, fetching resources) should only happen when the corresponding method is called explicitly.</p> <p>All methods are <code>async</code> to support non-blocking implementations.</p> <p>Subclass this to back skills with any storage -- filesystem, database, remote API, etc.  Register skills with a :class:<code>~agentskills_core.SkillRegistry</code> to create a unified skill catalog.</p> <p>Example::</p> <pre><code>class MyProvider(SkillProvider):\n    async def get_metadata(self, skill_id: str) -&gt; dict: ...\n    async def get_body(self, skill_id: str) -&gt; str: ...\n    # ... remaining abstract methods\n</code></pre>"},{"location":"packages/core/#agentskills_core.SkillProvider.discover","title":"discover  <code>async</code>","text":"<pre><code>discover() -&gt; list[str]\n</code></pre> <p>Return the IDs of every skill this provider can serve.</p> <p>Optional capability.  The default implementation refuses. Override it and set :attr:<code>supports_discovery</code> to <code>True</code> in backends that can enumerate.</p> <p>Implementations return sorted IDs, and return only skills that appear to exist -- a directory without a <code>SKILL.md</code> is not a skill.  Discovery does not validate: an ID that comes back here can still fail :func:<code>~agentskills_core.validate_skill</code>, which is what :meth:<code>SkillRegistry.register_all &lt;agentskills_core.SkillRegistry.register_all&gt;</code> reports on.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted skill IDs.  An empty list means the backend was</p> <code>list[str]</code> <p>enumerated and holds nothing.</p> <p>Raises:</p> Type Description <code>DiscoveryNotSupportedError</code> <p>If this provider cannot enumerate its skills.  Never returns an empty list to signal the same thing -- that would be indistinguishable from an empty backend, and would silently register nothing.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider.get_asset","title":"get_asset  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_asset(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Return the raw bytes of a single asset.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name containing the asset.</p> required <code>name</code> <code>str</code> <p>Name of the asset to retrieve.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content as bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the asset does not exist.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider.get_body","title":"get_body  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_body(skill_id: str) -&gt; str\n</code></pre> <p>Return the markdown body (instructions) for a skill.</p> <p>The body contains the skill's full instructions in Markdown format, corresponding to the content after the YAML frontmatter in the Agent Skills format.  It should only be loaded when the agent decides to activate the skill (progressive disclosure).</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name to look up.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The markdown instruction text.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill does not exist.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider.get_metadata","title":"get_metadata  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_metadata(skill_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Return the parsed YAML frontmatter for a skill.</p> <p>Per the specification the returned dict will always contain at least the required <code>name</code> and <code>description</code> keys.  Optional keys include <code>license</code>, <code>compatibility</code>, <code>metadata</code>, and <code>allowed-tools</code>.</p> <p>This method should return only the frontmatter metadata -- never the full instruction body -- to keep context usage low during the discovery phase.</p> <p>The returned dict must not be shared between calls: a caller that mutates it must not affect the next caller.  Implementations that cache a parsed dict should return a copy.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name to look up.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary of frontmatter key-value pairs.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill does not exist.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider.get_reference","title":"get_reference  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_reference(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Return the raw bytes of a single reference document.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name containing the reference.</p> required <code>name</code> <code>str</code> <p>Name of the reference to retrieve.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content as bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the reference does not exist.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider.get_script","title":"get_script  <code>abstractmethod</code> <code>async</code>","text":"<pre><code>get_script(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Return the raw bytes of a single script.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name containing the script.</p> required <code>name</code> <code>str</code> <p>Name of the script to retrieve.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content as bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the script does not exist.</p>"},{"location":"packages/core/#agentskills_core.SkillProvider.list_resources","title":"list_resources  <code>async</code>","text":"<pre><code>list_resources(skill_id: str) -&gt; dict[str, list[str]]\n</code></pre> <p>Return the resource names a skill contains, grouped by kind.</p> <p>Optional capability.  The default implementation refuses. Override it and set :attr:<code>supports_resource_listing</code> to <code>True</code> in backends that can enumerate.</p> <p>Implementations return every key in :data:<code>RESOURCE_KINDS</code>, using an empty list for a category the skill does not use, so callers never have to guard on key presence.  Names are sorted.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>The skill name to enumerate.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>Mapping of <code>\"references\"</code> / <code>\"scripts\"</code> / <code>\"assets\"</code> to</p> <code>dict[str, list[str]]</code> <p>sorted resource names.</p> <p>Raises:</p> Type Description <code>ResourceListingNotSupportedError</code> <p>If this provider cannot enumerate resources.  Never returns an empty mapping to signal the same thing -- that would be indistinguishable from a skill with no resources.</p> <code>SkillNotFoundError</code> <p>If the skill does not exist.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry","title":"SkillRegistry","text":"<p>Unified index over explicitly registered skills.</p> <p>Skills are added via :meth:<code>register</code>, either one at a time or as a batch of <code>(skill_id, provider)</code> tuples, or via :meth:<code>register_all</code> for a provider that can enumerate itself.  The registry enforces a flat namespace: each skill ID must be unique.  A :exc:<code>ValueError</code> is raised if a duplicate is detected.</p> <p>:meth:<code>register</code> is <code>async</code> because it validates every skill against the Agent Skills specification before storing it. :meth:<code>list_skills</code> and :meth:<code>get_skill</code> are synchronous lookups. :meth:<code>list_skills</code> returns :class:<code>~agentskills_core.Skill</code> instances sorted by ID. :meth:<code>get_skills_catalog</code> is <code>async</code> because it fetches metadata from providers.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.__init__","title":"__init__","text":"<pre><code>__init__(*, catalog_concurrency: int = DEFAULT_CATALOG_CONCURRENCY) -&gt; None\n</code></pre> <p>Create an empty registry.</p> <p>Parameters:</p> Name Type Description Default <code>catalog_concurrency</code> <code>int</code> <p>Maximum number of provider metadata fetches issued in parallel by :meth:<code>get_skills_catalog</code>.</p> <code>DEFAULT_CATALOG_CONCURRENCY</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If catalog_concurrency is less than 1.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.get_skill","title":"get_skill","text":"<pre><code>get_skill(skill_id: str) -&gt; Skill\n</code></pre> <p>Return the :class:<code>~agentskills_core.Skill</code> handle by name.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <p>Returns:</p> Type Description <code>Skill</code> <p>The registered :class:<code>~agentskills_core.Skill</code> instance.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If no skill with the given name is registered.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.get_skill_outline","title":"get_skill_outline  <code>async</code>","text":"<pre><code>get_skill_outline(skill_id: str) -&gt; SkillOutline\n</code></pre> <p>Return a registered skill's body outline.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <p>Returns:</p> Type Description <code>SkillOutline</code> <p>The skill's :class:<code>~agentskills_core.SkillOutline</code>.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If no skill with the given name is registered.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.get_skill_section","title":"get_skill_section  <code>async</code>","text":"<pre><code>get_skill_section(skill_id: str, key: str) -&gt; str\n</code></pre> <p>Return one section of a registered skill's body.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <code>key</code> <code>str</code> <p>A key from :meth:<code>get_skill_outline</code>.</p> required <p>Returns:</p> Type Description <code>str</code> <p>The section text.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If no skill with the given name is registered.</p> <code>SectionNotFoundError</code> <p>If the skill has no section with that key.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.get_skills_catalog","title":"get_skills_catalog  <code>async</code>","text":"<pre><code>get_skills_catalog(*, format: Literal['xml', 'markdown'] = 'xml', tags: Iterable[str] | None = None, include: Iterable[str] | None = None, exclude: Iterable[str] | None = None, max_chars: int | None = None, selection_hints: bool = True, total: int | None = None) -&gt; str\n</code></pre> <p>Build a skill-catalog string for system-prompt injection.</p> <p>Two output formats are supported:</p> <p><code>\"xml\"</code>     An <code>&lt;available_skills&gt;</code> XML block.  This is the     recommended format when using Claude or other     Anthropic models.</p> <p><code>\"markdown\"</code>     A human-readable Markdown catalog listing every registered     skill's name and description.</p> <p>Only <code>name</code>, <code>description</code> and the selection metadata are extracted from each skill's metadata, keeping token usage low.</p> <p>The catalog is injected into every system prompt on every turn, so its size is a fixed cost per request.  The filters below narrow it, and max_chars caps it::</p> <pre><code>await registry.get_skills_catalog(\n    tags=[\"incident\"],\n    exclude=[\"deprecated-runbook\"],\n    max_chars=8000,\n)\n</code></pre> <p>include and exclude match skill IDs and are applied before any metadata is fetched, so narrowing a large registry costs proportionally fewer provider round-trips.  tags needs metadata and is applied after.</p> <p>Parameters:</p> Name Type Description Default <code>format</code> <code>Literal['xml', 'markdown']</code> <p>Output format \u2014 <code>\"xml\"</code> (default) or <code>\"markdown\"</code>.</p> <code>'xml'</code> <code>tags</code> <code>Iterable[str] | None</code> <p>Keep only skills carrying at least one of these tags, read from the spec's free-form <code>metadata</code> mapping under <code>tags</code>.  Matching is case-insensitive.  A skill with no tags matches no tag filter.</p> <code>None</code> <code>include</code> <code>Iterable[str] | None</code> <p>Allow-list of skill IDs.  Only these are considered.  An ID that is not registered raises, since an allow-list naming a skill that does not exist silently costs the agent a capability.</p> <code>None</code> <code>exclude</code> <code>Iterable[str] | None</code> <p>Deny-list of skill IDs, applied after include and winning over it \u2014 a deny-list that another argument can override is not a deny-list.  Unregistered IDs are ignored here, because a deny-list is meant to outlive the thing it denies.</p> <code>None</code> <code>max_chars</code> <code>int | None</code> <p>Hard ceiling on the length of the returned string, including the truncation note.  Whole entries are dropped from the end until the result fits, so the output stays valid and the same arguments always produce the same catalog.  Roughly four characters per token is the usual estimate.</p> <code>None</code> <code>selection_hints</code> <code>bool</code> <p>Include each skill's <code>when_to_use</code> and <code>when_not_to_use</code> entries.  They make selection more accurate and they are charged on every turn, so pass <code>False</code> to trade that accuracy back for tokens.  The result is then byte-identical to a catalog built from skills that declare neither field.</p> <code>True</code> <code>total</code> <code>int | None</code> <p>The size of the set these skills were chosen from, for a caller that has already narrowed the catalog itself \u2014 a ranked subset, say.  The catalog then reports the shortfall the same way it reports truncation, so a skill that was ranked out is not indistinguishable from one that was never registered.  Defaults to the number of entries left after this call's own filters, which is to say no shortfall is reported for them: a static <code>tags=</code> filter is a deliberate, unchanging choice, and repeating it in the prompt every turn is noise the caller is charged for.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>A string ready for insertion into a system prompt.  When</p> <code>str</code> <p>entries were dropped, the XML root carries <code>truncated</code>,</p> <code>str</code> <p><code>shown</code> and <code>total</code> attributes and the Markdown gains a</p> <code>str</code> <p>closing note; nothing is ever dropped silently, because a</p> <code>str</code> <p>catalog that varies without saying so makes agent behaviour</p> <code>str</code> <p>non-reproducible.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If format is not <code>\"xml\"</code> or <code>\"markdown\"</code>, or if max_chars is too small to hold even an empty catalog and its note.</p> <code>SkillNotFoundError</code> <p>If include names an unregistered skill.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.list_skills","title":"list_skills","text":"<pre><code>list_skills() -&gt; list[Skill]\n</code></pre> <p>Return registered skills sorted by ID.</p> <p>Returns:</p> Type Description <code>list[Skill]</code> <p>Alphabetically sorted list of :class:<code>~agentskills_core.Skill</code></p> <code>list[Skill]</code> <p>instances.  Use :meth:<code>Skill.get_id</code> to obtain a skill's name.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.register","title":"register  <code>async</code>","text":"<pre><code>register(skill_id: str, provider: SkillProvider) -&gt; None\n</code></pre><pre><code>register(skills: list[tuple[str, SkillProvider]]) -&gt; None\n</code></pre> <pre><code>register(skill_id_or_skills: str | list[tuple[str, SkillProvider]], provider: SkillProvider | None = None) -&gt; None\n</code></pre> <p>Register one or more skills with their providers.</p> <p>Validates each skill against the Agent Skills specification using :func:<code>~agentskills_core.validate_skill</code>.  This catches misconfiguration (missing <code>SKILL.md</code>, unreachable endpoint, invalid metadata) at registration time rather than at first use.</p> <p>Single skill::</p> <pre><code>await registry.register(\"incident-response\", provider)\n</code></pre> <p>Batch registration::</p> <pre><code>await registry.register([\n    (\"incident-response\", fs_provider),\n    (\"api-style-guide\", http_provider),\n])\n</code></pre> <p>Batch registration is atomic \u2014 if any skill fails validation, none of the skills in the batch are registered.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id_or_skills</code> <code>str | list[tuple[str, SkillProvider]]</code> <p>Either a single skill ID <code>str</code>, or a <code>list</code> of <code>(skill_id, provider)</code> tuples for batch registration.</p> required <code>provider</code> <code>SkillProvider | None</code> <p>The :class:<code>~agentskills_core.SkillProvider</code> for the skill.  Required when registering a single skill; must be omitted for batch registration.</p> <code>None</code> <p>Raises:</p> Type Description <code>ValueError</code> <p>If a skill_id is already registered, if a skill fails validation, or if the arguments are invalid.</p>"},{"location":"packages/core/#agentskills_core.SkillRegistry.register_all","title":"register_all  <code>async</code>","text":"<pre><code>register_all(provider: SkillProvider) -&gt; list[str]\n</code></pre> <p>Register every skill a provider holds.</p> <p>Asks the provider to enumerate itself and registers the result, so a folder of thirty skills does not require thirty hard-coded identifiers::</p> <pre><code>provider = LocalFileSystemSkillProvider(Path(\"./skills\"))\nregistered = await registry.register_all(provider)\n</code></pre> <p>Discovery is an optional capability.  Providers that cannot enumerate raise rather than returning an empty list, so a misconfigured backend cannot look like an empty one -- check :attr:<code>SkillProvider.supports_discovery &lt;agentskills_core.SkillProvider.supports_discovery&gt;</code> first if the provider may not support it.</p> <p>Registration is atomic and reports every validation failure at once.  Discovering thirty skills and being told only about the first broken one turns a single fix into thirty rounds.</p> <p>Parameters:</p> Name Type Description Default <code>provider</code> <code>SkillProvider</code> <p>The backend to enumerate and register.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>The registered skill IDs, sorted.</p> <p>Raises:</p> Type Description <code>DiscoveryNotSupportedError</code> <p>If provider cannot enumerate the skills it holds.</p> <code>ValueError</code> <p>If any discovered ID is already registered, or if any discovered skill fails validation.  Nothing is registered in either case.</p>"},{"location":"packages/core/#agentskills_core.SkillUnavailableError","title":"SkillUnavailableError","text":"<p>               Bases: <code>AgentSkillsError</code></p> <p>A skill backend could not be reached, or failed transiently.</p> <p>Raised for server-side failures (<code>5xx</code>), rate limiting (<code>429</code>), timeouts, and connection errors -- anything where the skill may well exist and the same request could succeed later.</p> <p>This is deliberately not :class:<code>SkillNotFoundError</code>.  Mapping a <code>503</code> onto \"not found\" tells the caller a skill was deleted when the truth is that a server had a bad minute, which turns a retryable blip into a permanent-looking failure.</p> <p>Parameters:</p> Name Type Description Default <code>message</code> <code>str</code> <p>Human-readable description.  Must not contain credential material -- see the <code>agentskills-http</code> provider, which redacts URL query strings before they reach an exception message.</p> required <code>retry_after</code> <code>float | None</code> <p>Server-advised delay in seconds, taken from a <code>Retry-After</code> header when present.  <code>None</code> when the server gave no advice.</p> <code>None</code> <p>Example::</p> <pre><code>try:\n    body = await skill.get_body()\nexcept SkillNotFoundError:\n    ...        # genuinely gone; stop asking\nexcept SkillUnavailableError as exc:\n    ...        # try again later, optionally after exc.retry_after\n</code></pre>"},{"location":"packages/core/#agentskills_core.classify_resource","title":"classify_resource","text":"<pre><code>classify_resource(name: str, data: bytes, *, max_inline_image_bytes: int = DEFAULT_MAX_INLINE_IMAGE_BYTES) -&gt; ResourceMedia\n</code></pre> <p>Report what data is and whether it can be shown to a model.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Resource file name.  Used only when the bytes are not self-identifying, since a name is a claim and bytes are evidence.</p> required <code>data</code> <code>bytes</code> <p>Raw resource bytes as returned by a provider.</p> required <code>max_inline_image_bytes</code> <code>int</code> <p>Ceiling above which an image is described rather than shown.  Use <code>0</code> to never render.</p> <code>DEFAULT_MAX_INLINE_IMAGE_BYTES</code> <p>Returns:</p> Name Type Description <code>A</code> <code>ResourceMedia</code> <p>class:<code>ResourceMedia</code>.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If max_inline_image_bytes is negative.</p>"},{"location":"packages/core/#agentskills_core.encode_resource_content","title":"encode_resource_content","text":"<pre><code>encode_resource_content(name: str, data: bytes, *, max_inline_binary_bytes: int = DEFAULT_MAX_INLINE_BINARY_BYTES) -&gt; str\n</code></pre> <p>Encode raw resource bytes as text an LLM can consume.</p> <p>Valid UTF-8 is returned verbatim.  Everything else is returned as a JSON envelope::</p> <pre><code>{\n  \"name\": \"architecture.png\",\n  \"media_type\": \"image/png\",\n  \"size_bytes\": 20481,\n  \"encoding\": \"base64\",\n  \"content\": \"iVBORw0KGgo...\"\n}\n</code></pre> <p>Binaries larger than max_inline_binary_bytes use the same envelope with <code>\"encoding\": \"none\"</code> and an explanatory <code>note</code> instead of content, so the agent still learns the resource exists and why it was withheld.</p> <p>Parameters:</p> Name Type Description Default <code>name</code> <code>str</code> <p>Resource file name, used to infer the media type.</p> required <code>data</code> <code>bytes</code> <p>Raw resource bytes as returned by a provider.</p> required <code>max_inline_binary_bytes</code> <code>int</code> <p>Size ceiling for inlining binary content.  Use <code>0</code> to never inline binaries.</p> <code>DEFAULT_MAX_INLINE_BINARY_BYTES</code> <p>Returns:</p> Type Description <code>str</code> <p>The decoded text, or a JSON envelope string for binary content.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If max_inline_binary_bytes is negative.</p> <p>Example::</p> <pre><code>text = encode_resource_content(\"sev.md\", await skill.get_reference(\"sev.md\"))\n</code></pre>"},{"location":"packages/core/#agentskills_core.estimate_tokens","title":"estimate_tokens","text":"<pre><code>estimate_tokens(text: str) -&gt; int\n</code></pre> <p>Estimate the token cost of text at four characters per token.</p>"},{"location":"packages/core/#agentskills_core.get_logger","title":"get_logger","text":"<pre><code>get_logger(module: str) -&gt; logging.Logger\n</code></pre> <p>Return the <code>agentskills.*</code> logger for a module.</p> <p>Pass <code>__name__</code>.  The distribution prefix is rewritten so that <code>agentskills_http.static</code> logs as <code>agentskills.http.static</code>, giving every package a common namespace root while keeping the logger name tied to the module it lives in.</p> <p>Parameters:</p> Name Type Description Default <code>module</code> <code>str</code> <p>The calling module's <code>__name__</code>.</p> required <p>Returns:</p> Type Description <code>Logger</code> <p>A configured :class:<code>~logging.Logger</code>.</p>"},{"location":"packages/core/#agentskills_core.outline_of","title":"outline_of","text":"<pre><code>outline_of(skill_id: str, body: str) -&gt; SkillOutline\n</code></pre> <p>Build the outline of body.</p>"},{"location":"packages/core/#agentskills_core.redact_url","title":"redact_url","text":"<pre><code>redact_url(url: str, *, relative_to: str | None = None) -&gt; str\n</code></pre> <p>Strip credentials from url so it is safe to log or raise.</p> <p>Query strings, fragments and userinfo are dropped, because that is where credentials actually live: SAS tokens, signed-URL signatures and basic-auth passwords.  When relative_to is given the scheme and host are dropped too, leaving only the path beneath that base -- a host is recoverable from configuration, but a full path sitting in a shared log is not something the operator can take back.</p> <p>Parameters:</p> Name Type Description Default <code>url</code> <code>str</code> <p>The URL to sanitise.</p> required <code>relative_to</code> <code>str | None</code> <p>Base URL to express the result relative to.</p> <code>None</code> <p>Returns:</p> Type Description <code>str</code> <p>A string safe to place in a log record or an exception message.</p>"},{"location":"packages/core/#agentskills_core.resolve_fast_path","title":"resolve_fast_path  <code>async</code>","text":"<pre><code>resolve_fast_path(registry: SkillRegistry, *, include: Iterable[str] | None = None, max_tokens: int = DEFAULT_FAST_PATH_MAX_TOKENS) -&gt; FastPath | None\n</code></pre> <p>Decide whether registry qualifies for the single-skill fast path.</p> <p>Returns <code>None</code> -- meaning \"use the normal catalog path\" -- unless the effective skill set is exactly one and its body fits under max_tokens.</p> <p>include narrows the registry to an effective set, so a registry of fifty skills that a selector has narrowed to one takes the same path as a registry that only ever held one.  That is the same situation and the <code>len(list_skills()) == 1</code> test would miss it.</p> <p>Both refusals are logged, because an integration silently choosing between two prompt shapes is worse than a slightly slower one.</p>"},{"location":"packages/core/#agentskills_core.split_frontmatter","title":"split_frontmatter","text":"<pre><code>split_frontmatter(raw: str) -&gt; tuple[dict[str, Any], str]\n</code></pre> <p>Split <code>SKILL.md</code> content into YAML frontmatter and markdown body.</p> <p>Frontmatter is the YAML block delimited by <code>---</code> on its own line at the very start of the file.  If no valid frontmatter is detected the entire content is returned as the body with an empty dict.</p> <p>Parameters:</p> Name Type Description Default <code>raw</code> <code>str</code> <p>Full text content of a <code>SKILL.md</code> file.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>A <code>(frontmatter_dict, body_str)</code> tuple.  frontmatter_dict</p> <code>str</code> <p>is <code>{}</code> when no frontmatter is present.</p> <p>Example::</p> <pre><code>meta, body = split_frontmatter(Path(\"SKILL.md\").read_text())\nprint(meta.get(\"name\"))\n</code></pre>"},{"location":"packages/core/#agentskills_core.split_sections","title":"split_sections","text":"<pre><code>split_sections(body: str) -&gt; list[Section]\n</code></pre> <p>Split body at its headings, without overlapping.</p> <p>A heading owns its own line and everything up to the next heading of any level, so a nested section is not also counted inside its parent.  The parts therefore sum to the whole, which is the only property that makes a cost breakdown checkable.</p> <p>Text before the first heading becomes a level-0 section, and <code>#</code> inside a fenced code block is a shell comment rather than a heading.</p>"},{"location":"packages/core/#agentskills_core.validate_skill","title":"validate_skill  <code>async</code>","text":"<pre><code>validate_skill(skill: Skill) -&gt; list[str]\n</code></pre> <p>Validate a single skill against the Agent Skills specification.</p> <p>Checks that the skill's definition is well-formed and that its frontmatter satisfies all required and optional field constraints.</p> <p>Validation rules:</p> <ul> <li>Skill body must be non-empty.</li> <li><code>name</code> (required) -- 1-64 characters, lowercase <code>[a-z0-9-]</code>,   must not start or end with a hyphen, must not contain consecutive   hyphens, and must match the skill ID.</li> <li><code>description</code> (required) -- 1-1024 characters.</li> <li><code>version</code> (optional) -- valid semver when present.  Not yet part   of the upstream specification; see :func:<code>validate_version</code>.</li> <li><code>when_to_use</code> / <code>when_not_to_use</code> (optional) -- lists of at   most five non-empty strings of at most 200 characters each.  They   are charged on every turn, so they are bounded; see   :data:<code>SELECTION_FIELDS</code>.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>skill</code> <code>Skill</code> <p>The :class:<code>~agentskills_core.Skill</code> to validate.</p> required <p>Returns:</p> Type Description <code>list[str]</code> <p>A list of human-readable error messages.  An empty list means</p> <code>list[str]</code> <p>the skill is valid.</p>"},{"location":"packages/core/#agentskills_core.validate_version","title":"validate_version","text":"<pre><code>validate_version(value: object) -&gt; str | None\n</code></pre> <p>Check a frontmatter <code>version</code> value, returning an error or <code>None</code>.</p> <p><code>version</code> is not part of the upstream Agent Skills specification.  It is supported here as an optional extension so that consumers can pin, compare, and detect drift; absence is always valid.</p> <p>Parameters:</p> Name Type Description Default <code>value</code> <code>object</code> <p>The raw value parsed from YAML frontmatter.</p> required <p>Returns:</p> Type Description <code>str | None</code> <p>A human-readable error message, or <code>None</code> when value is a</p> <code>str | None</code> <p>valid semver string.</p>"},{"location":"packages/fs/","title":"Filesystem Provider (<code>agentskills-fs</code>)","text":"<p>Loads skills from a local directory.</p> <pre><code>pip install agentskills-fs\n</code></pre>"},{"location":"packages/fs/#agentskills_fs","title":"agentskills_fs","text":"<p>Local filesystem-based skill provider for the Agent Skills format.</p> <p>This package provides :class:<code>LocalFileSystemSkillProvider</code>, a concrete implementation of :class:<code>~agentskills_core.SkillProvider</code> that reads <code>Agent Skills &lt;https://agentskills.io&gt;</code>_ from a local directory tree.</p> <p>Install::</p> <pre><code>pip install agentskills-fs\n</code></pre>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider","title":"LocalFileSystemSkillProvider","text":"<p>               Bases: <code>SkillProvider</code></p> <p>Skill provider backed by a local directory tree.</p> <p>Each immediate subdirectory of root that contains a <code>SKILL.md</code> file is treated as a skill.  The directory name serves as the skill's unique identifier and must match the <code>name</code> field in the <code>SKILL.md</code> YAML frontmatter.</p> <p>Expected layout::</p> <pre><code>root/\n\u251c\u2500\u2500 incident-response/\n\u2502   \u251c\u2500\u2500 SKILL.md          # YAML frontmatter + markdown body\n\u2502   \u251c\u2500\u2500 references/       # optional supplementary docs\n\u2502   \u251c\u2500\u2500 scripts/          # optional executable code\n\u2502   \u2514\u2500\u2500 assets/           # optional static resources\n\u2514\u2500\u2500 another-skill/\n    \u2514\u2500\u2500 SKILL.md\n</code></pre> <p>Progressive disclosure guarantees:</p> <ul> <li>:meth:<code>get_metadata</code> reads and parses only the YAML frontmatter   (between the opening and closing <code>---</code> delimiters).</li> <li>:meth:<code>get_body</code> returns only the markdown after the frontmatter.</li> <li>Resource methods (:meth:<code>get_reference</code>, :meth:<code>get_script</code>,   :meth:<code>get_asset</code>) read individual files on demand.  Resource   names are discovered by the agent from the skill body.</li> </ul> <p>Parameters:</p> Name Type Description Default <code>root</code> <code>Path</code> <p>Path to the top-level directory containing skill subdirectories.</p> required <code>max_file_bytes</code> <code>int</code> <p>Maximum allowed file size in bytes. Files exceeding this limit raise :class:<code>~agentskills_core.AgentSkillsError</code>.  Defaults to 10 MB.</p> <code>DEFAULT_MAX_FILE_BYTES</code> <p><code>SKILL.md</code> contents are cached per provider instance after the first read, because a single skill is otherwise re-read up to five times in one agent session.  Call :meth:<code>invalidate</code> when skills change on disk.</p> <p>Resource listing is supported: see :meth:<code>list_resources</code>. Skill discovery is supported: see :meth:<code>discover</code>.</p> <p>Raises:</p> Type Description <code>NotADirectoryError</code> <p>If root does not exist or is not a directory.</p> <p>Example::</p> <pre><code>provider = LocalFileSystemSkillProvider(Path(\"./skills\"))\nregistry = SkillRegistry()\nawait registry.register_all(provider)\n\nskill = registry.get_skill(\"incident-response\")\nmeta = await skill.get_metadata()\nprint(f\"{meta['name']}: {meta['description']}\")\n</code></pre>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.discover","title":"discover  <code>async</code>","text":"<pre><code>discover() -&gt; list[str]\n</code></pre> <p>List the skill directories directly beneath the root.</p> <p>A directory counts as a skill when it holds a <code>SKILL.md</code>. Dotted directories, loose files and symlinks pointing outside the root are skipped, matching :meth:<code>list_resources</code>.</p> <p>Discovery does not read or validate the skills it finds, so a returned ID can still fail registration.  That is deliberate: enumerating a folder should not cost one full parse per skill.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted skill IDs.  Empty when the root holds no skills.</p>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.get_asset","title":"get_asset  <code>async</code>","text":"<pre><code>get_asset(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Read a single asset file as raw bytes.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <code>name</code> <code>str</code> <p>Asset filename.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw file content.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the file does not exist.</p>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.get_body","title":"get_body  <code>async</code>","text":"<pre><code>get_body(skill_id: str) -&gt; str\n</code></pre> <p>Return the markdown instruction body after the YAML frontmatter.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Markdown text (may be empty if <code>SKILL.md</code> has no body).</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill directory or <code>SKILL.md</code> does not exist.</p>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.get_metadata","title":"get_metadata  <code>async</code>","text":"<pre><code>get_metadata(skill_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Parse and return the YAML frontmatter of a skill's <code>SKILL.md</code>.</p> <p>Only the content between the opening and closing <code>---</code> delimiters is parsed.  The markdown body is discarded so that metadata-only queries remain lightweight.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary of frontmatter key-value pairs.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill directory or <code>SKILL.md</code> does not exist.</p>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.get_reference","title":"get_reference  <code>async</code>","text":"<pre><code>get_reference(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Read a single reference file as raw bytes.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <code>name</code> <code>str</code> <p>Reference filename.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw file content.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the file does not exist.</p>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.get_script","title":"get_script  <code>async</code>","text":"<pre><code>get_script(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Read a single script file as raw bytes.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <code>name</code> <code>str</code> <p>Script filename.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw file content.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the file does not exist.</p>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.invalidate","title":"invalidate","text":"<pre><code>invalidate(skill_id: str | None = None) -&gt; None\n</code></pre> <p>Drop cached <code>SKILL.md</code> content.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str | None</code> <p>Skill to forget.  Clears the whole cache when omitted.  Unknown IDs are ignored.</p> <code>None</code>"},{"location":"packages/fs/#agentskills_fs.LocalFileSystemSkillProvider.list_resources","title":"list_resources  <code>async</code>","text":"<pre><code>list_resources(skill_id: str) -&gt; dict[str, list[str]]\n</code></pre> <p>List the resource files a skill contains, grouped by kind.</p> <p>Only regular files directly inside <code>references/</code>, <code>scripts/</code> and <code>assets/</code> are reported.  Subdirectories, dotfiles and symlinks pointing outside the skill root are skipped rather than raising, so one stray entry cannot make a whole skill unlistable.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>Mapping of resource kind to sorted filenames.  Categories the</p> <code>dict[str, list[str]]</code> <p>skill does not use map to an empty list.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill directory does not exist.</p>"},{"location":"packages/http/","title":"HTTP Provider (<code>agentskills-http</code>)","text":"<p>Loads skills from a static HTTP host or CDN.</p> <pre><code>pip install agentskills-http\n</code></pre>"},{"location":"packages/http/#agentskills_http","title":"agentskills_http","text":"<p>HTTP-based skill providers for the Agent Skills format.</p> <p>This package provides :class:<code>HTTPStaticFileSkillProvider</code>, a concrete implementation of :class:<code>~agentskills_core.SkillProvider</code> that fetches <code>Agent Skills &lt;https://agentskills.io&gt;</code>_ from a static HTTP file host (S3, Azure Blob Storage, CDN, GitHub Pages, or any web server serving raw files).</p> <p>Install::</p> <pre><code>pip install agentskills-http\n</code></pre>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider","title":"HTTPStaticFileSkillProvider","text":"<p>               Bases: <code>SkillProvider</code></p> <p>Skill provider backed by a static HTTP file host.</p> <p>The provider expects an HTTP server (S3, Azure Blob, CDN, Nginx, GitHub Pages, etc.) that hosts skill files at predictable URL paths. Resource names (scripts, assets, references) are discovered by the agent from the skill body, or from an optional per-skill <code>index.json</code> manifest -- see resource_manifest.  The set of skills is likewise unknowable over plain HTTP unless the host publishes a root <code>index.json</code> -- see skill_manifest.</p> <p>The provider owns an :class:<code>httpx.AsyncClient</code> for connection pooling.  If you supply your own client the provider will use it without closing it.  Otherwise call :meth:<code>aclose</code> or use <code>async with</code> when you are finished.</p> <p>Parameters:</p> Name Type Description Default <code>base_url</code> <code>str</code> <p>Root URL where the skill tree is hosted.  A trailing slash is stripped automatically.</p> required <code>client</code> <code>AsyncClient | None</code> <p>Optional pre-configured :class:<code>httpx.AsyncClient</code>. When provided, the caller is responsible for closing it. The provider will still enforce max_response_bytes but will not override the client's timeout or redirect settings.</p> <code>None</code> <code>headers</code> <code>dict[str, str] | None</code> <p>Optional extra headers sent with every request (e.g. <code>Authorization</code>).</p> <code>None</code> <code>params</code> <code>dict[str, str] | None</code> <p>Optional query parameters appended to every request (e.g. SAS tokens for Azure Blob Storage).</p> <code>None</code> <code>require_tls</code> <code>bool</code> <p>If <code>True</code>, reject <code>http://</code> base URLs with a :class:<code>ValueError</code>.  Defaults to <code>False</code>, which allows HTTP but emits a :class:<code>UserWarning</code>.</p> <code>False</code> <code>max_response_bytes</code> <code>int</code> <p>Maximum allowed response size in bytes. Responses exceeding this limit raise :class:<code>~agentskills_core.AgentSkillsError</code>.  Defaults to 10 MB.</p> <code>DEFAULT_MAX_RESPONSE_BYTES</code> <code>revalidate</code> <code>bool</code> <p>If <code>True</code>, re-check cached <code>SKILL.md</code> content on every access using <code>If-None-Match</code> / <code>If-Modified-Since</code>.  Costs one (usually empty) round trip per access but picks up republished skills.  Defaults to <code>False</code>, which serves cached content until :meth:<code>invalidate</code> is called.</p> <code>False</code> <code>resource_manifest</code> <code>bool</code> <p>Set <code>True</code> if the host publishes a per-skill <code>index.json</code> listing resource names.  Enables :meth:<code>list_resources</code>.  Defaults to <code>False</code>, because a plain static host cannot be enumerated and claiming otherwise would make missing manifests look like skills with no resources.</p> <code>False</code> <code>skill_manifest</code> <code>bool</code> <p>Set <code>True</code> if the host publishes a root <code>index.json</code> with a <code>\"skills\"</code> list.  Enables :meth:<code>discover</code>, and so :meth:<code>SkillRegistry.register_all &lt;agentskills_core.SkillRegistry.register_all&gt;</code>.  Defaults to <code>False</code> for the same reason: without a manifest, \"no skills found\" would be a lie.</p> <code>False</code> <code>timeout</code> <code>float</code> <p>Request timeout in seconds.  Ignored when you supply your own client.</p> <code>DEFAULT_TIMEOUT_SECONDS</code> <code>max_retries</code> <code>int</code> <p>Retries after the initial attempt, for retryable failures only (<code>5xx</code>, <code>408</code>, <code>425</code>, <code>429</code>, timeouts, connection errors).  <code>0</code> disables retrying.</p> <code>DEFAULT_MAX_RETRIES</code> <code>retry_backoff</code> <code>float</code> <p>Base delay in seconds for exponential backoff. Actual sleeps are jittered across <code>[0, delay]</code> so that concurrent skill fetches do not retry in lockstep.</p> <code>DEFAULT_RETRY_BACKOFF_SECONDS</code> <code>max_retry_delay</code> <code>float</code> <p>Ceiling on any single sleep.  A <code>Retry-After</code> longer than this is not waited out -- the error is raised immediately with <code>retry_after</code> attached, because blocking a request path for minutes is worse than failing fast and letting the caller decide.</p> <code>DEFAULT_MAX_RETRY_DELAY_SECONDS</code> <p><code>SKILL.md</code> responses are cached per provider instance, because a single skill is otherwise re-fetched up to five times in one agent session.  Resource fetches (scripts, assets, references) are not cached: they are usually larger and read once.</p> <p>Example::</p> <pre><code>async with HTTPStaticFileSkillProvider(\"https://cdn.example.com/skills\") as provider:\n    registry = SkillRegistry()\n    await registry.register(\"incident-response\", provider)\n\n    skill = registry.get_skill(\"incident-response\")\n    meta = await skill.get_metadata()\n    print(f\"{meta['name']}: {meta['description']}\")\n</code></pre>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.aclose","title":"aclose  <code>async</code>","text":"<pre><code>aclose() -&gt; None\n</code></pre> <p>Close the underlying HTTP client if it is owned by this provider.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.discover","title":"discover  <code>async</code>","text":"<pre><code>discover() -&gt; list[str]\n</code></pre> <p>List the host's skills from the root <code>index.json</code> manifest.</p> <p>Requires <code>skill_manifest=True</code>.  The manifest is a JSON object at <code>{base_url}/index.json</code> with a <code>\"skills\"</code> list::</p> <pre><code>{\"skills\": [\"incident-response\", \"api-style-guide\"]}\n</code></pre> <p>It is the same file format as the per-skill resource manifest, one level up, so a host publishing both has one shape to generate rather than two.  Other keys are ignored, and entries that are not valid skill IDs are dropped -- a manifest is host-supplied data and an ID is later interpolated into a URL.</p> <p>Returns:</p> Type Description <code>list[str]</code> <p>Sorted skill IDs.  Empty when the manifest lists none.</p> <p>Raises:</p> Type Description <code>DiscoveryNotSupportedError</code> <p>If the provider was built without <code>skill_manifest=True</code>, or the host publishes no root manifest.</p> <code>AgentSkillsError</code> <p>If the manifest is not a JSON object.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.get_asset","title":"get_asset  <code>async</code>","text":"<pre><code>get_asset(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Fetch a single asset file.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <code>name</code> <code>str</code> <p>Asset filename.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content as bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the asset does not exist.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.get_body","title":"get_body  <code>async</code>","text":"<pre><code>get_body(skill_id: str) -&gt; str\n</code></pre> <p>Fetch <code>SKILL.md</code> and return the markdown body.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <p>Returns:</p> Type Description <code>str</code> <p>Markdown instruction text.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill's <code>SKILL.md</code> cannot be fetched.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.get_metadata","title":"get_metadata  <code>async</code>","text":"<pre><code>get_metadata(skill_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Fetch <code>SKILL.md</code> and return the parsed YAML frontmatter.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name to look up.</p> required <p>Returns:</p> Type Description <code>dict[str, Any]</code> <p>Dictionary of frontmatter key-value pairs.</p> <p>Raises:</p> Type Description <code>SkillNotFoundError</code> <p>If the skill's <code>SKILL.md</code> cannot be fetched.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.get_reference","title":"get_reference  <code>async</code>","text":"<pre><code>get_reference(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Fetch a single reference file.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <code>name</code> <code>str</code> <p>Reference filename.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content as bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the reference does not exist.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.get_script","title":"get_script  <code>async</code>","text":"<pre><code>get_script(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Fetch a single script file.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <code>name</code> <code>str</code> <p>Script filename.</p> required <p>Returns:</p> Type Description <code>bytes</code> <p>Raw content as bytes.</p> <p>Raises:</p> Type Description <code>ResourceNotFoundError</code> <p>If the script does not exist.</p>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.invalidate","title":"invalidate","text":"<pre><code>invalidate(skill_id: str | None = None) -&gt; None\n</code></pre> <p>Drop cached <code>SKILL.md</code> content.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str | None</code> <p>Skill to forget.  Clears the whole cache when omitted.  Unknown IDs are ignored.</p> <code>None</code>"},{"location":"packages/http/#agentskills_http.HTTPStaticFileSkillProvider.list_resources","title":"list_resources  <code>async</code>","text":"<pre><code>list_resources(skill_id: str) -&gt; dict[str, list[str]]\n</code></pre> <p>List a skill's resources from its <code>index.json</code> manifest.</p> <p>Requires <code>resource_manifest=True</code>.  The manifest is a JSON object at <code>{base_url}/{skill_id}/index.json</code> mapping resource kinds to name lists::</p> <pre><code>{\"references\": [\"sev.md\"], \"scripts\": [\"run.sh\"], \"assets\": []}\n</code></pre> <p>Unknown keys are ignored and missing kinds default to empty. Entries that are not valid resource names are dropped, since a manifest is host-supplied data and a name is later interpolated into a URL.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Skill name.</p> required <p>Returns:</p> Type Description <code>dict[str, list[str]]</code> <p>Mapping of resource kind to sorted resource names.</p> <p>Raises:</p> Type Description <code>ResourceListingNotSupportedError</code> <p>If the provider was built without <code>resource_manifest=True</code>, or the skill has no published manifest.</p> <code>SkillNotFoundError</code> <p>If the skill itself does not exist.</p> <code>AgentSkillsError</code> <p>If the manifest is not a JSON object.</p>"},{"location":"packages/langchain/","title":"LangChain Integration (<code>agentskills-langchain</code>)","text":"<p>Exposes skills as LangChain tools.</p> <pre><code>pip install agentskills-langchain\n</code></pre>"},{"location":"packages/langchain/#agentskills_langchain","title":"agentskills_langchain","text":"<p>LangChain integration for Agent Skills.</p> <p>This package bridges :mod:<code>agentskills_core</code> and <code>LangChain &lt;https://python.langchain.com&gt;</code>_, providing:</p> <ul> <li>:func:<code>get_tools</code> -- generates five   :class:<code>~langchain_core.tools.StructuredTool</code> instances that let an   LLM agent consume skills.</li> <li>:func:<code>get_tools_usage_instructions</code> -- returns agent-facing   instructions explaining how to use the tools.</li> </ul> <p>Install::</p> <pre><code>pip install agentskills-langchain\n</code></pre>"},{"location":"packages/langchain/#agentskills_langchain.get_tools","title":"get_tools","text":"<pre><code>get_tools(registry: SkillRegistry, *, max_inline_binary_bytes: int = DEFAULT_MAX_INLINE_BINARY_BYTES, fast_path: FastPath | None = None, vision: bool = False, max_inline_image_bytes: int = DEFAULT_MAX_INLINE_IMAGE_BYTES) -&gt; list[StructuredTool]\n</code></pre> <p>Build LangChain tools that expose an Agent Skills registry.</p> <p>Each tool wraps a :class:<code>~agentskills_core.SkillRegistry</code> or :class:<code>~agentskills_core.Skill</code> method, serialising the result as JSON (for dicts) or plain text (for content bodies). Tools are read-only -- they retrieve content but never execute scripts or modify state.</p> <p>Skill discovery is handled via the catalog in the system prompt, so no <code>list_skills</code> tool is included.</p> <p>All tools are async coroutines.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The :class:<code>~agentskills_core.SkillRegistry</code> whose skills should be exposed as tools.</p> required <code>max_inline_binary_bytes</code> <code>int</code> <p>Size ceiling for inlining binary resources as base64.  Larger resources are described but not returned.  See :func:<code>~agentskills_core.encode_resource_content</code>.</p> <code>DEFAULT_MAX_INLINE_BINARY_BYTES</code> <code>fast_path</code> <code>FastPath | None</code> <p>A :class:<code>~agentskills_core.FastPath</code> from :func:<code>~agentskills_core.resolve_fast_path</code>.  When given, the four body-access tools are omitted, because the body is already in the prompt.  Resource tools remain.  Use <code>fast_path.prompt</code> in place of the catalog and :func:<code>get_tools_usage_instructions</code>.</p> <code>None</code> <code>vision</code> <code>bool</code> <p>When <code>True</code>, bundled images small enough to inline are returned as native image content blocks instead of a base64 JSON envelope.  Off by default: handing an image block to a text-only model is an API error, not a degraded answer, so the caller declares the capability rather than the library guessing it.</p> <code>False</code> <code>max_inline_image_bytes</code> <code>int</code> <p>Size ceiling for native images.  Only consulted when <code>vision</code> is <code>True</code>.  Larger images fall back to the JSON envelope.</p> <code>DEFAULT_MAX_INLINE_IMAGE_BYTES</code> <p>Returns:</p> Type Description <code>list[StructuredTool]</code> <p>A list of :class:<code>~langchain_core.tools.StructuredTool</code></p> <code>list[StructuredTool]</code> <p>instances ready to be passed to a LangChain agent.</p>"},{"location":"packages/langchain/#agentskills_langchain.get_tools_usage_instructions","title":"get_tools_usage_instructions","text":"<pre><code>get_tools_usage_instructions() -&gt; str\n</code></pre> <p>Return agent instructions for using the Agent Skills tools.</p> <p>This text explains to an LLM agent how to use the skill tools (<code>get_skill_metadata</code>, <code>get_skill_body</code>, <code>get_skill_reference</code>, <code>get_skill_script</code>, <code>get_skill_asset</code>) following the progressive-disclosure workflow.</p> <p>Combine with the skill catalog produced by :meth:<code>SkillRegistry.get_skills_catalog</code> to give the agent both what skills are available and how to interact with them::</p> <pre><code>catalog = await registry.get_skills_catalog(format=\"xml\")\ninstructions = get_tools_usage_instructions()\nsystem_prompt = f\"{catalog}\\n\\n{instructions}\"\n</code></pre> <p>Returns:</p> Type Description <code>str</code> <p>A multi-line instruction string ready for system-prompt</p> <code>str</code> <p>insertion.</p>"},{"location":"packages/mcp-server/","title":"MCP Server Integration (<code>agentskills-mcp-server</code>)","text":"<p>Serves skills as MCP tools/resources.</p> <pre><code>pip install agentskills-mcp-server\n</code></pre>"},{"location":"packages/mcp-server/#agentskills_mcp_server","title":"agentskills_mcp_server","text":"<p>MCP server integration for Agent Skills.</p> <p>This package bridges :mod:<code>agentskills_core</code> and the <code>Model Context Protocol &lt;https://modelcontextprotocol.io&gt;</code>_, providing:</p> <ul> <li>:func:<code>create_mcp_server</code> -- builds a FastMCP server from a   :class:<code>~agentskills_core.SkillRegistry</code>.  Useful when you have   custom providers or need full control over registration.</li> <li>CLI entry-point (<code>python -m agentskills_mcp_server --config server.json</code>)   for zero-code server startup.</li> </ul> <p>Quick start (programmatic)::</p> <pre><code>from agentskills_core import SkillRegistry\nfrom agentskills_mcp_server import create_mcp_server\n\nregistry = SkillRegistry()\nawait registry.register(\"incident-response\", my_custom_provider)\nserver = create_mcp_server(registry, name=\"My Agent\")\nserver.run()  # stdio by default\n</code></pre> <p>CLI::</p> <pre><code>python -m agentskills_mcp_server --config server.json\n</code></pre> <p>Install::</p> <pre><code>pip install agentskills-mcp-server\n</code></pre>"},{"location":"packages/mcp-server/#agentskills_mcp_server.__getattr__","title":"__getattr__","text":"<pre><code>__getattr__(name: str) -&gt; object\n</code></pre> <p>Lazy-load optional extras to avoid hard dependencies at import time.</p>"},{"location":"packages/mcp-server/#agentskills_mcp_server.create_mcp_server","title":"create_mcp_server","text":"<pre><code>create_mcp_server(registry: SkillRegistry, *, name: str, instructions: str | None = None, max_inline_binary_bytes: int = DEFAULT_MAX_INLINE_BINARY_BYTES, fast_path: FastPath | None = None, vision: bool = False, max_inline_image_bytes: int = DEFAULT_MAX_INLINE_IMAGE_BYTES) -&gt; FastMCP\n</code></pre> <p>Build an MCP server that exposes an Agent Skills registry.</p> <p>The returned :class:<code>~mcp.server.fastmcp.FastMCP</code> server is transport-agnostic.  Call <code>server.run()</code> to start with the default stdio transport, or <code>server.run(transport=\"streamable-http\")</code> for HTTP.</p> <p>Tools let the LLM agent read skill content (metadata, body, references, scripts, assets).  Resources provide the skill catalog and usage instructions for system-prompt injection.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The :class:<code>~agentskills_core.SkillRegistry</code> whose skills should be exposed via MCP.</p> required <code>name</code> <code>str</code> <p>Display name for the MCP server.  Clients see this during the MCP initialization handshake.  Required.</p> required <code>instructions</code> <code>str | None</code> <p>Optional server-level instructions sent to the MCP client during initialization.  Use this to describe the server's purpose or capabilities.</p> <code>None</code> <code>max_inline_binary_bytes</code> <code>int</code> <p>Size ceiling for inlining binary resources as base64.  Larger resources are described but not returned.  See :func:<code>~agentskills_core.encode_resource_content</code>.</p> <code>DEFAULT_MAX_INLINE_BINARY_BYTES</code> <code>fast_path</code> <code>FastPath | None</code> <p>A :class:<code>~agentskills_core.FastPath</code> from :func:<code>~agentskills_core.resolve_fast_path</code>.  When given, both catalog resources serve the skill's body directly, the usage-instructions resource drops the selection workflow it no longer describes, and the four body-access tools are not registered at all.  Resource tools remain.</p> <code>None</code> <code>vision</code> <code>bool</code> <p>When <code>True</code>, bundled images small enough to inline are returned as native <code>ImageContent</code> instead of a base64 JSON envelope.  Off by default: handing an image to a text-only model is an API error, not a degraded answer, so the caller declares the capability rather than the library guessing it.</p> <code>False</code> <code>max_inline_image_bytes</code> <code>int</code> <p>Size ceiling for native images.  Only consulted when <code>vision</code> is <code>True</code>.  Larger images fall back to the JSON envelope.</p> <code>DEFAULT_MAX_INLINE_IMAGE_BYTES</code> <p>Returns:</p> Type Description <code>FastMCP</code> <p>A configured :class:<code>~mcp.server.fastmcp.FastMCP</code> server</p> <code>FastMCP</code> <p>instance, ready for <code>server.run()</code>.</p>"},{"location":"packages/retrieval/","title":"Skill Selection (<code>agentskills-retrieval</code>)","text":"<p>Narrows the catalog to the skills a query is actually about.</p> <pre><code>pip install agentskills-retrieval\n</code></pre>"},{"location":"packages/retrieval/#agentskills_retrieval","title":"agentskills_retrieval","text":"<p>Query-time skill selection for the Agent Skills SDK.</p> <p>The catalog is injected on every turn, so per-turn prompt cost is linear in the number of registered skills \u2014 and selection accuracy falls as the candidate list grows, so a large registry is both more expensive and worse at choosing. This package narrows the catalog to what the current query is actually about.</p> <p>Two selectors ship. :class:<code>LexicalSelector</code> is BM25 and needs nothing installed; :class:<code>EmbeddingSelector</code> takes any embedder behind a one-method protocol. Both return scores, and both compose with the <code>include=</code> filter the registry already has::</p> <pre><code>from agentskills_retrieval import LexicalSelector, build_selected_catalog\n\nselector = LexicalSelector(registry)\ncatalog = await build_selected_catalog(registry, selector, \"checkout is down\")\n</code></pre> <p>Nothing here is on by default. A registry that is not asked to select behaves exactly as it did before this package existed.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.Embedder","title":"Embedder","text":"<p>               Bases: <code>Protocol</code></p> <p>The whole contract: a name, and a way to turn text into vectors.</p> <p>Implement it over any client::</p> <pre><code>class OpenAIEmbedder:\n    embedder_id = \"text-embedding-3-small\"\n\n    async def embed(self, texts):\n        reply = await client.embeddings.create(\n            model=self.embedder_id, input=list(texts)\n        )\n        return [item.embedding for item in reply.data]\n</code></pre> <p><code>embedder_id</code> is part of every cache key, because two models' vectors are not comparable and a cache that mixes them silently returns nonsense.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.Embedder.embed","title":"embed  <code>async</code>","text":"<pre><code>embed(texts: Sequence[str]) -&gt; list[list[float]]\n</code></pre> <p>Return one vector per text, in the same order.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.EmbeddingCache","title":"EmbeddingCache","text":"<p>               Bases: <code>Protocol</code></p> <p>Somewhere to keep vectors between processes.</p> <p>Two methods, both synchronous, because the interesting backends (a file, a table, Redis) are all fast enough that making callers write an async adapter buys nothing.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.EmbeddingCache.get","title":"get","text":"<pre><code>get(key: str) -&gt; list[float] | None\n</code></pre> <p>Return the vector stored under key, or <code>None</code>.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.EmbeddingCache.set","title":"set","text":"<pre><code>set(key: str, vector: list[float]) -&gt; None\n</code></pre> <p>Store vector under key.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.EmbeddingSelector","title":"EmbeddingSelector","text":"<p>Select skills by cosine similarity between query and skill vectors.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The registry whose skills are ranked.</p> required <code>embedder</code> <code>Embedder</code> <p>Anything satisfying :class:<code>Embedder</code>.  Build one with :func:<code>load_embedder</code> when the name comes from config.</p> required <code>cache</code> <code>EmbeddingCache | None</code> <p>Where skill vectors are kept.  Defaults to an in-process dict; pass your own to survive a restart.</p> <code>None</code> <code>min_score</code> <code>float</code> <p>Cosine below this is rejected.  See :data:<code>DEFAULT_MIN_SCORE</code>.</p> <code>DEFAULT_MIN_SCORE</code> <code>negative_weight</code> <code>float</code> <p>How much similarity to <code>when_not_to_use</code> subtracts.  Pass <code>0.0</code> to ignore disclaimers.</p> <code>0.5</code>"},{"location":"packages/retrieval/#agentskills_retrieval.EmbeddingSelector.index","title":"index  <code>async</code>","text":"<pre><code>index() -&gt; None\n</code></pre> <p>Embed every registered skill, reusing anything already cached.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.EmbeddingSelector.select","title":"select  <code>async</code>","text":"<pre><code>select(query: str, *, limit: int = DEFAULT_LIMIT) -&gt; Selection\n</code></pre> <p>Return the best limit skills for query, by cosine similarity.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.InMemoryEmbeddingCache","title":"InMemoryEmbeddingCache","text":"<p>The default cache: a dict that dies with the process.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.InMemoryEmbeddingCache.__len__","title":"__len__","text":"<pre><code>__len__() -&gt; int\n</code></pre> <p>Number of cached vectors, which is what tests assert on.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.InMemoryEmbeddingCache.get","title":"get","text":"<pre><code>get(key: str) -&gt; list[float] | None\n</code></pre> <p>Return the vector stored under key, or <code>None</code>.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.InMemoryEmbeddingCache.set","title":"set","text":"<pre><code>set(key: str, vector: list[float]) -&gt; None\n</code></pre> <p>Store vector under key.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.LexicalSelector","title":"LexicalSelector","text":"<p>Select skills by BM25 over their catalog metadata.</p> <p>The corpus is built on first use and reused until the registry's set of skill IDs changes, so a long-lived agent pays for indexing once.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The registry whose skills are ranked.</p> required <code>min_score</code> <code>float</code> <p>Scores at or below this are rejected outright.  See :data:<code>DEFAULT_MIN_SCORE</code> for why the default is zero.</p> <code>DEFAULT_MIN_SCORE</code> <code>negative_weight</code> <code>float</code> <p>How much a <code>when_not_to_use</code> match subtracts. Pass <code>0.0</code> to ignore disclaimers entirely.</p> <code>NEGATIVE_WEIGHT</code>"},{"location":"packages/retrieval/#agentskills_retrieval.LexicalSelector.index","title":"index  <code>async</code>","text":"<pre><code>index() -&gt; None\n</code></pre> <p>Build the BM25 index, discarding any previous one.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.LexicalSelector.select","title":"select  <code>async</code>","text":"<pre><code>select(query: str, *, limit: int = DEFAULT_LIMIT) -&gt; Selection\n</code></pre> <p>Return the best limit skills for query.</p> <p>Parameters:</p> Name Type Description Default <code>query</code> <code>str</code> <p>Free text to rank against.  The caller decides what this is; see the README on why the last user message alone is a poor default.</p> required <code>limit</code> <code>int</code> <p>Maximum number of skills to return.</p> <code>DEFAULT_LIMIT</code> <p>Returns:</p> Name Type Description <code>A</code> <code>Selection</code> <p>class:<code>~agentskills_retrieval.Selection</code>, whose</p> <code>Selection</code> <p><code>skill_ids</code> feed <code>get_skills_catalog(include=...)</code>.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.ScoredSkill","title":"ScoredSkill  <code>dataclass</code>","text":"<p>One skill and what it scored, in the selector's own units.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.Selection","title":"Selection  <code>dataclass</code>","text":"<p>The outcome of one selection, including what it rejected.</p> <p>Attributes:</p> Name Type Description <code>query</code> <code>str</code> <p>The text that was ranked against.</p> <code>selected</code> <code>list[ScoredSkill]</code> <p>Skills that cleared the floor, best first.</p> <code>rejected</code> <code>list[ScoredSkill]</code> <p>Skills that did not, also best first.  Kept because \"the right skill scored just under the floor\" and \"the right skill was not registered\" are different bugs and look identical without this.</p> <code>considered</code> <code>int</code> <p>How many skills were scored, which is the denominator to report in the catalog.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.Selection.is_empty","title":"is_empty  <code>property</code>","text":"<pre><code>is_empty: bool\n</code></pre> <p>Whether nothing cleared the floor.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.Selection.skill_ids","title":"skill_ids  <code>property</code>","text":"<pre><code>skill_ids: list[str]\n</code></pre> <p>The selected IDs, ready to pass to <code>include=</code>.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.Selection.describe","title":"describe","text":"<pre><code>describe() -&gt; str\n</code></pre> <p>Return a one-line summary with scores, for logs and reports.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.SkillDocument","title":"SkillDocument  <code>dataclass</code>","text":"<p>One skill, as a ranker sees it.</p> <p>Attributes:</p> Name Type Description <code>skill_id</code> <code>str</code> <p>The registered ID, which is what a selection returns.</p> <code>positive_text</code> <code>str</code> <p>Name, description, <code>when_to_use</code> and tags.</p> <code>negative_text</code> <code>str</code> <p><code>when_not_to_use</code>, scored separately.</p> <code>content_hash</code> <code>str</code> <p>Digest of the text above, so an embedding can be cached against it and invalidated by nothing else.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.SkillDocument.negative_terms","title":"negative_terms  <code>property</code>","text":"<pre><code>negative_terms: list[str]\n</code></pre> <p>The tokens a query is penalised for matching.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.SkillDocument.positive_terms","title":"positive_terms  <code>property</code>","text":"<pre><code>positive_terms: list[str]\n</code></pre> <p>The tokens a query is matched against.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.SkillSelector","title":"SkillSelector","text":"<p>               Bases: <code>Protocol</code></p> <p>Rank registered skills against a query.</p> <p>One method, so a caller can supply its own ranker \u2014 a hosted reranking API, a hand-written routing table \u2014 without inheriting anything from this package.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.SkillSelector.select","title":"select  <code>async</code>","text":"<pre><code>select(query: str, *, limit: int = DEFAULT_LIMIT) -&gt; Selection\n</code></pre> <p>Return the best limit skills for query, with scores.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.build_corpus","title":"build_corpus  <code>async</code>","text":"<pre><code>build_corpus(registry: SkillRegistry, *, concurrency: int = 8) -&gt; list[SkillDocument]\n</code></pre> <p>Fetch metadata for every registered skill and index it.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The registry to read.</p> required <code>concurrency</code> <code>int</code> <p>Ceiling on simultaneous metadata fetches, which matters when the provider is network-backed.</p> <code>8</code> <p>Returns:</p> Name Type Description <code>One</code> <code>list[SkillDocument]</code> <p>class:<code>SkillDocument</code> per registered skill, in ID order.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.build_selected_catalog","title":"build_selected_catalog  <code>async</code>","text":"<pre><code>build_selected_catalog(registry: SkillRegistry, selector: SkillSelector, query: str, *, limit: int = DEFAULT_LIMIT, format: Literal['xml', 'markdown'] = 'xml', **catalog_kwargs: Any) -&gt; str\n</code></pre> <p>Rank the registry against query and build a catalog of the winners.</p> <p>The result reports its own narrowing \u2014 <code>shown</code>/<code>total</code> on the XML root, a closing note in Markdown \u2014 because from inside an agent a skill that was ranked out is indistinguishable from one that was never registered, and that is a miserable thing to debug.</p> <p>When nothing clears the selector's floor the full catalog is returned. \"Selection has no opinion\" is not \"the agent should have no skills\": a wrong prune silently removes a capability, which is a worse failure than a few wasted tokens. The fallback is logged.</p> <p>Parameters:</p> Name Type Description Default <code>registry</code> <code>SkillRegistry</code> <p>The registry to build from.</p> required <code>selector</code> <code>SkillSelector</code> <p>Any :class:<code>~agentskills_retrieval.SkillSelector</code>.</p> required <code>query</code> <code>str</code> <p>Text to rank against.</p> required <code>limit</code> <code>int</code> <p>Maximum number of skills to advertise.</p> <code>DEFAULT_LIMIT</code> <code>format</code> <code>Literal['xml', 'markdown']</code> <p><code>\"xml\"</code> or <code>\"markdown\"</code>, as for :meth:<code>~agentskills_core.SkillRegistry.get_skills_catalog</code>.</p> <code>'xml'</code> <code>**catalog_kwargs</code> <code>Any</code> <p>Passed straight through to :meth:<code>~agentskills_core.SkillRegistry.get_skills_catalog</code>. <code>include</code> and <code>total</code> are supplied by this function.</p> <code>{}</code> <p>Returns:</p> Type Description <code>str</code> <p>A catalog string ready for a system prompt.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.cosine","title":"cosine","text":"<pre><code>cosine(left: Sequence[float], right: Sequence[float]) -&gt; float\n</code></pre> <p>Return the cosine similarity of two vectors, or 0.0 if either is zero.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.document_of","title":"document_of","text":"<pre><code>document_of(skill_id: str, meta: dict[str, Any]) -&gt; SkillDocument\n</code></pre> <p>Build the searchable document for one skill's metadata.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.load_embedder","title":"load_embedder","text":"<pre><code>load_embedder(spec: str) -&gt; Embedder\n</code></pre> <p>Resolve <code>module:factory</code> to an embedder.</p> <p>Parameters:</p> Name Type Description Default <code>spec</code> <code>str</code> <p>A dotted module path, a colon, and the name of a zero-argument callable returning an :class:<code>Embedder</code>.</p> required <p>Returns:</p> Type Description <code>Embedder</code> <p>The embedder the factory built.</p> <p>Raises:</p> Type Description <code>ValueError</code> <p>If the spec is malformed, cannot be imported, or does not produce something with <code>embedder_id</code> and <code>embed</code>.</p>"},{"location":"packages/retrieval/#agentskills_retrieval.tokenize","title":"tokenize","text":"<pre><code>tokenize(text: str) -&gt; list[str]\n</code></pre> <p>Split text into lowercase alphanumeric terms, minus stopwords.</p> <p>No stemming: it would need a dependency, and this package's whole claim is that it is useful with none.  The cost is real \u2014 \"deploys\" will not match \"deploy\" \u2014 and it is the main thing an embedding selector buys back.</p>"},{"location":"packages/testing/","title":"Testing Toolkit (<code>agentskills-testing</code>)","text":"<p>Provider conformance suite and in-memory test provider.</p> <pre><code>pip install agentskills-testing\n</code></pre>"},{"location":"packages/testing/#agentskills_testing","title":"agentskills_testing","text":"<p>Conformance suite and test doubles for Agent Skills providers.</p> <p>Two things live here:</p> <ul> <li>:class:<code>ProviderConformanceSuite</code> \u2014 subclass it, supply a <code>provider</code>   fixture, and pytest runs the whole provider contract against your   implementation.</li> <li>:class:<code>InMemorySkillProvider</code> \u2014 a real, spec-compliant provider   backed by a dict, for tests that need a skill but not a disk.</li> </ul> <p>See https://github.com/pratikxpanda/agentskills-sdk for the guide.</p>"},{"location":"packages/testing/#agentskills_testing.ContentLimitConformanceSuite","title":"ContentLimitConformanceSuite","text":"<p>Assertions for providers that read bytes they did not author.</p> <p>A size limit is not part of the universal contract \u2014 an in-memory provider has nothing to bound, and demanding one would be asserting a filesystem's constraints against a dict.  It is required of any provider reading from disk, a network, or anything else a caller can grow without asking.</p> <p>Supply a <code>limited_provider</code> fixture whose limit is small enough that the fixture's own content exceeds it::</p> <pre><code>class TestMyProviderLimits(ContentLimitConformanceSuite):\n    @pytest.fixture\n    def limited_provider(self):\n        return MyProvider(..., max_bytes=8)\n</code></pre>"},{"location":"packages/testing/#agentskills_testing.ContentLimitConformanceSuite.limited_provider","title":"limited_provider","text":"<pre><code>limited_provider() -&gt; SkillProvider\n</code></pre> <p>Return a provider whose size limit the contract skill exceeds.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkill","title":"InMemorySkill  <code>dataclass</code>","text":"<p>One skill's content, held in memory.</p> <p>Attributes:</p> Name Type Description <code>metadata</code> <code>dict[str, Any]</code> <p>Parsed frontmatter. Must carry <code>name</code> and <code>description</code> to satisfy the specification.</p> <code>body</code> <code>str</code> <p>Markdown instructions.</p> <code>references</code> <code>dict[str, bytes]</code> <p>Reference documents by filename.</p> <code>scripts</code> <code>dict[str, bytes]</code> <p>Scripts by filename.</p> <code>assets</code> <code>dict[str, bytes]</code> <p>Assets by filename.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkill.resources","title":"resources","text":"<pre><code>resources(kind: str) -&gt; dict[str, bytes]\n</code></pre> <p>Return the resource mapping for kind.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider","title":"InMemorySkillProvider","text":"<p>               Bases: <code>SkillProvider</code></p> <p>A spec-compliant :class:<code>~agentskills_core.SkillProvider</code> backed by a dict.</p> <p>Parameters:</p> Name Type Description Default <code>skills</code> <code>Mapping[str, InMemorySkill | str] | None</code> <p>Skills by ID. A plain string value is taken as the body of a default skill, so the common case stays short.</p> <code>None</code> <code>supports_resource_listing</code> <code>bool</code> <p>Set <code>False</code> to emulate a backend that cannot enumerate, which is what the HTTP provider does without a manifest.</p> <code>True</code> <code>supports_discovery</code> <code>bool</code> <p>Set <code>False</code> to emulate a backend that cannot list the skills it holds.</p> <code>True</code> <p>Example::</p> <pre><code>provider = InMemorySkillProvider({\"incident-response\": build_skill()})\nregistry = SkillRegistry()\nawait registry.register(\"incident-response\", provider)\n</code></pre>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.add","title":"add","text":"<pre><code>add(skill_id: str, skill: InMemorySkill | str | None = None) -&gt; InMemorySkill\n</code></pre> <p>Add or replace a skill.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>ID to register the skill under.</p> required <code>skill</code> <code>InMemorySkill | str | None</code> <p>The skill, or a string taken as its body. Omit it for a default skill named after skill_id.</p> <code>None</code> <p>Returns:</p> Type Description <code>InMemorySkill</code> <p>The stored skill.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.discover","title":"discover  <code>async</code>","text":"<pre><code>discover() -&gt; list[str]\n</code></pre> <p>Return the stored skill IDs, sorted.</p> <p>Raises:</p> Type Description <code>DiscoveryNotSupportedError</code> <p>If the provider was built with <code>supports_discovery=False</code>.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.get_asset","title":"get_asset  <code>async</code>","text":"<pre><code>get_asset(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Return an asset.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.get_body","title":"get_body  <code>async</code>","text":"<pre><code>get_body(skill_id: str) -&gt; str\n</code></pre> <p>Return the skill's markdown body.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.get_metadata","title":"get_metadata  <code>async</code>","text":"<pre><code>get_metadata(skill_id: str) -&gt; dict[str, Any]\n</code></pre> <p>Return a copy of the skill's frontmatter.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.get_reference","title":"get_reference  <code>async</code>","text":"<pre><code>get_reference(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Return a reference document.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.get_script","title":"get_script  <code>async</code>","text":"<pre><code>get_script(skill_id: str, name: str) -&gt; bytes\n</code></pre> <p>Return a script.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.list_resources","title":"list_resources  <code>async</code>","text":"<pre><code>list_resources(skill_id: str) -&gt; dict[str, list[str]]\n</code></pre> <p>Return the skill's resource names, grouped by kind and sorted.</p> <p>Raises:</p> Type Description <code>ResourceListingNotSupportedError</code> <p>If the provider was built with <code>supports_resource_listing=False</code>.</p> <code>SkillNotFoundError</code> <p>If the skill is unknown.</p>"},{"location":"packages/testing/#agentskills_testing.InMemorySkillProvider.skill_ids","title":"skill_ids","text":"<pre><code>skill_ids() -&gt; list[str]\n</code></pre> <p>Return the registered skill IDs, sorted.</p>"},{"location":"packages/testing/#agentskills_testing.ProviderConformanceSuite","title":"ProviderConformanceSuite","text":"<p>Assertions every :class:<code>~agentskills_core.SkillProvider</code> must satisfy.</p> <p>Subclass it and provide a <code>provider</code> fixture::</p> <pre><code>class TestMyProvider(ProviderConformanceSuite):\n    @pytest.fixture\n    def provider(self):\n        return MyProvider(...)\n</code></pre> <p>The fixture must be populated per :data:<code>CONTRACT</code>.  Nothing here is opt-out: a provider that cannot pass these is not a provider, it is a class with the right method names.</p>"},{"location":"packages/testing/#agentskills_testing.ProviderConformanceSuite.provider","title":"provider","text":"<pre><code>provider() -&gt; SkillProvider\n</code></pre> <p>Return the provider under test, populated per :data:<code>CONTRACT</code>.</p>"},{"location":"packages/testing/#agentskills_testing.build_skill","title":"build_skill","text":"<pre><code>build_skill(skill_id: str = DEFAULT_SKILL_ID, *, description: str = DEFAULT_DESCRIPTION, body: str = DEFAULT_BODY, version: str | None = None, metadata: Mapping[str, Any] | None = None, references: Mapping[str, bytes] | None = None, scripts: Mapping[str, bytes] | None = None, assets: Mapping[str, bytes] | None = None) -&gt; InMemorySkill\n</code></pre> <p>Build a valid :class:<code>InMemorySkill</code>.</p> <p>The defaults produce a skill that passes <code>validate_skill()</code>, so a test that does not care about content does not have to invent any.</p> <p>Parameters:</p> Name Type Description Default <code>skill_id</code> <code>str</code> <p>Becomes the <code>name</code> in the frontmatter.</p> <code>DEFAULT_SKILL_ID</code> <code>description</code> <code>str</code> <p>Frontmatter description.</p> <code>DEFAULT_DESCRIPTION</code> <code>body</code> <code>str</code> <p>Markdown instructions.</p> <code>DEFAULT_BODY</code> <code>version</code> <code>str | None</code> <p>Optional semver string.</p> <code>None</code> <code>metadata</code> <code>Mapping[str, Any] | None</code> <p>Extra frontmatter, merged over the generated fields.</p> <code>None</code> <code>references</code> <code>Mapping[str, bytes] | None</code> <p>Reference documents by filename.</p> <code>None</code> <code>scripts</code> <code>Mapping[str, bytes] | None</code> <p>Scripts by filename.</p> <code>None</code> <code>assets</code> <code>Mapping[str, bytes] | None</code> <p>Assets by filename.</p> <code>None</code> <p>Returns:</p> Type Description <code>InMemorySkill</code> <p>A skill ready to hand to :class:<code>InMemorySkillProvider</code>.</p>"},{"location":"packages/testing/#agentskills_testing.render_skill_md","title":"render_skill_md","text":"<pre><code>render_skill_md(skill: InMemorySkill) -&gt; str\n</code></pre> <p>Render skill as the <code>SKILL.md</code> text a file-backed provider would hold.</p> <p>Useful for populating a temporary directory when a test needs the filesystem provider rather than this one.</p>"},{"location":"packages/tools/","title":"CLI Tooling (<code>agentskills-tools</code>)","text":"<p>Scaffold, validate, lint, inspect, evaluate, and serve skills.</p> <pre><code>pip install agentskills-tools\n</code></pre>"},{"location":"packages/tools/#agentskills_tools","title":"agentskills_tools","text":"<p>Command line tools for authoring and validating Agent Skills.</p> <p>The console script is <code>agentskills</code>.  Every command is also reachable in-process through :func:<code>~agentskills_tools.cli.main</code>, which takes an argument list and returns an exit code rather than calling :func:<code>sys.exit</code> \u2014 that is what makes it testable.</p>"},{"location":"packages/tools/#agentskills_tools.CliError","title":"CliError","text":"<p>               Bases: <code>Exception</code></p> <p>A problem with the invocation itself, not with a skill.</p> <p>Raised for a missing path or an empty directory \u2014 conditions that mean the command could not run, as opposed to running and finding fault with a skill.</p>"},{"location":"packages/tools/#agentskills_tools.Finding","title":"Finding  <code>dataclass</code>","text":"<p>A single problem found in a skill.</p> <p>Attributes:</p> Name Type Description <code>severity</code> <code>str</code> <p>Either <code>\"error\"</code> or <code>\"warning\"</code>.</p> <code>code</code> <code>str</code> <p>Stable machine-readable identifier, e.g. <code>\"frontmatter-invalid-yaml\"</code>.  Callers may key off this; message is free to be reworded.</p> <code>message</code> <code>str</code> <p>Human-readable explanation.</p> <code>line</code> <code>int | None</code> <p>1-based line in the file the finding is about, when the problem can be attributed to one.  <code>None</code> otherwise.</p> <code>file</code> <code>str | None</code> <p>Path the finding is about, relative to the working directory, when it is not the skill's <code>SKILL.md</code>.</p>"},{"location":"packages/tools/#agentskills_tools.SkillLocation","title":"SkillLocation  <code>dataclass</code>","text":"<p>One skill directory and the ID it will be registered under.</p>"},{"location":"packages/tools/#agentskills_tools.SkillReport","title":"SkillReport  <code>dataclass</code>","text":"<p>Every finding for one skill.</p>"},{"location":"packages/tools/#agentskills_tools.SkillReport.errors","title":"errors  <code>property</code>","text":"<pre><code>errors: list[Finding]\n</code></pre> <p>Findings with <code>error</code> severity.</p>"},{"location":"packages/tools/#agentskills_tools.SkillReport.ok","title":"ok  <code>property</code>","text":"<pre><code>ok: bool\n</code></pre> <p><code>True</code> when the skill produced no error-severity findings.</p>"},{"location":"packages/tools/#agentskills_tools.SkillReport.warnings","title":"warnings  <code>property</code>","text":"<pre><code>warnings: list[Finding]\n</code></pre> <p>Findings with <code>warning</code> severity.</p>"},{"location":"packages/tools/#agentskills_tools.discover","title":"discover","text":"<pre><code>discover(target: Path) -&gt; tuple[Path, list[SkillLocation]]\n</code></pre> <p>Resolve target into a provider root and the skills beneath it.</p> <p>A directory containing <code>SKILL.md</code> is a single skill, and its parent becomes the provider root because :class:<code>~agentskills_fs.LocalFileSystemSkillProvider</code> addresses skills by directory name below a root.  Any other directory is treated as a collection, and each immediate subdirectory holding a <code>SKILL.md</code> is one skill.</p> <p>Parameters:</p> Name Type Description Default <code>target</code> <code>Path</code> <p>Path to a skill folder or a folder of skill folders.</p> required <p>Returns:</p> Type Description <code>Path</code> <p>A <code>(root, locations)</code> tuple.  locations is sorted by skill</p> <code>list[SkillLocation]</code> <p>ID and is never empty.</p> <p>Raises:</p> Type Description <code>CliError</code> <p>If target is not a directory, or is a directory with no skills in it.</p>"},{"location":"packages/tools/#agentskills_tools.main","title":"main","text":"<pre><code>main(argv: list[str] | None = None, out: TextIO | None = None) -&gt; int\n</code></pre> <p>Run the <code>agentskills</code> command line.</p> <p>Parameters:</p> Name Type Description Default <code>argv</code> <code>list[str] | None</code> <p>Arguments to parse.  Defaults to <code>sys.argv[1:]</code>.</p> <code>None</code> <code>out</code> <code>TextIO | None</code> <p>Stream to write reports to.  Defaults to stdout.</p> <code>None</code> <p>Returns:</p> Type Description <code>int</code> <p>A process exit code.</p>"}]}